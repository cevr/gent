name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    # The build compiles for the host (apps/tui/scripts/build.ts, the gent-cell compile); the owner runs darwin-arm64.
    # A pinned arm64 image, not macos-latest, whose image moves under the job.
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Install
        run: bun install --frozen-lockfile

      - name: Gate
        run: bun run gate

      - name: E2E
        run: bun run test:e2e
