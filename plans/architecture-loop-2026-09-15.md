# Per-package architecture loop (2026-09-15 →)

Goal (user): run `/improve-codebase-architecture` on each package until no
more findings, compared against opencode v2 (Effect) and pi (minimalism);
add and remove while keeping capabilities. Effect-native, actor model, lean
core, fully extensible. Baseline `74b1b5d5`: core 27.6k, tui 24.1k,
extensions 16.5k, tooling 2.3k, sdk 2.0k, server 0.7k, e2e 0.3k src LOC.
pi has no `v2` branch on its origin; opencode's `v2` worktree is the prior
the Effect comparison uses.

Each row: the finding, the deletion-test verdict after my own read, and
what happened. A pass ends for a package when a run yields nothing Strong
or Worth exploring.

## sdk / server / tooling / e2e — pass 1

| #   | Finding                                                                                                                                     | Verdict                                                             | Status                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1a | `apps/server/src/debug/scenario.ts` (520) unreachable: `GENT_DEBUG_MODE` read once, set nowhere; `debug-failing`/`debug-slow` had no setter | confirmed by grep                                                   | landed `0ec4f226`                                                                                                                                                                   |
| S1b | `apps/server/src/main.ts` re-implements `Gent.server` (SDK) as a second root                                                                | real; fixed port + idle shutdown must move onto `GentServerOptions` | landed `0a9aca6e` (main.ts 205 → 84; port/serverId/shell/idleShutdown on `GentServerOptions`; launcher guard)                                                                       |
| S2  | six `packages/e2e` files (1,110 LOC) spawn no process and sit outside the gate                                                              | confirmed (`transportCases` has one direct case)                    | landed `81a5281d` (e2e 2,172 → 930 LOC; six duplicates deleted, rest moved into core gate; pre-existing red: e2e auth PTY test waits for "ChatGPT Pro/Plus", fails on a3c424d5 too) |
| S3  | workspace id hashed twice (`transport-headers.ts` vs `platform.hash`)                                                                       | real, but `client.ts` uses it as a default parameter                | landed `871a2607` (`workspace-rpc.ts` owns `workspaceIdForCwd`; wire constant, not a host fact; third copy in a core test removed)                                                  |
| S4  | `shipped-extensions.ts` alias used by one of the two roots it claims to serve                                                               | pass-through                                                        | landed `0ec4f226`                                                                                                                                                                   |
| S5  | four tooling guard suites never ran (`package.json` named five files); six assertions had drifted to `code-cell`                            | confirmed                                                           | landed `0ec4f226` (`./tests/`; a lint fixture under `fixtures/` must stay out of the glob)                                                                                          |
| S6  | suppression inventory carried four kinds oxlint already bans, and scanned block disables twice                                              | confirmed (zero occurrences of each dead kind)                      | landed `0ec4f226`                                                                                                                                                                   |
| S7  | `core-dead-exports` scans core only; sdk surface pinned by a name snapshot                                                                  | Worth exploring                                                     | landed `b0424020` (`dead-exports.ts` scans core + sdk; snapshot kept, the two guards catch different plants; extensions not scanned, 123 false positives from Schema.Class)         |
| S8  | `runtime-boundary.ts` four-line trampoline for the `runPromise` lint rule                                                                   | Speculative; rule is load-bearing                                   | rejected                                                                                                                                                                            |

## tui — pass 1

| #   | Finding                                                                                                                           | Verdict                                                                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | `use-session-feed.ts` re-projects what `session.getSnapshot` already returns as `ProjectedMessage` (~500 LOC parallel projection) | Strong; pi single-sources, opencode duplicates and is larger                                                                                                                                                                                                                                         | landed `cfe8fdcb`/`ecbaaaa4`/`ea6835c2`: `ProjectedMessage.segments` in core; duplicate replay/live handler collapsed; hook 978 → 933 (live overlay + UI state stay); `AssistantMessageLegacy` is test-vehicle-only, migrate 10 fixtures before deleting |
| T2  | `atom-solid/` (684) has no production consumer beyond a `RegistryProvider` mount and one `Result` type                            | confirmed by grep                                                                                                                                                                                                                                                                                    | in flight (rift `tui-dead`)                                                                                                                                                                                                                              |
| T3  | six panes hand-write the same keyboard/scroll/filter block; `filter-list-state.ts` too shallow                                    | Strong; pi has one `showSelector`                                                                                                                                                                                                                                                                    | DONE 37381f3f: `SelectList` takes the block; five panes on it, `filter-list-state.ts` deleted; wake and btw own no list                                                                                                                                  |
| T4  | `composerSurface`, `ClientComposer`, `paletteLevel` seams have zero adapters                                                      | confirmed by grep                                                                                                                                                                                                                                                                                    | in flight (rift `tui-dead`)                                                                                                                                                                                                                              |
| T5  | 180-LOC router with history for two routes; branch picker is a boot-time pane                                                     | Strong                                                                                                                                                                                                                                                                                               | landed `b6844f84` (router deleted; `navigateToBranchPicker` had zero callers so history was always empty; branch picker is a boot-time docked pane over the mounted session)                                                                             |
| T6  | four-way client context split re-merged by every consumer                                                                         | Worth exploring; order with T1                                                                                                                                                                                                                                                                       | landed `d2faf1cb`: one `ClientContext` + `useClient()`, value built once (identity-stable); six facet hooks removed; helper modules stay (pure, tested subjects); client/ 1,339 → 1,259                                                                  |
| T7  | auth is 1,236 LOC + 1,706 test LOC for an ops-time flow                                                                           | Worth exploring; High risk; after T3                                                                                                                                                                                                                                                                 | landed `d65df2ad`: both pickers on `SelectList`, dead in-flight flags and cursor events removed, catalog is `Option`; src 1,136 → 675 code lines; e2e auth PTY red fixed (label is "Claude Code", not "ChatGPT Pro/Plus")                                |
| T8  | child-session tracker is a third live-agent catalog                                                                               | **rejected**: the tracker is a push projection over `session.events` (0 req/min, one stream per running child) keyed by `toolCallId` with `toolCalls`/`streamText`; the two polled catalogs (`list-agents`, ≤30 req/min ceiling, gated) carry neither. Folding would put live text behind a 2 s poll | —                                                                                                                                                                                                                                                        |

## core — pass 1

| #   | Finding                                                                                                                                                                             | Verdict                                                                                                                                                                                                                                                            | Status                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | turn position inferred by probing 200 message ids; three stacked replay mechanisms (durable bindings, process-local map, persisted results); pi/opencode write the position durably | Strong; the structural one                                                                                                                                                                                                                                         | probe replaced: landed `b86a1040`/`80ea0b9f`/`af2b5441` (`turn_records` table, migration 018, written with the step's messages; resume reads one row). Replay collapse **rejected on evidence**: bindings (4 tests), process-local (15), persisted results (1, newly covered: a cut two-call step) each carry a capability the record cannot express. Gamut run 18 green (19 tests, 3m10s, 8 records written) |
| C2  | `SessionRuntime` is a validating pass-through to the actor                                                                                                                          | **rejected**: eight in-core consumers (`agent-runner`, `child-completion`, `session-mutations-live`, `rpc-handlers`, tests) plus the extension host; it is the actor client, and the message-id/commandId minting lives there                                      | —                                                                                                                                                                                                                                                                                                                                                                                                             |
| C3  | `StepResult.Stop` re-encodes `StepOutcome` as three booleans                                                                                                                        | **rejected as stated**: the booleans are the turn-level end state after continuation policy (a `Failed` step can `Continue`), and `TurnCompleted` carries them; a `TurnEnding` tag would save ~40 LOC, not 120                                                     | —                                                                                                                                                                                                                                                                                                                                                                                                             |
| C4  | `Session`/`Files` facets facaded twice (`extension-services.ts` + `make-extension-host-context.ts`)                                                                                 | Worth exploring                                                                                                                                                                                                                                                    | landed `e40a1e4b`: one build site per facet; four facets were rebuilt per leaf, `Session` alone was double-wrapped; workspace pin now read from the actor entity id (follow-up: pin `Interaction` too)                                                                                                                                                                                                        |
| C5  | queue is three untagged arrays; `startsWith("follow-up:")` policy in `agent-loop.state.ts:119`; both priors use one tagged inbox                                                    | Strong on the prefix check; the merge heuristic is product behaviour                                                                                                                                                                                               | prefix gone: `QueuedTurnItem.keyed` replaces the id-prefix test; three arrays stay (product heuristic)                                                                                                                                                                                                                                                                                                        |
| C6  | six single-importer modules (2,970 LOC) in the turn path                                                                                                                            | Worth exploring; both priors accept flat files                                                                                                                                                                                                                     | landed `b8449ddd`/`236f9a2a`/`cc80c72a`: queue, handlers, actor-state, turn-source folded into their one importer (bodies byte-identical); `worker` and `turn-tool-execution` stay (test subjects); the other seven have 3+ importers (ledger count was stale); 29 → 25 files. Next real seam: `agent-loop.turn-execution.ts` is 1,992 LOC                                                                    |
| C7  | `AgentRunnerService` Tag has one adapter; child policy (depth, visibility, caps) in core                                                                                            | Tag removal **rejected**: `stubAgentRunnerLayer` (test-root.ts) is a second adapter installed by every tool harness and 40 of 48 e2e harnesses; live-runner probe fails `delegate-rpc.test.ts:65` (prompt propagation). Child policy in core stays Worth exploring | policy half **rejected** `036f4c98`: two non-delegate spawners (btw, read-session) need `private` cleanup that no facade exposes; no extension declares the caps. Probe found the depth test never called admission; both depth tests now go through `admitChildSession`, plus a pending-cap delegate test. Defect found: `createSession` handoff bypasses the depth cap (rift `handoff-depth`)               |
| C8  | `transactWithEvent` twice; `makeInteractionService` factory with one instance                                                                                                       | **rejected**: the two helpers differ in shape (fixed events vs mutation-decided envelope); inlining the factory costs a 343-line test rewrite for ~30 LOC                                                                                                          | —                                                                                                                                                                                                                                                                                                                                                                                                             |

## extensions — pass 1

The agent's original E5/E7/E8 text was lost with the transcript; the rows
below record what was verified and acted on.

| #   | Finding                                                                                                                         | Verdict                                                                                                                                                                                                                                                                           | Status                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | `promptGuidelines` on read/grep/bash repeated the same "use the fs tools" prose three times                                     | confirmed                                                                                                                                                                                                                                                                         | landed `56ca6bcc` (one bash guideline; read/grep carry none)                                                                                                        |
| E2  | bash read-only detection was a prefix check; compound commands (`ls; rm`, `$(...)`, `xargs`, `-exec`) slipped past as read-only | Strong                                                                                                                                                                                                                                                                            | landed `56ca6bcc` (`classifyBashCommand`: every segment read-only and no hidden command)                                                                            |
| E3  | anthropic and openai provider modules each own a credential cache with the same persist/invalidate shape                        | Strong; one `makeCredentialCache<C>` behind both Tags                                                                                                                                                                                                                             | landed `7746f9d0`                                                                                                                                                   |
| E4  | ACP adapter could sit on the vendor SDK instead of the hand-rolled protocol half                                                | **rejected**: `@agentclientprotocol/sdk` 1.4.0 is +2 packages (~6 MB) and brings zod beside effect/Schema; only `protocol.ts`+`schema.ts` (685 LOC) are replaceable and the seam (`AcpSessionManager`/`TurnExecutor`) is already right; no fake peer exists to make the swap safe | bug found: `response-finish.ts` switches on AI finish reasons, ACP's `end_turn`/`max_tokens`/… never match → landed (fix + in-memory peer: 36 ACP tests, 25 probes) |
| E5  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E6  | eight cell test files gated on `process.platform === "darwin"` although the cell has no darwin-only dependency                  | confirmed (suite green ungated)                                                                                                                                                                                                                                                   | landed `56ca6bcc`                                                                                                                                                   |
| E7  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E8  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E+  | model compaction sent whole messages to the summarizer; one oversized tool result starved the older turns                       | Strong (found while reading E1)                                                                                                                                                                                                                                                   | landed `c5858158` (`MODEL_COMPACTION_MESSAGE_CHARS` clip per message)                                                                                               |

## extensions — pass 2

Read-only agent run after 7746f9d0; E9 verified by hand against
`boundToolResultForModel` (core bounds every tool result and pages with
`context.read`). Cell subtree, `provider-credentials.ts`, `acp-agents/` and
`workflows.ts` reported clean.

| #   | Finding                                                                                                                       | Verdict                                                          | Status                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------ |
| E9  | bash truncates a second time (50/50 lines) and spills to `/tmp/gent/outputs`; core already bounds and pages every tool result | Strong; pi keeps one truncation policy                           | landed `716bd7ae` (−202 LOC; stderr now returned separately) |
| E10 | `websearch.ts` (202 LOC) parses an external wire format with no test                                                          | Strong (untested surface, not shallow)                           | landed `8b514dbe`                                            |
| E11 | `readOptionalEnv` defined twice; three drivers hand-build the same contribution literal                                       | dedup Strong; the retry literals are real per-provider facts     | landed `21701c3d` (dedup only)                               |
| E12 | `read` reports `truncated` with no continuation offset                                                                        | Worth exploring; `nextOffset` matches the cell vocabulary        | landed `9a9d0330`                                            |
| E13 | `read-session.ts` exports a `truncate` no caller uses                                                                         | confirmed by grep                                                | landed `21701c3d`                                            |
| E14 | `OutputBuffer.getFullText()` dead and self-documented as broken                                                               | confirmed (one hit: the definition)                              | landed `716bd7ae`                                            |
| E15 | `dispatching-tool-storage.ts` is a 17-line alias with one consumer                                                            | Speculative; the consumer is the core-feature-independence guard | rejected, the guard needs it                                 |

## storage — found while verifying T5 (2026-09-15)

Four `BranchSummarized` rows from a removed feature made session
`01a0836b` unloadable ("Failed to load session events" on every reconnect).
Landed `4e8b2334`: `listEvents` skips rows whose tag left the `AgentEvent`
union and logs `event.retired-tag-skipped`; a known tag with a bad payload
still fails as corruption. Verified in pane `wZ:p18` on the same session.

## batch review (2026-09-15, commits 629e300c..e40a1e4b)

Codex review: one "blocking" (bun.lock) was a false positive (frozen install
clean); eight should-fix items landed in one revision round:
`a348e32c` (transcript commit re-checks mode/generation, clear goes through
the queue, composer shell spill under `~/.gent/shell-output`, SelectList
resets an external filter and clears the cursor on unmount), `79c28c74`
(background bash notice bounded with the `context.read` locator),
`6fe86a21` (test `Files` facet uses the real `Path`), `f5e7f2e5`
(in-process clients count toward idle shutdown).

## tui — markdown history (user request, 2026-09-15)

`NativeTranscript` committed items with the synchronous
`writeSolidToScrollback`, before opentui's async tree-sitter highlight
concealed `##` and backticks; opencode awaits `ScrollbackSurface.settle()`
first. Landed `629e300c`: surface render, settle, commit, serialized on one
fiber chain with the replay reset. Verified in pane `wZ:p18` on session
`01a0a3c8-9c97-759e-ac97-4edefea5384f`.

## tui — pass 2 (2026-09-15, after `044ed18f`)

Claims re-checked by grep on main before acceptance.

| #   | Finding                                                                                                                                            | Verdict                                                                | Status                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T9  | agents and thread panes hand-roll the docked chrome box and geometry; `CHROME_ROWS` disagrees (6 vs 5); `ChromePanel` owns only the floating arm   | Strong; add `ChromePanel.Dock`                                         | landed `337a7390`: `ChromePanel.Dock` + `useDockGeometry`; chrome rows claim themselves (`Section`/`Footer`/`Error`/`Success`); agents 714 → 675, thread 555 → 537, settings 136 → 118; branch picker kept (different height rule)                                         |
| T10 | four truncation policies; only `picker-text.ts` measures grapheme width; `wake.client.tsx` re-declares `truncate`                                  | Strong; one width-aware primitive                                      | landed `9e98c718`: `utils/truncate.ts` (grapheme + `Bun.stringWidth`) is the one column cut; three copies deleted, `truncateText`/`truncateDescription` folded; `truncatePath` kept (path policy); receipt: old `truncate("漢字漢字漢字", 8)` returned the input unchanged |
| T11 | `SessionController` has 36 members; `client` re-exposes `useClient()`; five prompt-search accessors over one signal; test mock is a 30-key literal | Strong; drop `client`, bundle prompt search                            | landed `b8ec28a7`: interface 32 → 26 members (`client`, five prompt-search/ui accessors, `clearMessages` dropped; `promptSearch` is one member); test mock 30 → 24 keys                                                                                                    |
| T12 | `ClientDriver` service + layer + facet union member + two wirings for one consumer (`driver.client.ts`); errors pre-mapped to `Error`              | Strong; three RPC wrappers on `ClientTransport`                        | landed `98dd2ee8`: `driverList`/`driverSet`/`driverClear` on `ClientTransport`; `ClientDriver`, its layer, facet member and both wirings deleted; tagged transport error kept (`driver-transport.test.ts`)                                                                 |
| T13 | `requestExtension` has three overloads; the explicit-transport arm has zero production callers; one service-arm caller in `builtins/index.ts`      | Worth exploring; delete the overloads, caller uses `transport.request` | landed `8a45f268`: `requestExtension` deleted (typecheck named two consumers); skills autocomplete yields the transport                                                                                                                                                    |
| T14 | `prompt-search-flow.ts` is a filter + rename with one caller; its effect type duplicates `SessionUiEffect`                                         | Strong; inline                                                         | landed `dc97bd76`: file deleted, filter+map inlined in `transitionSessionUi`; three reducer tests added                                                                                                                                                                    |
| T15 | `main.tsx` (headless) and `extensions/context.tsx` build the same seven-layer client `ManagedRuntime`; headless stubs five shell callbacks         | Strong; one `makeClientRuntime` factory                                | landed `dce13b49`: `extensions/client-runtime.ts` `makeClientRuntime(deps)` with no-op defaults; four sites call it; context.tsx 339 → 314, main.tsx 763 → 730                                                                                                             |
| T16 | `Effect.catchEager(... client.setError(formatError(e)))` written ten times across three files                                                      | Worth exploring; one `surfaceError` combinator                         | landed `84a546d7`: `surfaceError` on the client value; 9 sites rewritten (the `respondInteraction` `tapError` site keeps the `cast.failed` log)                                                                                                                            |

Untested surfaces named by the pass: `use-composer-controller.ts` (577 LOC) and `session-command-registry.ts` (244 LOC) have no direct tests; `createPasteManager` and `describeAmbiguous`/`slashAutocompleteItems` are pure and testable. Sequenced after the `AssistantMessageLegacy` fixture migration.

## sdk / server / tooling / e2e — pass 2 (2026-09-15, after `044ed18f`)

`apps/server` (86 LOC) is a launcher; no finding.

| #   | Finding                                                                                                                                                                                                            | Verdict                                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S9  | e2e `pty-fixture.ts` hardcodes `~/.gent/logs/Users-cvr-.../gent-client.log`; the TUI writes `/tmp/gent/logs/<hash>-<ts>-client.log`; `shutdown.watchdog-fired` is emitted by nothing, so the assertion cannot fail | Strong; delete fixture + test (exit-code half duplicates the double-ESC test)                                                                                                                                                                                        | landed `dbabfc50`: fixture and test deleted; e2e.test.ts 295 → 271                                                                                                                                                                                                                                                                                                                                                                                               |
| S10 | `packages/sdk/src/server.ts` is the only shipped file importing `test-utils` (`LanguageModelLayers`); `ProviderSpec.mock` puts four test knobs on the SDK option surface                                           | **deferred**: `--debug` and `GENT_PROVIDER_MODE=debug-scripted` are product modes with three shipped callers; moving the layer into the launchers needs a carve-out in `bannedLauncherPatterns`, tightened at `0a9aca6e`. Revisit if a second debug provider appears | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| S11 | `canSignalServerLockEntry` is exported for tests only; `dead-exports.ts` counts test files as consumers while `core-public-api-consumers.ts` does not                                                              | Worth exploring; un-export, test through `signalIfIdentityOwned`                                                                                                                                                                                                     | landed `a923a248`: export dropped; new test through `signalIfIdentityOwned` fails with the hostname check removed                                                                                                                                                                                                                                                                                                                                                |
| S12 | two `server-lifecycle.test.ts` suites (lines 103, 145) use no subprocess helper; one duplicates `server-lock.test.ts:142`                                                                                          | Worth exploring; move to sdk tests, then add the process-only guard                                                                                                                                                                                                  | landed `854078eb`: two suites moved to sdk tests (server-lifecycle 296 → 190); `e2e-fixture-imports.ts` guard fails on a copied in-process suite                                                                                                                                                                                                                                                                                                                 |
| S13 | `dead-exports.ts`, `core-public-api-consumers.ts`, `core-public-exports.ts` answer "does this export have a consumer" with three scopes and three test policies                                                    | Worth exploring; one table-driven scan; carries the Schema.Class-aware extensions rule                                                                                                                                                                               | landed `1f0557c1`: `export-consumers.ts` one `SCANNED_SURFACES` table + `PACKAGE_SURFACES`; three files deleted; core/sdk rows keep `testsCount: true` (flipping adds 36 + 6 findings); extensions row report-only, flags 9 names (acp-agents/schema.ts `TextContent`, `ImageContent`, `McpServerStdio`, `McpServerHttp`, `McpServerSse`, `CancelNotification`, `SessionUpdate`, `PermissionOutcome`; handoff-tool.ts `HandoffError`), then set `enforced: true` |
| S14 | `workspace-test-runner.ts` hardcodes five packages; a new package is silently skipped                                                                                                                              | Worth exploring; derive from `scripts.test`                                                                                                                                                                                                                          | landed `c91dbd98`: `workspace-test-packages.ts` derives core/extensions/sdk/tooling/tui from `scripts.test`; e2e (`test:e2e` only) and core-internal (no scripts) skipped                                                                                                                                                                                                                                                                                        |
| S15 | `transport-headers.ts` is an 11-line re-export with three in-package importers                                                                                                                                     | Speculative; skipped                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## core — pass 2 (2026-09-15, after `044ed18f`)

src 28,112 LOC; storage, membrane, platform, publisher, mutations, test-utils passed the deletion test.

| #   | Finding                                                                                                                                                                                                      | Verdict                                                       | Status                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C9  | `modelCapabilities`/`rpcCapabilities`/`modelToolCount` are bucket accessors with one importing file (`activation.ts`, four sites); seven production sites read the buckets directly, including `registry.ts` | Strong; delete, read the bucket                               | landed `3cad81e9`: accessors deleted; contribution.ts 92 → 72                                                                                                                          |
| C10 | `AgentRunSucceeded.preview` has two producers with different clip policy (`agent-runner.ts:539` appends `…`, `child-completion.ts:153` cuts silently); core carries four clip dialects                       | Strong on the divergence; one `clipPreview` beside the event  | landed `29b6453d`: `clipChars` in head-tail.ts, `clipPreview` beside the event; receipt: background child emitted 300 chars with no marker before the fix (`delegate-preview.test.ts`) |
| C11 | effective driver id + context model id derived at three sites (`model-resolver.ts:60`, `retry.ts:18`, `turn-execution.ts:507-527` twice); `driverRetryPolicy` re-resolves what its caller just resolved      | Worth exploring; `resolveAgentDriver` returns the derived ids | landed `4be07a21`: `effectiveModelDriver` in domain/agent.ts, computed once in `turn-resolve.ts` as `ResolvedTurn.modelDriver`; turn-execution 1992 → 1976; five routing tests         |

C7 policy scope note from the pass: the three caps live in `domain/agent.ts:188-192`; moving policy out means moving the `admitChild` branches and the durable reservation in `session-operation-storage.ts`, not the numbers.

## defects found by the pass 2 probes (2026-09-15)

| #   | Finding                                                                                                                                                                      | Status                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | `SessionMutations.createSession` accepted `parentSessionId` with no depth check; the compaction handoff nested past `DEFAULT_MAX_AGENT_RUN_DEPTH` (comment claimed coverage) | landed `e909c4a2`: `runtime/session-depth.ts` owns the rule for `admitChildSession` and `createSession`; `SessionDepthLimitError` on the RPC union; guard `core-child-session-depth.ts` fails when a `parentSessionId` writer skips it |
| D2  | `apps/tui/integration/session-feed-boundary.test.tsx` imported the router deleted at `b6844f84`; the directory was outside typecheck, so `test:e2e` failed silently          | landed `d0d4c9f1`: integration dir in `apps/tui/tsconfig.json` include and turbo inputs; test:e2e 22/1 → 26/0                                                                                                                          |

## extensions — dead names after S13 (2026-09-15)

Landed `742a80c4`: 16 names deleted from `acp-agents/schema.ts` and `handoff-tool.ts` (the nine the scan flagged plus seven the deletions exposed); extensions row `enforced: true`, probe exits 1 on a planted export. Scan blind spot noted: `testsCount: true` counts names inside fixture strings in `packages/tooling/tests/export-consumers.test.ts`, which hid `SessionUpdate` and `HandoffError`. Fix candidate: exclude `packages/tooling/tests` from consumer scans.

Gamut run 19 at `b91cce78` (sonnet-sonnet): 19 tests green, 2m22s, $0.98, 16 steps, 5 children, turn_records max step 16.
Gamut run 20 at `7f2648c3` (sonnet-sonnet, binary at `79341310`, after C17 split, S21-S25 logger, T24-T27): 19 tests green, 2m39s, $0.96, 17 steps, 5 children, all six tasks; TUI rendered the full run with the 15 theme colors gone (T24 pane check).

## core — pass 3 (2026-09-15, after `84a6093c`)

src 28,167 LOC. server/, domain/, providers/, runtime/ periphery, storage/, registry checked; nothing else above Speculative. Stale doc: `packages/core/AGENTS.md` names `recordToolResult`, which no longer exists.

| #   | Finding                                                                                                                                                | Verdict                                              | Status                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| C12 | `EventStoreService.broadcast` is a Tag member with zero production callers; `deliver` is the acked, serialized write path (opencode has one `publish`) | Strong; drop from the Tag, keep as the private input | landed `74e6c16e`: `broadcast` dropped from the Tag; `registry.broadcast` stays the private input to `makeSerializedEventDelivery` |
| C13 | `listSlashCommands` is a C9-shaped bucket accessor; one production caller already holds `resolved`                                                     | Worth exploring; delete                              | landed `f9c029ad`: accessor deleted; handler reads `getResolved().slashCommands`                                                   |
| C14 | `truncateDisplayText` in message-part-display.ts is byte-equivalent to `clipChars(text, max, "…")`                                                     | Worth exploring; fold                                | landed `7a2b9df6`: folded onto `clipChars`                                                                                         |
| C15 | `estimateTextTokens` re-declared in `extensions/src/compaction/model-compaction.ts`; compaction budget can desync from the projection budget           | Worth exploring; one export                          | landed `90950007`: compaction imports the core estimate; test proves a `/3` copy desyncs the summary bound                         |
| C16 | `ExtensionProtocolError.phase` has six literals; one producer, the `phase` parameter is never passed; the TUI ignores the field                        | Worth exploring; narrow or drop                      | landed `5f350e9f`: `phase` removed, helper inlined; no wire consumer read it                                                       |

## sdk / server / tooling / e2e — pass 3 (2026-09-15, after `84a6093c`)

sdk 2,160, server 86, tooling 2,505, e2e 311 src LOC. Client, namespaced proxy, lock, logger, fingerprint, guards, fixtures checked; nothing else above Speculative.

| #   | Finding                                                                                                                                                                                               | Verdict                                                                   | Status                                                                                                                                                                                                                                            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S16 | `packages/tooling` has no tsconfig and no `typecheck` script; root tsconfig `include: []`; 2,505 LOC of guards never typechecked (D2 shape)                                                           | Strong; add tsconfig + script, fix what it reports                        | landed `d609aa77`: tsconfig + `typecheck` script; first run 29 errors / 44 warnings fixed; planted type error fails the root typecheck                                                                                                            |
| S17 | `suppression-inventory.ts` `line` field is written, never read (`_line` discarded); 46/51 entries drifted, 12 name suppressions that no longer exist, 16 exact duplicates                             | Strong; drop `line`, dedupe, delete dead, flag an approval with no match  | landed `160d771a`: `line` dropped; 52 → 36 (dedupe) → 26 rows, matching the 26 `@effect-diagnostics` comments in the tree; `findUnusedSuppressionApprovals` fails on a dead approval; file 360 → 217                                              |
| S18 | `apps/server/src/main.ts` reads `GENT_CWD`, `GENT_DB_PATH`, `GENT_SERVER_ID`; no setter anywhere (S1a shape)                                                                                          | Worth exploring; delete the three reads (personal library, no operator)   | landed `ae9d6da4`: three reads removed; main.ts 86 → 79                                                                                                                                                                                           |
| S19 | `GentObservability`, `seedDebugSession`, `BuildFingerprint` on the `@gent/sdk` root reach only the name snapshot test; the scan has no entry-point row for the SDK index (opencode root is six lines) | Strong; drop from index, add a `@gent/sdk` row to `SCANNED_SURFACES`      | landed `58dbc924`: `@gent/sdk` entry-point row; probe named 14 names (the three plus 11 type re-exports consumed via `@gent/core/protocol`); all removed, index.ts 73 → 58                                                                        |
| S20 | six e2e PTY tests assert only that the prompt glyph or typed text is still on screen, or an output byte count                                                                                         | Worth exploring; delete three, give two an observable assertion or delete | landed `228c52bd`: three tautological tests and the `-H` duplicate deleted; session test runs `--mock-empty` offline and waits for "Generating"; e2e 19 → 15 tests, 46 s → 33 s. Side finding: the old test hit the live provider with a fake key |

Cross-package note for tui pass 3: `apps/tui/src/ops/local-health.ts:168-174` declares a second `ServerLockEntry` schema beside the SDK's.

## tui — pass 3 (2026-09-15, after `84a6093c`)

src 22,698 LOC. Dead-export scan over ~501 exports: zero unused. Every file above 250 LOC read; context stack and extension pipeline clean.

| #   | Finding                                                                                                                                                                           | Verdict                                                        | Status                                                                                                                                                                                                                                                                                                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T17 | `bordered-input.tsx` component (`BorderedInput`, `BorderLine`) has no consumer; only `formatCwdGit`/`formatElapsed` are imported; `utils/border-segments.ts` serves the dead half | Strong; delete, move the formatters                            | landed `5bb7c42a`: component, `border-segments.ts` and its test deleted; `formatCwdGit` + `BorderLabelItem` live in `utils/session-labels.ts` with five new tests                                                                                                                                                                                                  |
| T18 | `createThemeView` is 55 hand-written getters forwarding to `values()`; a missed getter is a silent runtime hole                                                                   | Worth exploring; one reactive view built from the keys         | landed `06b97392`: view built from the resolved keys (`for key in` over an `untrack` spread, no `as`); context.tsx 305 → 155; two tests                                                                                                                                                                                                                            |
| T19 | `Theme._hasSelectedListItemText` and `thinkingOpacity` declared, resolved, proxied, set in fx.json, never read; `tint` barrel export has no importer                              | Strong; delete                                                 | landed `862c497d`: both fields and the fx.json value deleted; `tint` module-private                                                                                                                                                                                                                                                                                |
| T20 | four duration formatters, three dialects (`2m05s` vs `2m 5s`); `formatElapsed` ≡ `formatThinkTime(ms/1000)`                                                                       | Worth exploring; one `utils/format-duration.ts`                | landed `90cec971`: `utils/format-duration.ts` with `compact`/`padded`/`precise` (the third ladder was live in tool receipts); visible output unchanged per call site                                                                                                                                                                                               |
| T21 | `autocomplete-popup.tsx` inlines a fourth wrap-around selection; with command palette and prompt-search still off `SelectList`, `PickerFrame` exists only to share a frame        | Worth exploring; finish the SelectList migration for all three | landed `12891404`/`a656492a`/`47f6dc3a`: palette, prompt search, autocomplete on `SelectList` (`api.reset`/`moveTo` added; backspace on empty query is a no-op); `moveSelection` ×2, inline wrap, `handleKey` deleted; break probe: clamping `wrapIndex` fails 5 tests across the three surfaces; `PickerFrame` kept (two callers, not `ChromePanel.Root`'s shape) |
| T22 | `branch-picker.tsx` re-declares the Dock box; `Dock` now takes `bodyRows`                                                                                                         | Speculative; needs a pane check; deferred                      | —                                                                                                                                                                                                                                                                                                                                                                  |
| T23 | `ops/local-health.ts:168-174` declares a second `ServerLockEntry` schema beside the SDK's (main.tsx imports the SDK one)                                                          | Strong; one decoder                                            | landed `3f19d835`: doctor report takes `Option<ServerLockEntry>` from the SDK decoder; unreachable "Invalid server lock" branch gone; two tests                                                                                                                                                                                                                    |

Pane check at `4d38c673` (wZ:p18, gamut cwd): `/` autocomplete wraps on Up, filters on `mo`, closes on Escape; Ctrl+P palette wraps on Up; Ctrl+R prompt search previews `echo second-cmd` on Down and accepts on Enter.

## extensions — pass 3 (2026-09-15, after `91327886`)

src 16,029 LOC, 98 files. cell, providers, acp-agents, fs/exec/network/session/interaction tools, compaction, delegate, skills, btw checked; authoring rule holds (no leaf imports a core-internal Tag).

| #   | Finding                                                                                                                                                                                            | Verdict                                                                               | Status                                                                           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| E16 | `@gent/extensions/client` (src/client.ts, 37 lines of re-exports) has no scan row; `declarationsIn` skips re-exports without a `specifier`; 14 of 24 names have no outside consumer (S19 shape)    | Strong; entry-point row + drop the dead names                                         | done `7a329955` (entry-point scan row; guard regex covers `type X as Y`)         |
| E17 | goal-store.ts and wake/index.ts hand-roll staging + `Files.rename`; `Files.write(..., { atomic: true })` already does it scoped; the copies leak `.tmp` on failure; `rename` has no other consumer | Strong; use the flag, drop `rename` from the facet; then share the branch-state store | done `0c03fb9d` (atomic flag, `rename` dropped, shared store; `.tmp` leak probe) |
| E18 | six `new CapabilityError({ extensionId, capabilityId })` sites re-state ids `request(...)`/`defineRequests` already hold                                                                           | Worth exploring; `request` wraps the error channel (core edit)                        | done `f2a86f12` (`request` wraps handler errors; one new suppression)            |

## extensions — pass 4 (2026-09-15, after `643b68c2`)

src read in full; E16-E18 closed. Below the bar (not proposed): `read-session.ts` `Option.fromNullishOr` on a non-nullable, goal snapshot ↔ Option triple conversion, openai pending-callback raw Map, test-only layer seams (`FallbackFileIndexLive`, `AnthropicBetaCache.layer`, `layerFromIO`), `openCellProcess` binary/worker split.

| #   | Finding                                                                                                                                                                                                           | Verdict                                  | Status                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| E19 | agents-view wire row declares `model`/`turns`/`costUsd`/`durationMs`; producer always writes `Option.none()`, TUI never reads them, `filterRows` matches an always-empty `model`                                  | Strong                                   | done `137deafb` (four metrics gone from the agents-view row)                                                            |
| E20 | wake, btw, background-bash resources do `Scope.make()`+finalizer+`forkIn` inside `Layer.effect`; `yield* Effect.scope` is the pattern used by cell-execution and core                                             | Worth exploring                          | done `da9042de` + `00bb86f1` (`yield* Effect.scope`; bash hunk rides in the E21 commit)                                 |
| E21 | background bash keys jobs on `ToolCallId.make("unknown")` when `ctx.toolCallId` is absent; delegate fails closed with `AgentRunError` for the same case                                                           | Worth exploring; fail closed in bash too | done `00bb86f1` (fails closed with `AgentRunError`; probe proves the test fails without the guard)                      |
| E22 | `listClaudeCodeKeychainServices` + `ClaudeAccount` exported for a picker that does not exist; zero consumers in packages/, apps/, tests                                                                           | Strong                                   | done `2e96f541` (deletion test: typecheck + suite green with both surfaces renamed away)                                |
| E23 | keychain-transform and codex-transform duplicate `withHeaders`, `Unauthorized401Error`, auth-error→`TransportError` mapping, and the 401 retry block; lift to `provider-http.ts` beside `provider-credentials.ts` | Worth exploring                          | done `5a3d4378` (new `provider-http.ts`; keychain 423 → 337, codex 443 → 347)                                           |
| E24 | `anthropic/oauth.ts` re-export barrel; `anthropic-headers.ts` identity `getModelBetas` and `LONG_CONTEXT_BETAS` alias over `model-config.ts`; same function tested via two paths                                  | Worth exploring                          | done `6b689f29` (identity `getModelBetas` + `LONG_CONTEXT_BETAS` alias gone; four cases moved to the model-config test) |

## core — pass 4 (2026-09-15, after `c6d81c3d`)

src 28,134 LOC. behavior, actor, session-runtime, rpc-handlers, session-mutations-live, sqlite rows, capability factories, api.ts, event/schema tables checked; nothing else above Speculative.

| #   | Finding                                                                                                                                                                                                                                                                                                                           | Verdict                                                            | Status                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C17 | `agent-loop.turn-execution.ts:132-748` (617 LOC: `projectContextWindow`, `resolveTurnSource`, `computeStreamEndedCost`, helpers) captures nothing from the factory; 33 of 34 imports are used only there; two test files build the whole loop to reach a pure projection; opencode splits the same surface into 7 files ≤ 793 LOC | Strong; `turn-window.ts` + `turn-source.ts`, bodies byte-identical | done `425f60ab` + `0acb6aa5` (`turn-source.ts` 513, `turn-window.ts` 190; factory 1,976 → 1,305; direct window tests, step-boundary handoff case moved) |

## sdk / server / tooling / e2e — pass 4 (2026-09-15, after `c6d81c3d`)

sdk 2,145, server 79, tooling 2,384, e2e 311 src LOC. apps/server, tooling guards, e2e's 15 tests checked clean (the "ESC closes skill popup" test could also assert "Skills" is gone). The fixture-string blind spot currently masks nothing.

| #   | Finding                                                                                                                                                                                        | Verdict                                                          | Status                                                                                        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| S21 | `GENT_LOG_FORMAT`/`GENT_LOG_FILE` gate `prettyLogger` (46 LOC of ANSI/tree output) and `GentLoggerPretty`; nothing sets them; only the logger test consumes the pretty layer                   | Strong; JSON file logger only (S18 shape)                        | done `5a12b222` (pretty logger + env switches gone; fallback `Layer.empty`)                   |
| S22 | `ProviderSpec.mock` `failing`/`retries`/`delayMs` never passed (12 sites use `mock()` or `{ empty: true }`); `GentServerOptions.env` read once, set nowhere                                    | Strong; `mock: { empty?: boolean }`, drop `env`                  | done `4da51a52` (spec narrowed; `failing` kept in core test-utils, 2 consumers)               |
| S23 | `LogPaths.trace` has no reader (OTLP tracer never writes it); doc links a `resolveLogPaths` that does not exist                                                                                | Worth exploring; drop, fix the link                              | done `d65b5b22`                                                                               |
| S24 | `apps/tui/src/utils/client-trace-logger.ts` re-derives the SDK's JSON entry (`extractMessage`, `collectSpans`, cause line) and a hand-rolled 250 ms batch; `local-health.ts` parses both files | Worth exploring; export `makeJsonFileLogger(path)`, TUI calls it | done `41ae8b38` (TUI writes through `makeJsonFileLogger`; same key set receipt; 122 → 18 LOC) |
| S25 | `platform-duplication-guards.ts` import on the last line                                                                                                                                       | Speculative; fold into the next touch                            | done `5a12b222` (import hoisted)                                                              |

## tui — pass 4 (2026-09-15, after `c6d81c3d`)

src 22,175 LOC. Dead-export sweep: zero. Big files re-read (client/context, use-session-feed, message-list, main, auth, composer controller, session controller, resolve, keyboard, chrome-panel, select-list) clean. A `apps/tui/src` scan row would cost 122 props-interface exemptions and catch none of these; not proposed.

| #   | Finding                                                                                                                                                                                                                                                | Verdict                                                                  | Status                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T24 | 15 of 51 `ThemeColors` members (`secondary`, `accent`, `backgroundPanel`, `borderActive`, five `diff*`, six `markdown*`) declared, resolved, set in all seven JSONs, never read; opencode reads all 15, the port kept the schema without the renderers | Strong; delete from types, resolve, and the seven JSONs                  | done `5a607390` (15 colors gone; deletion test named only the writer in `resolve.ts`)                                                                      |
| T25 | `TOOL_SPINNERS` + `getSpinnerFrames` have no production reader; all five spinner sites use `workingIconFrame`                                                                                                                                          | Strong; delete with the test block                                       | done `661a26a7`                                                                                                                                            |
| T26 | `client/index.ts` re-exports 16 SDK names; four are imported through the barrel, twelve are not; `use-session-feed.ts` already imports from `@gent/sdk`                                                                                                | Strong; drop the twelve (or all, pointing the four at the SDK)           | done `36c8aaf6` (barrel 23 → 2 lines; four SDK entry names dropped by the guardrail: `AuthProviderInfo`, `AuthMethod`, `AuthAuthorization`, `MessagePart`) |
| T27 | `ChildSessionTrackerService.getChildren` has no caller (the hook re-derives it over its store); `getAll`/`stop` used only in test teardown                                                                                                             | Worth exploring; drop `getChildren`; `stop` on hook cleanup or drop both | done `65a637d9` (`getChildren`/`stop`/`getAll` gone; scope-close probe fails with `forkDetach`)                                                            |

## core — pass 5 (2026-09-15, after `3e42f374`)

src 27,571 LOC after the C17 split. `turn-source.ts`, `turn-window.ts`, `turn-persistence.ts`, auth-guard, session-snapshot, agent-runner, child-completion, session-runtime re-read; nothing else above Speculative.

| #   | Finding                                                                                                                                                                             | Verdict         | Status                                                                                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------ |
| C18 | `findPersistedEvent` / `findPersistedToolResults` in `turn-persistence.ts` scan the whole branch log for a point lookup; `EventStorage.getLatestEvent` already answers it           | Strong          | rejected — `getLatestEvent` cannot answer these lookups; see the note below                                              |
| C19 | `AuthGuard.requiredProviders`, `missingRequiredProviders` and `AuthGuard.Test` have no production caller; only `listProviders` is read                                              | Strong          | done `9fca887d` (only `listProviders` kept; vehicle tests migrated, not dropped)                                         |
| C20 | `getSessionSnapshot` reads the branch log twice — its own `listEvents` plus actor `GetMetrics`; the handler can fold metrics over the events it already holds and drop `GetMetrics` | Worth exploring | done `8992cdd1` (`GetMetrics` gone from handler, actor, protocol and `SessionRuntime`; folding `[]` failed 3 of 5 cases) |
| C21 | `persistDurableMessage` (`turn-source.ts:317`) re-implements `persistMessageReceived`; delegate instead                                                                             | Worth exploring | done `eea4ae84` (delegates; no discriminating test exists — see the note below)                                          |
| C22 | `AgentRunSucceeded` is assembled at two sites (`agent-runner.ts:508`, `child-completion.ts:145`); one shared `childRunSucceeded` recipe                                             | Worth exploring | done `cea5a36e` (`childRunSucceeded` in `event.ts:365`; removing the clip failed both preview cases)                     |
| C23 | `SessionRuntime.Live` is a composition root only tests run; production composes `Client` + `AgentLoopLiveActor` itself                                                              | Worth exploring | rejected — accurate claim, wrong conclusion; see the note below                                                          |

C18 rejected: `EventStorage.getLatestEvent` cannot answer these lookups. Both `findPersistedEvent` callers (`turn-persistence.ts:167`, `:313`) match `MessageReceived` by `message.id`, but that event nests the id (`event.ts:87-89`) while `getLatestEvent` filters `json_extract(e.event_json, '$.messageId')` (`event-storage.ts:246`) — a tag-only query returns the latest `MessageReceived`, not the matching one, which is a correctness regression. Both callers also need an `EventEnvelope` for `eventPublisher.deliver`, whose dedup set keys on `envelope.id` (`event.ts:425-436`); `getLatestEvent` returns a bare `AgentEvent`. The `TurnCompleted` site (`agent-loop.turn-execution.ts:691`) has a top-level `messageId` but hits the same envelope problem. Doing this properly means adding an envelope-returning point lookup to `EventStorage` — a larger change than the row describes. Carried forward as a next action, not as this row.

C21 caveat: the discrimination probe restored the pre-C21 inline closure and the new test at `packages/core/tests/runtime/agent/turn-persistence.test.ts` still passed. Both implementations satisfy once-only, so C21 ships no behavior change and no discriminating test exists. The test is kept because that invariant had no direct coverage before. C21 also forced an honest contract change: `projectContextWindow`'s `persist` field had `R = never`, which only held because the old closure pre-resolved its services; the field is now generic (`PersistR`) and its error channel gains `EventStorageError`.

C23 rejected: the claim is accurate but the conclusion is wrong. `SessionRuntime.Live` is exactly `Layer.effect(SessionRuntime, makeLiveSessionRuntime).pipe(Layer.provideMerge(AgentLoopLiveActor(config)))` (`session-runtime.ts:588-596`), and production does compose `Client` + `AgentLoopLiveActor` separately (`dependencies.ts:327,336-351`) because the actor layer must build after `agentRuntimeLive`. But the four test callers (`tests/runtime/agent-loop/actor-command.test.ts:107`, `tests/runtime/agent-runner.test.ts:191`, `tests/runtime/session-runtime.test.ts:92`, `tests/extensions/memory/agent-override.test.ts:87`) each pass the identical `{ baseSections: [] }`. Deleting it would add a `Layer.provideMerge(AgentLoopLiveActor(...))` to all four and import an internal actor layer into each: duplicated wiring, not removed wiring. It is a named two-layer composition used four times, not an unused root.

## sdk / server / tooling / e2e — pass 5 (2026-09-15, after `3639ef82`)

sdk 1,993, apps/server 79, tooling 2,404, e2e 311 src LOC. All 12 sdk src modules, `apps/server/src/main.ts`, all 16 tooling guards and all of `packages/e2e` read in full. Export-consumer scan run over `packages/`, `apps/` and `tests/` in both directions; NUL-byte sweep over 800 tracked files found none; `check-guardrails` baseline exit 0. Prior art: opencode-v2 has no central e2e or guard package, pi has 38 loose scripts in `scripts/`; gent's single tooling package with one `check-guardrails` entry is deeper than both and stays.

| #   | Finding                                                                                                                                                                                                                                                                                                                               | Verdict         | Status                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | --------------------------------------------------------------------------------------------------------------------- |
| S26 | `packages/tooling/src/` and `packages/e2e/src/` have no row in `SCANNED_SURFACES` (`export-consumers.ts:66-125` lists only core, sdk, extensions), so 19 tooling names and `spawnWithDir` (`packages/e2e/src/pty-fixture.ts:45`, called only at `:97`, `:102`, `:119`) are exported with no file outside their own module naming them | Worth exploring | done `24b71eb8` (two `ownFileCounts: true` rows, verified to raise zero findings; `spawnWithDir` unexported)          |
| S27 | `waitForExit` written twice with divergent contracts: `pty-fixture.ts:20-40` returns `Effect<void>` and guards the pid; `server-process-fixture.ts:112-128` returns `Effect<number>` with `0`/`-1` sentinels and no pid guard; both poll `process.kill(pid, 0)` on a 50 ms sleep with the same `gent/no-sleep` waiver                 | Worth exploring | done `24b71eb8` (new `wait-for-process-exit.ts` returns `Effect<boolean>`; the `0`/`-1` sentinels deleted, not moved) |

Checked, not reported: the tooling-fixture blind spot masks nothing today (`EditError`, `EditParams`, `WakeAlarmsService` all held by own-file references); `AssistantMessageLegacy` now has two live hits in `message-list.tsx` and is TUI-owned; the e2e "ESC closes skill popup" strengthening is unsound as written because `ctx.output` is a cumulative PTY buffer. Not re-reported: S8, S10 (deferred, unchanged), S15 (skipped), S9, S11-S14, S16-S25.

## tui — pass 5 (2026-09-15, after `3639ef82`)

src 21,971 LOC. Platform, routes, components, extensions, client, hooks, utils, theme read; 521-export dead sweep over `apps/`, `packages/`, `tests/`. Prior art: gent 21,971 LOC vs pi 36,724 and opencode-v2 99,396; largest file gent 996 (`client/context.tsx`) vs pi 2,461 and opencode-v2 3,658. Nothing to add — opencode-v2's router, second overlay stack and plugin host are already covered by gent's extension seam with less code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                 | Verdict         | Status                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T28 | `platform/path-runtime.ts:3` and `platform/fs-runtime-boundary.ts:4` each build a module-scope `ManagedRuntime` outside any `Scope` to run `Path`/`FileSystem` synchronously, but both services are already in context at the call sites (`file-refs.ts:78-79`, `shell.ts:79` yields `FileSystem` at `:75`); gent is the only one of the three prior arts with an unscoped module-load `ManagedRuntime`, and it has two | Strong          | done `a5a96c01` (`path-runtime.ts` + `fs-runtime-boundary.ts` deleted; `Path.Path` yielded in context; prompt history on the client runtime) — pane check PASSED |
| T29 | `commands/slash-commands.ts:18-52` returns `Effect.Effect<SlashCommandResult>` with `never` error and `never` requirements; every branch is `Effect.succeed`; the one production caller (`session-controller.ts:543`) already owns a real Effect boundary                                                                                                                                                               | Worth exploring | done `f49f9e62` (returns `SlashCommandResult`; seven tests now synchronous)                                                                                      |
| T30 | `extensions/client-activity.ts` wraps the activity reader in `Option`, but `extensions/context.tsx:93,165` supplies an unconditional default, so it is always `Some` in the TUI and always `None` in headless (`main.tsx:164-174`); sole reader `builtins/herdr.client.ts:13-15` re-tests a decision the composition root already made                                                                                  | Worth exploring | done `f49f9e62` (plain reader with the `"unknown"` default, `Option` deleted; choice recorded in `apps/tui/AGENTS.md`)                                           |
| T31 | `routes/session-ui-state.ts:10-14` declares a field-identical copy of `components/prompt-search-state.ts:14-24` and copies field-by-field in both directions (`:79-88` rebuild, `:163` call, `:168-175` unpack); the consumer takes the component shape (`prompt-search-palette.tsx:21,25`)                                                                                                                             | Worth exploring | done `f49f9e62` (overlay carries `PromptSearchState`; `getPromptSearchState` deleted)                                                                            |

Checked, not reported: theme `Omit` + optional re-add is load-bearing (only `fx.json` sets `selectedListItemText`/`backgroundMenu`); surviving `diff*`/`markdown*`/`backgroundMenu` colors all have readers; the 521-export sweep's eight hits are own-file props interfaces (the pass-4 exemption class); `utils/wait-for.ts` fails the deletion test (inlining moves the retry loop into a Solid effect body); `client/event-hub.ts:79` trailing import is S25-shaped cosmetics; `utils/format-error.ts` and `routes/auth-state.ts` have no adapter to collapse.

T28-T31 regression probes, each break/run/restore using a temp-file snapshot restored with `command cp -f`: T28 new `apps/tui/tests/prompt-history-store.test.ts` — resolving paths from host `homedir()` gave 0 pass / 2 fail, restored 2 pass. T31 — rebuilding the overlay field-by-field and dropping `highlighted` gave 8 pass / 1 fail, restored 9 pass. T30 — default reader reporting `"idle"` gave 1 pass / 1 fail, restored 2 pass. S27 — inverting the exited branch gave 3 pass / 2 fail, restored 5 pass. T29 is a type-level collapse proven by the clean typecheck.

T30 encoding decision: plain reader with the `"unknown"` default, `Option` deleted. `extensions/context.tsx:93` already supplied an unconditional default, and `client-runtime.ts:37-44` already establishes the house pattern that absent surface callbacks collapse to no-op defaults (`noopShell`, `noopLifecycle`). Dropping the default instead would push an `Option` into a widget whose only answer would be `"unknown"`. Recorded in `apps/tui/AGENTS.md`.

`packages/tooling/tests/platform-duplication-guards.test.ts:289,294` names the deleted `path-runtime.ts` only as a sample filename inside a `withX(callback)` assertion — the file is the vehicle, the wrapper rule is the subject, so no migration was owed. `plans/WAVE-28.md:600` also names it and was left alone as a dated historical receipt.

T28 pane check (2026-09-15, binary rebuilt 11:55 from merged main, pane `wZ:p18`, cwd `/private/tmp/gent-gamut`): all three behaviors pass. `@file#line` — typing `@src/domain/money.ts#5` resolved and listed real workspace files, so `file-refs.ts` reaches `Path.Path` from context. `!cmd` shell spill — `!echo t28-shell-probe` printed `t28-shell-probe`, so `shell.ts` reaches `Path.Path` from context. Prompt history read — up-arrow recalled the on-disk entry. Prompt history write — `~/.cache/gent/prompt-history.json` advanced to 68 bytes at 11:58:51, past the 11:55 rebuild, so `writeEntries` fires despite its `Effect.ignoreCause`. `useWorkspace().home` is the user home, not the workspace directory, so `~/.cache/gent/` is the correct post-T28 location (`use-prompt-history.ts:24-29`).

Probe-sequence artifact, not a product fault: the recalled entry concatenated with newly typed text because gent binds no Ctrl+U. No `\x15`, `ctrl+u`, `clearLine` or `clear-line` binding exists anywhere in `apps/tui/src`; the composer clears on `escape` (`use-composer-controller.ts:362,390`). Nothing to fix.

T35 shape: theme and mode are now orthogonal. The palette carries two Appearance levels — Theme (`System`, then `fx`, `opencode`, `catppuccin`, `dracula`, `nord`, `gruvbox`, `tokyonight`, enumerated from `all()`, each calling `set(name)` only) and Mode (`Dark`/`Light`, calling `setMode(...)` only). `"system"` stays a catalog entry — the one generated from the terminal palette — rather than a third mode, so following the terminal is a theme choice. No context change was needed: `store.active` and `store.mode` were already independent and `values()` already resolves the active entry against the current mode. All seven themes resolve to the full 37 `ThemeColors` keys in BOTH modes, verified by a standalone script over the catalog and by a shipped test; the six ported themes carry 35 keys and the optional re-add at `resolve.ts:44-52` supplies `selectedListItemText` and `backgroundMenu`, so that path is now covered by a test rather than only serving dead files.

T38 shape and proof: `makeClientSessionResource` auto-fetches on session change, owns no loading/error pair and takes no query, so a sibling `makeClientSessionQuery` was added rather than bending it — same post-reply key guard, plus the loading/error pair the panes draw and a typed query parameter. Both controllers hold it, so the guard is owned once. The bug reproduced against the CURRENT controllers on the first attempt: agents wrote `[row("first")]` and thread wrote `sessions() === 1` after a session switch. Disabling the key guard after the refactor failed both again. One existing test was adjusted: `agents-pane.test.tsx`'s reopen case passed `current: () => Option.none()`, which now gates fetching; its subject is the pending-token reset and `current()` was only a vehicle, so it was given the loop the shell is on. The three `agents-controller.test.ts` sites also pass `None` but exercise only `select`/`fetchDetail` and stayed green.

Probes: T35 with the catalog hardcoded to `["fx"]` failed on `"Theme 8"`; T37 with a throw inside `classifyLogFile` failed both tests; T38 as above, twice. Every restore used `command cp -f` from a temp snapshot with `git status --porcelain` checked after.

One environment note from T37. The agent reported `FileSystem.utimes` as universally clamped — every file coming back with mtime `2262-04-11T23:47:16.854Z` regardless of input — and wrote its fixtures in order instead of controlling mtimes. **That claim does not reproduce and is not a general defect.** A direct probe through `BunFileSystem.layer` requesting `2020-01-02T03:04:05.000Z` returned exactly `some(2020-01-02T03:04:05.000Z)`. Whatever the agent hit was narrower than reported (a specific call shape or sub-millisecond value), so no upstream issue is warranted on this evidence. The in-order fixture approach is harmless and stays; a future test may use `utimes` normally. Separately, `inspectLogs` reads the shared `/tmp/gent/logs`, which a live gent also writes to, so its test asserts only properties that hold regardless of foreign files and removes its fixtures with `Effect.ensuring`; giving `inspectLogs` a directory parameter would end the sharing but is beyond the finding.

## extensions — pass 5 (2026-09-15, after `cb90d7ab`) — CLOSED

**extensions pass 5: closed, no findings above Speculative.** First package to close.

src 15,801 LOC, 98 files, 19 tools; every module read — the cell cluster (~25 files), both provider drivers plus `openai-compatible-driver.ts`, `acp-agents/`, `fs-tools/`, `exec-tools/`, `network-tools/`, `session-tools/`, `interaction-tools/`, `compaction/`, `delegate/`, `skills/`, `goal/`, `wake/`, `btw/`, `agents-view/`, `workflows.ts`, `handoff.ts`, `client.ts`, `agents.ts`, `index.ts`.

Pass-4 open question resolved — the 429/529 transient-retry block stays in `anthropic/keychain-transform.ts`: `openai/codex-transform.ts:327-346` has no transient-retry block at all, `provider-http.ts` has exactly two consumers (`keychain-transform.ts:71`, `codex-transform.ts:39`), `isTransientStatus`/`TransientResponseError`/the `Schedule.exponential` retry appear only at `keychain-transform.ts:97-107,315-335`, and 529 is Anthropic-only (`:56-59`). No second consumer exists; the duplication is not real.

Dead-export sweep: 442 names checked both directions over `packages/`, `apps/`, `tests/` with the Mach-O binary excluded. Five hits, all the pass-4 same-file-interface exemption class (`branch-state-store.ts:14`, `provider-credentials.ts:63`, `fs-tools/file-index.ts:25`, `fs-tools/edit.ts:87`, `btw/index.ts:48`).

Orphan probe: six cell modules have no test naming them but all six have real in-src consumers, so none is orphaned (`cell-namespace-storage.ts`, `cell-tool-call-recovery.ts`, `cell-input.ts`, `cell-operation-receipt.ts`, `current-cell-tool-operation.ts`, `dispatching-tool-storage.ts` — the last also guarded at `packages/tooling/tests/core-feature-independence.test.ts:24`, which E15 was rejected to protect).

`provider-http.ts` has no test of its own but both consumers drive its 401 invalidate-and-retry-once path (`tests/anthropic/anthropic-keychain-transform.test.ts:640-709`, `tests/openai/openai-codex-transform.test.ts:785-801`). The E23 extraction is covered.

Extension-authority rule holds: six `core-internal` imports, all `GentPlatform`/`BunGentPlatformLive`; no `FileLockService` or `ExtensionStatePublisher` in extensions src; no leaf takes a ctx parameter; the one `static Test` (`skills/skills.ts:187`) has real consumers.

Five duplication probes dismissed on the deletion test (collapsing each parameterizes a helper and moves complexity): approve-metadata literals, `.gent` path building, truncation constants, the `State.changed`/`queueFollowUp` pulse pairs, and the three hint-to-config mappers whose wire keys and reasoning strategies differ three ways.

Prior art: pi scatters retry per API surface; opencode-v2 centralizes classification in `packages/ai/src/provider-error.ts:124-137`. Gent's split — loop policy in `packages/core/src/runtime/retry.ts` plus one provider-specific HTTP block — is no worse than either. Nothing to add, nothing to remove.

## core — pass 6 (2026-09-15, after `5b768a29`)

src 28,111 LOC. Pass-5 landings verified in place. Read: `agent-loop.behavior.ts` (883), `.actor.ts` (939), `.turn-execution.ts` (1,305), `.protocol.ts`, `.worker.ts`, `.state.ts`, `tool-runner.ts`, `turn-persistence.ts`, `turn-source.ts`, `turn-window.ts`, `turn-resolve.ts`, `event-storage.ts`, `schema.ts` events DDL, `event.ts`, `model-context.ts`, `rpc-handlers.ts`, `session-runtime.ts`, `session-mutations-live.ts`, `agent-runner.ts`, `extensions/branch-tools.ts`, plus the tooling scan tables.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Verdict         | Status                                                                                                                                                                                                              |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C24 | The second core entry point `@gent/core/extensions/branch-tools` (`packages/core/package.json:8-9`) is scanned by no export-consumer surface — `surfaceOf` (`export-consumers.ts:138-143`) discards it via the `packages/core/src/extensions/` exempt prefix (`:79`) and the `DECLARATION` regex (`:152-153`) cannot see re-exports; five of its 57 re-exports have no consumer (`ModelContextError`, `ModelContextProjection`, `ModelContextProjectionError`, `projectModelContext`, `UsageSchema`) | Strong          | done `38e7baaa` (row alone was the fix — `declarationsIn` already routes a specifier surface to `reExportedNames`; guardrails named exactly the five re-exports then exit 0; removing the row fails both new tests) |
| C25 | `findPersistedToolResults` (`turn-persistence.ts:82-130`) calls `listEvents` with no `afterId` (`:90-93`) and index-scans twice (`:95-102`, `:105-111`) on every tool step of every turn (`agent-loop.turn-execution.ts:309`, reached after only the `getMessage` early return at `:301-306`); `listEvents` (`event-storage.ts:147-198`) selects every branch row and Schema-decodes each in two passes, so the work grows with transcript length to answer a point question                         | Worth exploring | done `625897c9` (`json_extract`, no migration; 750 rows/4.510ms → 3-4 rows/0.034-0.169ms on a 150-turn branch) — live check PASSED (gamut run 21)                                                                   |

C24 proposal: add a seventh `SCANNED_SURFACES` row for `branch-tools.ts` with `specifier: Option.some("@gent/core/extensions/branch-tools")`, `outsideOf: ["packages/core/src/", "packages/core-internal/"]`, `enforced: true` — the shape of the `api.ts` row (`:68-75`) and the S19/E16 rows — then delete the five dead re-export lines. The five names stay alive inside core by relative import, so only the re-export line is dead, not the module. This makes the "add a name here only when a branch-tool feature cannot be written without it" instruction (`branch-tools.ts:18-19`) enforced instead of advisory.

C25 proposal: add one `EventStorageService` method (`event-storage.ts:32-60`) returning `ReadonlyArray<EventEnvelope>` for the events between an assistant message and the next assistant boundary, keyed on the assistant message id. Keep the envelope return type — the publisher's dedup set keys on `envelope.id` (`event.ts:453`). This covers the per-step site ONLY; the three cold replay-only sites (`turn-persistence.ts:167`, `:313`, `agent-loop.turn-execution.ts:691`) stay as they are. C18 stays rejected: this is a new envelope-returning method, not `getLatestEvent`. Cost unmeasured and the schema work is real (a `message_id` column plus index, or a `json_extract` predicate — the `events` table has neither today, `schema.ts:153-165,185-187`).

Checked, not reported: the C21 pinned-`R` class is clean — `never` pins remain only at `gent-platform.ts:97`, `interaction-request.ts:153`, `capability/tool.ts:53` (the erased extension-leaf contract, by design) and `test-utils/extension-harness.ts:161`; the other callback fields already declare honest service lists or are R-generic. No new enforced dead exports after the pass-5 deletions (guardrails exit 0; `foldSessionMetrics`, `clipChars`, `AuthGuard.listProviders` all consumed). Prior art: opencode-v2 has no `listEvents`/`event_json` surface so it gives no guidance on C25; pi runs its loop in 803 + 592 LOC against gent's 1,305 + 939 + 883, and the extra size buys the durable queue, steering, cluster entity keep-alive and the seam adapters — no single collapse passed the deletion test.

C24 outcome: the `SCANNED_SURFACES` row alone was the fix — `DECLARATION` needed no change, because `declarationsIn` already routes any surface carrying a `specifier` to `reExportedNames`, which handles `type X`, `X as Y` and multi-line blocks. One ordering constraint: `surfaceOf` takes the first prefix match, so the new row must sit ahead of the `packages/core/src/` row. Deletion test: with the row in place guardrails named exactly the five re-exports (`:101-:104`, `:107`), five findings, exit 1; exit 0 after deleting them. Two of the five sat inside blocks keeping live names (`estimateTextTokens`, `ModelContextBudget`, `type Usage`), so those were entry-level edits, not statement deletions. All 18 files naming the specifier are under `packages/extensions/src/` (cell, compaction); no file reaches the five names through it. The `export-consumers.test.ts:77` assertion was updated, not deleted.

C25 schema choice: `json_extract`, no migration. Both bounds read `$.message.id` and `$.message.role` since `MessageReceived` nests its message, and no `assistantMessageId` path is needed because the window is an id range rather than a field match. `EXPLAIN QUERY PLAN` confirms both probes ride `idx_events_session_tag` and the range read rides `idx_events_session`. A `message_id` column plus index plus migration would buy nothing and would cost a write-path column every event must fill. Measured on a 150-turn branch (750 events): `listEvents` decoded 750 rows in 4.510 ms; `listToolResultWindow` decoded 3 rows in 0.034 ms (newest step) and 4 rows in 0.169 ms (oldest) — 27x on the worst case, 130x on the common one, and row count no longer tracks transcript length. Regression proof: disabling the next-assistant boundary clause leaks the following step's `MessageReceived` + `ToolCallSucceeded` (1 fail / 7 pass), restored green; the replay suite owning `findPersistedToolResults` passes unchanged (14 tests). Per-step site only; the three cold replay-only sites still use `listEvents` and C18 was not re-raised.

C25 verification limit: it changes what a replaying tool step sees mid-turn, and a green gate is not verification for that. Only a real session exercises resume-after-restart against a long transcript. Treat C25 as unverified in the live loop until a pane check or gamut run covers it.

Carried forward: `findPersistedEvent` still does `listEvents` + reverse-find on three cold paths; the id-range pattern now exists, so that is cheap to apply if a later pass wants it.

T32 seam choice: exported the pure helpers AND the `createPasteManager` factory, rather than lifting the paste manager beside its thresholds. Lifting that store to module scope to make it importable would have reintroduced the shared-counter bug that an earlier pass fixed and that the stale `paste-indicator.test.ts:15` comment still described. Exporting the factory keeps per-instance semantics, so each test builds its own manager and the shared-state reset scaffolding is gone; a new case pins that two managers keep separate id sequences. `apps/tui/` is outside `SCANNED_SURFACES` (`export-consumers.ts:68-140`), so the new exports cannot trip the unconsumed-export guard. In `prompt-history.test.ts` the real `canNavigateAtCursor` is imported and the 55-line clone deleted; the index/saved-entry bookkeeping stays a documented local model because the real store is a Solid singleton (`use-prompt-history.ts:94-109`).

T32 probe (reverse direction): baseline 36 pass while importing nothing from `src`; after rewiring 38 pass. Three separate production breaks, each restored from a scratchpad snapshot with `command cp -f`: `PASTE_THRESHOLD_LINES` 3→4 failed 2 tests; a module-scope shared id counter failed 3; narrowing the in-history cursor rule to `pos === 0` failed 3. Every one of those breaks was invisible to the old test files.

T33 approach: resolved the db directory once in the extension setup and passed the resolved path in, rather than having `file-finder.ts` yield `ClientWorkspace`. No module under `apps/tui/src/utils/` or `hooks/` yields a client service today, and yielding it inside the finder would add `ClientWorkspace` to `searchFiles`'s `R` type and infect the extension's `items` signature; `files.client.ts:32` already yields it. Pre-change probe confirmed the finding: two homes, two cwds gave `homeA/.gent/fff created: true`, `homeB/.gent/fff created: false` — the memo won and the second home was ignored. Post-change, reinstating the memo gives 0 pass / 1 fail.

T34 correction to the finding's prescription: the scoped logger does NOT create the directory. `makeJsonFileLogger` (`packages/sdk/src/logger.ts:109-125`) goes straight to `fs.open(path, { flag: "a+" })` and never calls `ensureLogDir`; only `clearLogFile` (`:132-138`) does. So the import-time `mkdirSync` was the ONLY thing creating that directory for the TUI, and `clientTraceLogger` silently depended on it — deleting it and relying on the existing scoped logger would have reproduced ENOENT at startup. `ensureLogDir` is therefore sequenced inside `clientTraceLogger`, ahead of `makeJsonFileLogger`, so the directory is created in the same scope that opens the file. Related: `clearClientLog` (`client-logger.ts:50-52`) is wrapped in `Effect.ignore`, and a `writeFileSync` into a missing directory throws and is then swallowed — it would have become a silent startup no-op. This required exporting `ensureLogDir` from `packages/sdk/src/index.ts` and recording it in the locked public surface (`packages/sdk/tests/public-surface.test.ts`); that lock was the sole gate failure on the first run and was caught correctly.

T32-shape audit over all 57 `apps/tui/tests/` files: only three import nothing from `../src`, and all three are legitimate — `sdk-utilities.test.ts` imports the real SDK, `extension-lifecycle.test.ts` exercises a local toy and asserts no production behavior claim, `headless-cli-exit.test.ts` drives the CLI as a subprocess. No further T32 instances exist.

One test case the agent wrote and then dropped: `searchFiles needs no FileSystem of its own`. The memo-reinstatement probe showed it still passed while the real case failed, so it could not catch the regression, and its hand-written `Effect<unknown, unknown>` annotation only restated what typecheck already enforces.

T34 pane check (2026-09-15, binary rebuilt 12:19:17 from merged main): PASSED. `/tmp/gent/logs` was deleted before launch; the cold start recreated it and wrote both `7dcd69a5-20260915161929-client.log` (4,883 bytes) and the matching server log at 12:19. This confirms the scoped `ensureLogDir` inside `clientTraceLogger` creates the directory, and that the predicted ENOENT from naively deleting the import-time `mkdirSync` does not occur.

T33 pane check (2026-09-15, binary rebuilt 12:19:17 from merged main): PASSED. Typing `@src/domain/mon` in the composer resolved and listed `src/domain/money.ts` and `src/domain/transaction.ts` from the workspace, so `@` autocomplete still works with the db directory resolved in `files.client.ts` setup. The FFF db sits at `~/.gent/fff` and NOT at `/private/tmp/gent-gamut/.gent/fff`, which confirms the finding was real: `home` is the user home resolved once at setup, so the db location follows the home rather than the first caller's cwd.

Gamut run 21 (2026-09-15, binary rebuilt 12:19:17 from merged main at `4b78c7a3`, headless `gent -H` in `/private/tmp/gent-gamut`): GREEN, `GAMUT EXIT 0`. The testbed went from 13 pass / 5 fail at reset to 19 pass / 0 fail, confirmed by an independent `bun test` after the run. All six README tasks were completed. This closes C25's live-verification gap: the bounded id-range tool-result read carried real multi-step tool turns, which is the path a green gate cannot exercise. The run used headless mode rather than the pane, because gent resumes the last session per cwd and a stale transcript had already contaminated one earlier probe by reporting the work as "already implemented"; headless guarantees a new session. Pre-existing and unrelated: `bun run typecheck` in the testbed still reports the `Cents`-branding overload errors in `tests/money.test.ts` and `tests/report.test.ts`, confirmed on the baseline by `git stash`.

## sdk / server / tooling / e2e — pass 6 (2026-09-15, after `433fdda9`) — CLOSED

**sdk/server/tooling/e2e pass 6: closed, no findings above Speculative.** Second package to close.

sdk 1,993, apps/server 79, tooling 2,404, e2e 311 src LOC. All 12 sdk src modules, `apps/server/src/main.ts`, all 16 tooling guards plus both scan tables, and all four e2e src modules with both test files — including `e2e.test.ts`, which pass 5 did not open. Pass-4/5 landings verified in place: `ProviderSpec.mock` narrowed to `{ empty? }` (`server.ts:59-66`), no `env` on `GentServerOptions`, no pretty logger or `GENT_LOG_FORMAT`/`GENT_LOG_FILE`, no `LogPaths.trace` (`log-paths.ts:50-54`), the three env reads S18 removed have not returned.

Pass-5 gap closed — the `waitForProcessExit` change is safe for `e2e.test.ts`, established by reading rather than running. The old PTY wait (`3d61dd79^:pty-fixture.ts:20-40`) and the new shared one (`wait-for-process-exit.ts:13-34`) are the same loop with the same pid guard, 50 ms poll and deadline test; only the result changed from `return` to `return true`/`false`. The sole PTY consumer is `cleanup` (`pty-fixture.ts:51-60`), which now uses the boolean to escalate to `SIGKILL` (`:55-58`) — identical on the exit path, strictly stronger on the timeout path. No `e2e.test.ts` suite reads the fixture's return value (the exit-code suite reads `ctx.pty.exited`, `e2e.test.ts:72`) and no `0`/`-1` sentinel remnants remain. `IPty.pid` is a plain number that the implementation can set to `-1`, which the pid guard reads as dead exactly as before. Honest limit: all nine suites reach `cleanup` through `acquireTestContext` (`:37`), so only a PTY run confirms real teardown timing — not needed to accept the commit, still needed before anyone calls the PTY suite green.

The two new `ownFileCounts: true` rows are load-bearing, not decorative. Planting one unreferenced export in each surface produced both findings by file and line and moved the guard from exit 0 to exit 1 (`packages/e2e/src/auth-seed-boundary.ts:29`, `packages/tooling/src/e2e-fixture-imports.ts:37`). Flipping both rows to `ownFileCounts: false` surfaces 19 names, all the standing same-file-interface exemption class, so `true` is the correct rule. They raise zero findings because the surface is clean, not because they cannot speak.

No guard has an unreachable path: all 16 modules are imported by `check-guardrails.ts:3-22`, which `package.json:11` runs as half of `bun run lint`. The real gap is coverage, not reachability — `core-vendor-model-pins.ts` and `core-alias-test-layers.ts` are the only guards with no test file, and break-probes prove both still bite (planted SKU reported at `packages/core/src/runtime/retry.ts:121`, exit 1; planted `static Test` alias reported at `packages/core/src/domain/event-publisher.ts:29`, exit 1). Recorded for a later pass: the alias guard matches only the literal member name `Test` (`core-alias-test-layers.ts:28`), so a `static Fake`/`static Stub` alias would go unseen — documented behavior, below the bar on its own. **Closed at `5d857c53`**: core pass 8 re-raised it with the multi-line half attached, and the guard now reads the member body instead of matching a line. Both guards named here also gained the test file they lacked.

C24's seventh `SCANNED_SURFACES` row is treated as spoken for by the core pass 6 row, not re-reported here.

Prior art: opencode-v2 puts 34,472 LOC of Playwright e2e under `packages/app/e2e` with its own mock server and no guard package; pi has 40 loose files in `scripts/` (7,869 LOC) including hand-rolled `check-*.mjs` with no shared runner. Gent's single `check-guardrails` entry over 16 pure `find*` functions, each a testable subject, is deeper than both. Nothing to add, nothing to remove.

## tui — pass 6 (2026-09-15, after `433fdda9`)

src 21,927 LOC, tests 14,798 LOC; largest file `client/context.tsx` (996). Read or swept: `platform/`, `utils/`, `hooks/`, `routes/`, `components/`, `commands/`, `extensions/` incl. all builtins, `client/`, `workspace/`, `theme/`, `ops/`, `services/`, plus all 74 test files.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Verdict         | Status                                                                                                                                                                                                                |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T32 | Two gate tests assert against copies of production logic: `tests/paste-indicator.test.ts:1` imports only `bun:test` and re-declares the thresholds, `countLines`, `isLargePaste` (`:4-13`) plus a whole paste store (`:16-35`) that exist at `use-composer-controller.ts:26-31,34-57`; `tests/prompt-history.test.ts:1-2` re-declares `canNavigateAtCursor` (`:7-17`) and a 55-line `navigate`/`add` clone (`:62-121`) of `use-prompt-history.ts:50-60,154-198`; 36 tests pass while importing nothing a production edit can reach, and `:15` still describes a "module-level counter" that is now a per-controller closure (`:34-35,94`) | Strong          | done `cd97818a` (exported `countLines`/`isLargePaste`/`canNavigateAtCursor` + the `createPasteManager` factory; clones deleted; 36 → 38 tests, and three production breaks that were invisible before now fail 2/3/3) |
| T33 | `utils/file-finder.ts:43-53` memoizes the FFF db directory in a module-scope `let dbDir` filled on first call and never re-read against its `home` argument (`:47` returns before `:49` recomputes), and `:66` keys the finder cache by `cwd` alone while `home` reaches `create` only through that memo (`:84-89`) — `home` is a parameter the module accepts then ignores, threaded from `extensions/builtins/files.client.ts:61`; nothing disposes either cache                                                                                                                                                                        | Worth exploring | done `d87e6619` (db dir resolved once in `files.client.ts` setup and passed in; module-scope memo and `ensureDbDir` deleted; `searchFiles` no longer requires `FileSystem`) — pane check PASSED                       |
| T34 | `utils/client-logger.ts:24` creates the log directory with a bare `mkdirSync` inside a module-scope `Effect.runSync` that fires at import, before any scope exists, while the SDK already ships the scoped `ensureLogDir` (`packages/sdk/src/log-paths.ts:72-77`, sole consumer `packages/sdk/src/logger.ts:135`); the TUI imports the module for its side effect (`main.tsx:44,82`) and separately builds a scoped file logger over the same directory (`client-trace-logger.ts:14-18`, wired at `main.tsx:683`)                                                                                                                         | Worth exploring | done `61f6eea4` (`ensureLogDir` sequenced inside `clientTraceLogger`, ahead of `makeJsonFileLogger`) — pane check PASSED                                                                                              |

T32 needs no break/run/restore probe: the two test files import nothing from `src`, which is a static proof of drift-blindness stronger than a single probe. T33 and T34 remain unprobed and each need a break/run/restore before landing.

Checked, not reported: no `ManagedRuntime` is built at module load anywhere under `apps/tui/src` after T28 — the only `ManagedRuntime.make` is inside the `makeClientRuntime` factory (`extensions/client-runtime.ts:47`), called per provider mount (`extensions/context.tsx:138`); remaining module-scope state is bounded and legitimate (the LRU render cache `utils/mermaid.ts:117-118,152-157`, lookup tables, one grapheme segmenter, a log sequence counter). The prompt-history module singleton (`use-prompt-history.ts:94-109`) fails the deletion test — the shared store is what makes history survive the `keyed` session remount (`app.tsx:54`), and `home` now arrives per call from `useWorkspace()`, which is what `a5a96c01` fixed. The untested composer controller is smaller than the ledger recorded: `tests/composer-render.test.tsx:63,87,107` mounts the real `Composer`, which calls `useComposerController` (`composer.tsx:32`), so submit, suspend and the slash popup do run through it — the genuinely untested logic is the paste and history arithmetic, which is T32's subject. `createSessionCommandRegistry` and `describeAmbiguous` are the own-file exemption class. `platform/text-width-adapter.ts` earns its place with four importers as the single named seam over `Bun.stringWidth`; both prior arts keep the same seam and both are larger (opencode-v2 imports its `string-width` across features plus a second copy in merman; pi hand-rolls ~200 lines of wcwidth tables and a 512-entry cache). `onSlashCommand` (`session-controller.ts:546-555`) still wraps a synchronous body but is the controller's declared interface consumed through `client.surfaceError`; collapsing it would push error surfacing into the component.

## core — pass 7 (2026-09-15, after `4b78c7a3`)

Read clean: `turn-response.ts` (406), `config-service.ts` (332), `tool-runner.ts` (431), `session-profile.ts` (305), `model-context.ts` (633), `session-operation-storage.ts` (409), `extension-hooks.ts`, `message-storage.ts`, `activation.ts`, the `events` DDL and indexes (`schema.ts:152-182`). Guardrails baseline exit 0. Pass-6 landings verified in place: `export-consumers.ts` carries the `branch-tools.ts` row with the five re-exports gone; `listToolResultWindow` exists with four storage tests and `findPersistedToolResults` calls it (`turn-persistence.ts:88-93`).

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verdict | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------ |
| C26 | `reconcileToolProjections` (`turn-persistence.ts:181-220`) builds its `closed` set from an unbounded whole-branch `listEvents` with no `afterId` (`:191-194`) to answer a per-step question, on the hot tool path — `recordToolOutcome` calls it unconditionally (`:349-355`) from five production sites (`turn-tool-execution.ts:188`, `:257`; `agent-loop.turn-execution.ts:329`, `:371`, `:978`), of which `:371` runs on every tool step of every turn; it already receives `assistantMessageId` (`:186`), exactly the key C25's `listToolResultWindow` (`storage/event-storage.ts:265-345`) takes | Strong  | landed |

C26 proposal: replace the `listEvents` call at `turn-persistence.ts:191-194` with `eventStorage.listToolResultWindow({ sessionId, branchId, assistantMessageId })` — the same call `findPersistedToolResults` already makes 20 lines above. No schema change, no migration, no new storage method; C25 built and indexed this exact read. Add the missing test: no file names `recordToolOutcome` or `reconcileToolProjections` today, so this hot path has no direct coverage.

C26 measurement on a 200-turn branch of 1,400 events, building the real `closed` set of terminal `toolCallId`s: today `listEvents` decodes 1,400 rows in 0.501 ms/call; the bounded window decodes 4 rows in 0.003 ms (newest step, open-ended arm) and 6 rows in 0.173 ms (oldest step, closed range). Row count stops tracking transcript length.

C26 correctness checked rather than assumed. The window is sufficient because `closed` is consulted only for `params.parts` (`:201-203`), the anchored step's own results. Every terminal-event writer stamps `assistantMessageId` from the current or dispatching tool call (`tool-runner.ts:109-115,:129,:148`; `turn-response.ts:294-321`; `turn-persistence.ts:214-215`), so a nested cell or delegate call (`read-session.ts:138`, `delegate-tool.ts:114`, `agent-runner.ts:338`) inherits the dispatcher's anchor and stays inside the window. `publish` is `append` + `deliver` (`domain/event.ts:565-568`) and `append` assigns a tail id, so this step's terminal events always sort after the anchor `MessageReceived`. The resumed case cannot close the window early: `resumeTurn` (`agent-loop.turn-execution.ts:895`) returns before reconciling unless `position.pendingAssistant` is present (`:908-913`), so the anchor is the newest assistant message and the open-ended arm applies — pinned by `tests/storage/sqlite-event-storage.test.ts:248-260`. No extension writes terminal tool events out of band: `exec-tools/bash.ts` has no `ToolCallSucceeded`/`ToolCallFailed`/`publish` (only `storage.markFailed`, `:389`), and the sole extension `publish` is an ACP PubSub (`acp-agents/protocol.ts:362`), not an event append. `core-retired-reconciler.ts` guards the removed _resource_ reconciler and does not touch `reconcileToolProjections`, so C26 raises no conflict.

Carried-forward candidate checked and DISCARDED — `findPersistedEvent` (`turn-persistence.ts:69-80`) keeps its `listEvents` + reverse-find on the three cold paths. Each site is a duplicate-suppression branch, not a per-step read: `persistMessageReceived` reaches it only when `getMessage` already found the message (`:149-151`), and its hot callers (the per-step-boundary steering write `agent-loop.turn-execution.ts:1023`, the turn-start write `:1228`) take the `append` branch (`:164-170`) and never scan; the `finalizeTurn` site runs only when `turnDurationMs` is already set (`:688-691`), a re-finalise after restart. The id-range pattern also does not fit cheaply: `findPersistedEvent` takes an arbitrary `match: (envelope) => boolean` predicate (`:72`), so a bounded read needs a new envelope-returning point lookup per match shape (`MessageReceived` by nested `$.message.id`, `TurnCompleted` by top-level `$.messageId`) — the larger change the C18 rejection already described. Cost is paid once per resume, not per step.

Checked, not reported: the other `listEvents` sites are bounded or legitimately whole-branch (`event-store-live.ts:24` pages by `afterId`, `agent-loop.behavior.ts:824` is once-per-loop-start recovery, `rpc-handlers.ts:102` is the snapshot read). All `listMessages` sites (`turn-resolve.ts:130`, `agent-runner.ts:439`, `:487`, `child-completion.ts:124`, `session-mutations-live.ts:408`, `rpc-handlers.ts:100`, `:434`) consume every message, so none is a point lookup. Prior art gave no guidance: opencode-v2 has no `listEvents`/`event_json` equivalent and pi keeps no durable event log for tool replay, so neither offers a reconcile pattern. Rules 6 and 8 stay intact under C26 — it alters only how the already-closed set is read, not what replays.

C26 landed. `reconcileToolProjections` now reads `listToolResultWindow` anchored on the `assistantMessageId` it already receives (`turn-persistence.ts:193-197`); the three-line change adds no schema, migration, or storage method. The correctness analysis was re-verified against the code before the change and held on every point: `closed` is still consulted only for `params.parts`; `CurrentToolCall` and `CurrentDispatchingCall` both carry `assistantMessageId` (`current-tool-call.ts:6-11`, `current-dispatching-call.ts:15-23`) and `tool-runner.ts:110-115` falls back from the current call to the dispatcher, so a nested call inherits the dispatcher's anchor; `EventPublisher.publish` is `append` + `deliver` (`event-publisher.ts:66-72`, `event.ts:565-568`) so terminal events take tail ids after the anchor; `resolveTurnPosition` returns `noPendingStep` unless a pending assistant message exists (`agent-loop.turn-execution.ts:825-893`), so `resumeTurn` reconciles only on the newest assistant message and the open-ended arm applies. Two further writers were checked and fit: `turn-source.ts:195-208` provides `CurrentToolCall` from `persistence.assistantMessageId` and persists results through `persistMessageParts`, and `cell-tool-host.ts` requires `EventPublisher` but publishes no terminal tool event of its own. Baseline `check-guardrails` exit 0 before and after.

C26 coverage added: `packages/core/tests/runtime/agent-loop/tool-outcome-recording.test.ts`, the first direct test of `recordToolOutcome`. Three cases over the real SQLite `events` table with `EventStoreLive` + `EventPublisherLive`: an unclosed result gets its terminal event, an already-closed result is left alone, and — the window boundary — a terminal event belonging to the NEXT step does not count as closing the anchored step's call. Probe A (revert to whole-branch `listEvents`) fails the boundary case only, 2 pass / 1 fail: the whole-branch read sees the later step's terminal event and wrongly suppresses the write. Probe B (`Effect.die` inside `reconcileToolProjections`) fails all three, proving every case reaches the changed code rather than a dedup replay.

C26 measurement, re-run here on the same 200-turn / 1,400-event shape: `listEvents` decodes 1,400 rows at 12.96 ms/call; the window decodes 3 rows at 0.055 ms (newest step, open-ended arm) and 3 rows at 0.460 ms (oldest step, closed range). The shape and the direction reproduce the review — row count stops tracking transcript length, ~470x fewer rows and ~235x less time on the newest step — but the absolute figures do not match the review's 0.501 ms / 0.003 ms / 0.173 ms, and the window's row count is 3 here rather than 4 and 6. The row gap is the synthetic branch shape (this run puts exactly `ToolCallStarted` + `ToolCallSucceeded` inside the step window, plus one trailing event for the newest anchor); the ms gap is measurement method, not behavior. Reported as measured rather than reconciled to the review's numbers.

C26 live check (gamut run 22, 2026-09-15, binary rebuilt 12:40:22 from merged main at `afa148f0`, headless `gent -H`): PASSED, `GAMUT EXIT 0`. The testbed went from red at reset to 20 pass / 0 fail, confirmed by an independent `bun test`, with all six README tasks completed including delegated multi-step tool work. This closes the live-verification gap the row carried: the bounded per-step reconcile read carried real multi-step tool turns, which a green gate cannot exercise. Pre-existing and unrelated, as in run 21: the testbed's own `bun run typecheck` still reports the branded `Cents` errors in `tests/money.test.ts` and `tests/report.test.ts`, confirmed on the untouched baseline by stashing.

C26 is not verified live. It changes the hot tool path mid-turn, and a green gate does not exercise a real multi-step turn against a real provider. A gamut run or live check is still owed.

## tui — pass 7 (2026-09-15, after `4b78c7a3`)

src 21,917 LOC, 135 files. Every file above 200 LOC re-read at current state: `client/context.tsx` (996), `use-session-feed.ts` (933), `message-list.tsx` (754), `session-controller.ts` (752), `main.tsx` (730), `agents-view.client.tsx` (668), `use-composer-controller.ts` (569), `thread-view.client.tsx` (537), `command-palette.tsx` (497), `native-transcript.tsx` (426), `select-list.tsx` (423), `app-bootstrap.ts` (369), `session.tsx` (354), `resolve.ts` (349), `local-health.ts` (341), `client-transport.ts` (318), `extensions/context.tsx` (314), `chrome-panel.tsx` (309), `client-facets.ts` (308), `child-session-tracker.ts` (275), `theme/resolve.ts` (252), `loader-boundary.ts` (223); plus all of `keyboard/`, `command/`, `commands/`, `env/`, `workspace/`, `hooks/`, `theme/`. Pass-6 landings verified in place. Dead-export sweep over 520 names in both directions across `apps/` and `packages/` with Mach-O binaries excluded: 51 hits, all the standing same-file-interface exemption class.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verdict         | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T35 | Six of seven bundled themes are registered but unreachable: `theme/default-themes.ts:11-19` registers all seven, but the only selector is the palette's theme level (`command-palette.tsx:96-130`), whose three items call `set("system")` (`:108`), `set("fx")` (`:116`) and `set("fx")` (`:125`) — the sole `set(...)` sites in the tree; the six dead entries carry 861 of 906 JSON lines and each is missing two of the 37 `ThemeColors` keys, surviving on the optional re-add at `theme/resolve.ts:44-52`                                                                                                                                                        | Strong          | done `c33cdbb7` (palette gained a second Appearance level; Theme enumerates `all()`, Mode is a separate Dark/Light; no theme deleted) — pane check PASSED at `wZ:p18`: the palette lists `Theme  Switch color theme` and `Mode  Dark or light variant` as separate Appearance rows; the Theme level header reads `Theme 8 [All]` and enumerates System + all seven catalog themes; selecting `catppuccin` moved the `•` selection marker off `fx`, and `selected()` reads the same `store.active` that the `values` memo resolves through, so the active theme changed. Theme choice is in-memory by design — `theme/context.tsx:118-120` `set()` only writes `store.active` and there is no persistence path, so no config row is written |
| T36 | `ThemeContextValue` carries two members no consumer reads: `ready` (`theme/context.tsx:18`, written at `:65,85,95,106,113`, exposed at `:149-150`) is never gated on because the palette-detection race is already handled by `main.tsx:411` resolving the mode before `render()`; `all` (`:14`, wired at `:141`) has no hit outside its declaration — 46 of 47 `useTheme()` sites destructure only `{ theme }`                                                                                                                                                                                                                                                        | Strong          | done `c33cdbb7` (`ready` dropped; zero readers confirmed — the only other `.ready` hit is an unrelated log label at `use-session-feed.ts:73`; `produce` import dropped with it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T37 | The log directory is declared twice: `packages/sdk/src/log-paths.ts:13` exports `LOG_DIR` (re-exported `index.ts:43`, locked in `public-surface.test.ts:11`), and `apps/tui/src/ops/local-health.ts:6` declares a private `LOG_DIR` with the same value used at `:143,144,146,149,161`; the same file re-states the SDK's file-naming contract at `:162-163` against the prefix format `buildLogPaths` owns (`log-paths.ts:66-67`) — the last un-adapted copy after T34 made the SDK the sole owner                                                                                                                                                                    | Worth exploring | done `facc7a25` (`log-paths.ts:13` is the only `LOG_DIR` left in `apps/` or `packages/`; one `LOG_SUFFIX` feeds both `buildLogPaths` and a new exported `classifyLogFile`; first `inspectLogs` test added)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T38 | Two docked panes hand-roll the keyed fetch `makeClientSessionResource` provides (`extensions/client-services.ts:114-203`) and both lose its stale-reply guard: the factory re-reads `transport.currentSession()` after the reply lands and drops it when the key moved (`:167-176`), but `agents-view.client.tsx:86-107` and `thread-view.client.tsx:257-318` each re-declare the signals and a byte-similar match block with NO post-reply key check, while both refetch across session switches (`agents-view.client.tsx:266-271` plus a 2 s poll at `:592-599`; thread-view at `:494-499`), so a reply in flight across a switch writes the previous session's rows | Worth exploring | done `bec60ebd` (new sibling `makeClientSessionQuery` carries the key guard plus the loading/error pair and a typed query; both panes hold it) — **pane check PASSED** at `wZ:p18` on the merged framing (binary 13:29 from `771dc606`): the user asked for the agents pane to take the slash-command `PickerFrame` look, which rewrites the rendering of the same file, so one pane check after that merges covers T38's session-switch fetch and the new framing together                                                                                                                                                                                                                                                                |

T35 is a choice of which half to finish, not a cleanup: delete the six JSONs leaving `DEFAULT_THEMES = { fx }` (no test migrates — `theme-view.test.ts:8-9` already uses only `DEFAULT_THEMES.fx`), or make the palette level enumerate `all()`. opencode-v2 ships a per-theme picker that enumerates its catalog, which is why the six JSONs were ported; gent kept the files without the picker. Raised with the user rather than decided silently, because removing six themes drops a user-facing capability and finishing the picker adds one. **User decision (2026-09-15): finish the theme picker.** The palette's theme level enumerates `all()` so all seven themes become selectable; mode stays a separate Dark/Light toggle. This keeps `all` live, so T36 reduces to dropping `ready` only. Each of the six ported themes is missing `selectedListItemText` and `backgroundMenu`, which the optional re-add at `theme/resolve.ts:44-52` supplies — that path is now load-bearing for real user-visible themes rather than for dead files.

Two stale doc claims to fix with T35/T36: `apps/tui/AGENTS.md:38` says the fallback is the "opencode" theme, but the code falls back to `fx` (`theme/context.tsx:84,105,129`); `apps/tui/AGENTS.md:31` documents `useTheme()` as returning `{ theme, mode, setMode, all, set }`.

Checked, not reported: `ClientTransport` has no dead members (`listSessions`, `listMessages`, `deleteSession`, `agentDetail`, `driverList/Set/Clear` all have real consumers in `thread-view`, `agents-view`, `driver.client.ts`); `ExtensionAgentDetail`'s six fields all have readers (`status` at `agents-view.client.tsx:316`, `omittedMessages` at `thread-view.client.tsx:482`, the rest at `:376-377`), so E19 has no transport-side twin; `ThemeMode`'s `"system"` literal is unused as a mode (`app.tsx:16` and `main.tsx:411` both narrow to `"dark" | "light"`) but is one literal, folded into the T35/T36 touch; the three `ChromePanel.Footer` hint strings are per-pane product copy, not duplication; `headless-runner.ts` and `headless-tool-renderers.ts` are clean. T22 not re-proposed (deferred, pane check still owed); T8 not re-proposed. No `local-health` test covers `inspectLogs` today, so T37 wants one.

Prior art: gent tui 21,917 LOC against pi 36,724 and opencode-v2 99,396. Nothing to add from either — gent's one `SelectList` plus `ChromePanel.Dock` is already the narrower interface, and `makeClientSessionResource` is a seam neither prior has, so T38 is about using it rather than adding one.

## core — pass 8 (2026-09-15, after `e26c0583`) — **CLOSED**

src 28,181 LOC. Scoped by recent history per the skill's YAGNI rule, weighting the paths pass 7 touched: `runtime/agent/turn-persistence.ts`, `storage/event-storage.ts`, `extensions/branch-tools.ts`. Baseline `bun packages/tooling/src/check-guardrails.ts` exit 0 before inspection. Read-only pass: no edits, commits, or rifts.

**No Strong and no Worth-exploring finding. core CLOSES.** Three packages have now closed this way: extensions (pass 5, `5b768a29`), sdk/server/tooling/e2e (pass 6, `a9e32e01`), core (pass 8).

Every candidate adjudicated, with receipts, so a later pass does not re-derive them:

| Candidate                                                                                                                                                                                                                                                  | Verdict                                                                    | Deletion test                                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `findPersistedEvent` (`runtime/agent/turn-persistence.ts:69-80`) still does `listEvents` + reverse-find on three cold paths (`:151`, `:300`, `runtime/agent/agent-loop.turn-execution.ts:691`)                                                             | Speculative — already discarded in pass 7 on the same receipts             | Moves. Deleting it re-inlines the scan at three sites or forces two new storage methods.                                        |
| `core-alias-test-layers` guard (`packages/tooling/src/core-alias-test-layers.ts:27`) matches only `/static\s+Test\s*=[^=]*=>\s*(\w+)\.Live\s*$/` — a literal `Test` and a single-line `=> X.Live`; a `static Fake`/`Stub` or a multi-line alias slips past | Speculative — a guard hole with zero live instances, **closed `5d857c53`** | N/A, widening a guard adds no module                                                                                            |
| `ExtensionHostContext` (`domain/extension-services.ts:254-270`) vs `ExtensionContextService` (`:272-288`) look like duplicate facet records                                                                                                                | Speculative — the asymmetry is load-bearing                                | Moves. Deleting the narrowing adapter pushes the "child start requires a host-owned tool call" guard into every extension leaf. |
| `ResourceScope` seam exclusion list (`packages/tooling/src/core-unadapted-seams.ts` `EXTENSION_LOAD_SCOPES`)                                                                                                                                               | Speculative — inert today, no dead seam                                    | N/A                                                                                                                             |
| `packages/sdk/src/server.ts:22` is the only shipped file importing core `test-utils`                                                                                                                                                                       | Not reportable — this is row S10, deferred; revisit condition unmet        | —                                                                                                                               |

Re-verified rather than taken on trust. Each `findPersistedEvent` caller is a duplicate-suppression branch, not a per-step read: `persistMessageReceived` reaches it only after `getMessage` already found the message (`:148-151`), and `finalizeTurn` only when `turnDurationMs` is already set (`:690-691`), a re-finalise after restart. Its `match: (envelope) => boolean` predicate (`:72`) is arbitrary, so a bounded read needs a new envelope-returning point lookup per match shape — the larger change the C18 rejection recorded. The guard hole has no instance, and the pass-8 enumeration was wrong twice — corrected here after the guard was widened at `5d857c53`. The seven runtime paths sit under `packages/core/src/runtime/`, not `packages/core/src/`: `runtime/config-service.ts:296`, `runtime/session-profile.ts:266`, `runtime/agent/tool-runner.ts:422`, `runtime/extensions/registry.ts:530`, `runtime/approval-service.ts:106`, `runtime/gent-platform.ts:112`, `runtime/model-registry.ts:252`, plus `domain/event-publisher.ts:29`, `domain/auth.ts:203` and `domain/event.ts:623` `EventStore.Memory`. And the count was not ten: a tree-wide scan finds fourteen alternative-layer statics, four outside `packages/core` — `packages/extensions/src/skills/skills.ts:187`, `packages/sdk/src/build-fingerprint.ts:93`, `apps/tui/src/services/os-service.ts:29`, `apps/tui/src/services/link-opener.ts:80`. Thirteen carry the member name `Test`; `EventStore.Memory` is the fourteenth and sits deliberately outside the guard's name table, because `Memory` names a strategy rather than claiming to be a stand-in. Every one is a real alternative implementation, so the hole still has no live instance. S10's revisit condition ("if a second debug provider appears") is unmet: `resolveLanguageModelLayer` (`server.ts:180-191`) has exactly two arms and `.debug()`/`.empty` are the only shipped consumers. The host/leaf facet records are not duplicates — the host record carries `host` and no `toolCallId`/`turn`, the leaf carries `toolCallId` + `turn` and no `host`, and `AnthropicPlatform.fromSetup` reads `ctx.host.osInfo.platform` off the host record at setup (`packages/extensions/src/anthropic/platform-adapter.ts:52-58`), never off the leaf.

Checked and clean: `domain/contribution.ts` and `domain/extension-host.ts` carry no C9/C13-shaped bucket accessors (the bucket _is_ the discrimination, `contribution.ts:42-61`); `domain/session-mutations.ts` is a 7-method interface over a 585-line implementation (`server/session-mutations-live.ts:585`), a deep module rather than a pass-through; `storage/session-operation-storage.ts` `DurableOperations` (`:79-84`) is a four-entry receipt table, each entry with a distinct schema.

Prior art. opencode-v2 (`052be04466`, detached on `origin/v2`) has a 152,627-LOC core against gent's 28,181, with `session/` alone at 10,131 across 14+ files; only 4 files sit in `packages/core/src/effect/` while 314 core files import `effect`, and `bus.ts` (908) is a pub/sub rather than a mailbox — Effect-_using_, not effect-native, and no actor model. Nothing to add: its `session/inbox.ts` is the nearest analogue to gent's queue, but gent's queue is already an actor mailbox with a durable `QueuedTurnItem.keyed` tag (C5). Nothing to remove: its `projector.ts` is the separate-projector design Architecture-hardening Batch 4 already dropped, so adopting it would reverse a closed decision. pi (`71dca87`) keeps a leaner `agent-loop.ts` (803) but buys the size back in `interactive-mode.ts` (6,620) and `agent-session.ts` (3,552); gent's extra loop LOC buys the durable queue, steering, cluster entity keep-alive, and the seam adapters, and pass 6 already found no single collapse that passes the deletion test. pi keeps no durable event log for tool replay, so neither prior offers guidance on gent's reconcile/replay paths — the same conclusion passes 6 and 7 reached.

The one actionable item pass 8 surfaced is closed at `5d857c53`. The guard no longer pattern-matches a line: it walks the member's initializer with the brace-depth scan `core-unadapted-seams.ts` already uses in `declaredMembers`, normalizes the body, and asks whether the whole body is nothing but an optional arrow head followed by `<Service>.Live`. The member name became a table (`ALTERNATIVE_NAMES = ["Test","Fake","Stub","Mock"]`), so adding a name is a one-line edit rather than a pattern change, and multi-line handling is the absence of a line assumption rather than a wider regex. The discriminator is "is the entire body an alias to `Live`", not "is the body short" — which is what keeps the near-miss clean: `ExtensionRegistry.Test` (`runtime/extensions/registry.ts:530`) is a single delegating call to `ExtensionRegistry.fromResolved(resolveExtensions([]))`, a real alternative because the sibling it reaches is not `Live`.

Probes proved the two narrownesses independent, which one combined case would have hidden: against the original guard verbatim, 5 pass / 3 fail; with the names widened but single-line matching kept, 7 pass / 1 fail, the multi-line case still failing on its own. A third shape turned up that neither narrowness described — a bare property alias `static Fake: Layer.Layer<X> = X.Live` with no arrow head, which the old `=>`-requiring regex could not see. Real-tree scan with the widened guard: 0 findings. Guardrails exit 0. `packages/tooling/tests/core-alias-test-layers.test.ts` is the guard's first test file.

Scope note, left as found at the time and **decided at `e060df38`: widen.** The guard's `SRC_PREFIX = "packages/"` filter had put `apps/tui/src/services/os-service.ts:29` and `apps/tui/src/services/link-opener.ts:80` out of range. Both are real `Layer.succeed` implementations, so it was never a live hole, but it ran against the standing lesson that reduction greps must include `apps/`.

The decision turned on what the sibling guards do, which splits cleanly in two. Narrow by design: `core-feature-independence.ts:44` and `core-vendor-model-pins.ts:25` both pin `packages/core/src/`, and both forbid _core reaching outward_ — feature tables, vendor SKUs — so the scope is part of the rule. Whole shipped tree by design: `core-retired-reconciler.ts:45` already uses `/^(?:packages\/(?:core|extensions|sdk)\/src|apps\/[^/]+\/src)\//` and `core-unadapted-seams.ts:50` pairs `packages/extensions/src/` with `apps/`. Nothing about an alias is core-specific — a service in the TUI earns a `Test` layer on the same terms as one in core — so this guard belongs in the second family, and the `packages/` prefix was an accident of where the first instances lived rather than a scope decision. One `SHIPPED_SOURCE` regex now replaces both the prefix and the separate `/src/` test.

Measured before committing to it: 416 files in range, **136 newly in range under `apps/`**, and **0 findings** — no false positive anywhere in the tree. `check-guardrails` exits 0 before and after, confirmed independently here at the rebased commit. Blast radius is exactly two entries, `apps/server` (one in-range file, `main.ts`) and `apps/tui`; no test, fixture, mock or generated file sits under any `apps/*/src/`, and the `src/` segment keeps `apps/tui/tests/`, `scripts/`, `examples/` and `docs/` out. Probe: reverting the scope to `packages/`-only gives 8 pass / 1 fail, the failure being `flags an alias in an app source tree, not only a package one`, expecting `["apps/tui/src/services/widget.ts:2"]` and receiving `[]`. The existing negative case was tightened to cover `apps/tui/tests/` and `scripts/` so the new breadth has a stated floor.

The file filter stayed inside the guard rather than moving to a roots table in `check-guardrails.ts`. Deletion test: a table there would scatter this guard's scope into a file that has no other opinion about it — `check-guardrails.ts` only hands every guard the same whole-tree file list, and `export-consumers.ts` keeps `SCANNED_SURFACES` inside itself for the same reason. Known and left: the `core-` filename prefix is now inaccurate, but `core-retired-reconciler` shows the prefix here means "structural guard" rather than "core-scoped", and renaming reaches `check-guardrails.ts` and the test file, so it is a separate edit.

## tui — user request: the agents pane takes the slash-command framing (2026-09-15)

User request, verbatim: "lets also make the agents panel have the same UI as slash commands, just under the status bar". Not a numbered finding — recorded here because it landed in the middle of pass 7/8 and because the way it failed is the lesson.

Scoping held on position and missed on count. There is no separate status bar: the `ready · Claude Sonnet 5 · medium` line is `BorderLabelItem`s on ComposerFrame's bottom border (`routes/session.tsx:107-190`, rendered `components/composer-frame.tsx:33-45`), and the pane already registered at `slot: "below-input"` (`extensions/builtins/agents-view.client.tsx:597`, mounted `routes/session.tsx:307`), so it was already below that line and the mount point did not move. What differed was only the framing. I had also said there were two `Dock` sites in the file; there is one (`:503`). The thing near `:628` is the `widgetContribution` that mounts the pane, and the pane's detail surface is a `ChromePanel.Section` inside the single Dock.

Landed `44806509`. The structural gain was not the swap itself: the title row was already hand-rolled identically in `components/autocomplete-popup.tsx:152-154` and `components/command-palette.tsx:467-471`, so copying `PickerFrame` usage into a third pane would have made three copies. The title moved into `PickerFrame` as a `title` prop instead and both existing consumers lost their own copy. Deletion test: the change removed code at the call sites rather than adding a third copy.

Two accommodations the `PickerFrame` shape needed, both found by rendering rather than by reasoning. `useDockGeometry` subtracts 5 columns for a bordered pane's border, margin and pad (`components/chrome-panel.tsx:128-133`); a picker has no side border, so reusing it truncated every row 5 columns short, hence `usePickerGeometry`. And `pickerHeight` budgets one body row per item, which is right for a flat list but starves this pane, because it opens each section with a heading and adds a detail line — 5 rows across 3 sections draw 8 lines. `drawnLines` counts drawn lines instead. Only `agents-view` and `thread-view` use `decoration()`, so `pickerHeight` stays correct for the palette and the autocomplete.

**Then the gate passed it and it was still visibly broken.** Every agent row wrapped its age onto a line of its own (`1m` under `› • New Chat · main · idle`), on every row, found by reading the live pane at `wZ:p18` side by side against the `/` popup. Fixed `771dc606`.

The budget was off by three, not one. Rows are not drawn in the frame directly: they sit inside `SelectList` -> `ChromePanel.Body`, a scrollbox padding one column each side (`components/chrome-panel.tsx:203-213`), and the row box adds its own `paddingLeft={1}` — so a row spends 3 of the rule's columns, and `rowWidth` returning `width - 1` left a line exactly as wide as the terminal. Measured across 40/58/59/60/80/81/100/120 columns: rows drew at `rule - 3` every time, no rounding term and no scrollbar column, so this wrapped at **every** width including the 80 the tests use — never a narrow-pane tipping point. `sectionWidth` was already right at `width - 2`, because a `Section` sits outside the body's padding and carries no row pad. The missing `wrapMode="none"`/`truncate` on the agents row `<text>` was a real inconsistency with its siblings (`autocomplete-popup.tsx:112-113`, `thread-view.client.tsx:401`) but not the cause: with the budget fixed, removing the clamp keeps all 12 tests green, because `rowLine` already builds exactly `rowWidth()` columns. Kept anyway, as a guard on a future row built wider than the budget, and the commit says it is not probe-forced.

Why the pass-7 tests could not see it, which is the part worth keeping: the row fixture omitted `updatedAt` entirely, so `ageFor` returned `""` (`agents-view.client.tsx:314-318`) — and the age is the only part of a row drawn against the right edge, so no row could overflow however long its name. The width assertion was then `rowLine.length > rule - 3`, i.e. `79 > 77`, which passed while the row overspent by exactly 2 and which **rejected the correct 77 once fixed**. The tolerance encoded the bug's own arithmetic. Tests now use an `agedRow(id, name, updatedAt)` fixture and pin the measured values (`rule - 3` for rows, `rule - 1` for the detail line, `{ row: 55, section: 56 }` at 58 columns), with two cases asserting the age is not alone on a line.

Probes separated the two halves: geometry reverted with the clamp kept, 1 fail / 11 pass; clamp reverted with geometry kept, 0 fail / 12 pass; both reverted, 3 fail / 8 pass; final, 0 fail / 12 pass. An earlier attempt at the geometry probe passed 11/11 on rendered assertions alone, because the clamp cuts to the row's box whatever the budget says, so no rendered row can witness an overspent budget — a direct budget assertion was added to close that gap.

One thing I judged wrong and was corrected on: the `› │` line under the title is the shared filter row `SelectList` draws whenever `showInput()` is true (`components/select-list.tsx:376-402`), and the agents pane passes `filter` without `showInput` (`agents-view.client.tsx:530`). Correct that it is not a defect, wrong about the witnesses — the command palette opts out with `showInput: false` (`command-palette.tsx:483`) and the autocomplete popup passes no `filter` and draws its own `›` inline (`autocomplete-popup.tsx:155-156`), so neither of the two pickers I cited actually draws it.

Pane check PASSED at `wZ:p18`, binary rebuilt 13:29 from gated main at `771dc606`: rules top and bottom at full width, no side borders and no rounded corners, title `Agents · 0 running, 1 idle, 370 inactive` inside the top rule, one muted footer line below it, `Idle (1)`/`Inactive (370)` headings present, and after scrolling, every age (`9s`, `16m`, `32m`, `33m`, `47m`) right-aligned on its own row's line with long delegate names ending in `…` on one line. Indented child rows budget correctly.

`thread-view` does not share the bug and was left untouched: it is still on `useDockGeometry` (`width - 7`) against the same 3-column spend, measured with a populated `updatedAt` at 58 and 80 columns with the age on the row line and zero wrapped lines both times.

T38 pane check PASSED at `wZ:p18`, run on the merged framing so it covered both at once. With the agents pane open, the palette's Sessions level was used to switch to a different session (the current one is marked `•`, so the cursor was moved off it), then the pane reopened with `^t`. The pane re-keyed: the title went from `Agents · 0 running, 1 idle, 370 inactive` to `0 running, 2 idle, 369 inactive`, the `Idle` heading from `(1)` to `(2)`, and the selection marker `›` moved to a different row than the newly-idle session — so the rows describe the session now in the shell, not the one it left. No row carried a previous-session value, and the ages stayed right-aligned with no wraps. This is the guard `makeClientSessionQuery` added: the hand-rolled fetches both wrote whatever reply landed last, with no post-reply key check.

## tui — pass 8 (2026-09-15, after `771dc606`)

src 22,058 LOC across `apps/tui/src` (37,487 with its 57 test files). Scoped by recent history per the skill's YAGNI rule, weighting what the reframing and the width fix had just touched: `components/picker-frame.tsx`, `extensions/builtins/agents-view.client.tsx`, `components/chrome-panel.tsx`, `components/command-palette.tsx`, `components/autocomplete-popup.tsx`, `theme/context.tsx`. Read-only pass: no edits, commits or rifts. **Does not close the package.**

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Verdict         | Status                                                        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------- |
| T39 | **Two frame dialects for one docked-pane job.** `PickerFrame` (`components/picker-frame.tsx:62-90`) now carries three consumers — `agents-view.client.tsx:528`, `command-palette.tsx:468`, `autocomplete-popup.tsx:151`. `ChromePanel.Dock` carries two — `thread-view.client.tsx:437`, `settings-picker.tsx:102` (which backs `/model` and `/think`) — plus `useDockGeometry` at `:382` and `:63`, and a context row-claiming registry (`DockContext`/`DockRegistry`/`claimDockRow`, `chrome-panel.tsx:81-97,175,190,226,242,264,287`) whose only job is deriving the Dock's height. Two pieces already have **no production reader at all**: `DOCK_BODY_ROWS` (`:100`) and the `bodyRows` prop (`:139-140`) are referenced only by their own declaration and `tests/components/chrome-panel-dock.test.tsx`. The two geometry hooks are the same shape with different chrome constants — `width-2` then `-5/-4` (`chrome-panel.tsx:127-135`) against `width-3/-2` (`picker-frame.tsx:54-60`). | Worth exploring | done `2118d469`/`18c44917`/`1183aef6` — **pane check PASSED** |
| T40 | `toThemeCatalog` (`theme/context.tsx:22`) is an identity function — the body is the parameter — with one call site at `:59`. It does no type work: `DEFAULT_THEMES` is already `satisfies Record<string, ThemeJson>` (`theme/default-themes.ts:19`), and the only widening the store needs is for `setStore("themes", "system", …)` at `:82` to add a key, which the spread's inferred type already permits. No reference anywhere else in `apps/` or `packages/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Speculative     | done `1183aef6`                                               |

**User decision (2026-09-15) on T39: convert both panes, keep `PickerFrame`.** Put to the user with rendered previews of all three framings, because it trades a rounded 10-row box for a 6-row ruled picker on a real model roster — a product call, not a cleanup. The user was offered a third option that raised `pickerHeight`'s 6-item cap and did **not** take it, so the cap stays: `/model` and `/think` show 6 rows at a glance and scroll for the rest. Rows are not lost either way — both frames put the list in a scrolling `ChromePanel.Body` (`chrome-panel.tsx:203-215`) with `useScrollSync` scrolling the cursor into view (`select-list.tsx:301`, `hooks/use-scroll-sync.ts:41-49`). Recorded so a later pass does not re-litigate the framing.

Deletion test on T39: **concentrates.** Removing `Dock`, `useDockGeometry` and the row-claiming registry removes a whole second frame dialect and its derived-height mechanism, leaving one frame module with one height rule and one column budget. `DOCK_BODY_ROWS`/`bodyRows` concentrate on their own, being inert already. `ChromePanel.Root`/`Body`/`Section`/`Error`/`Footer` all survive — `Root` still backs three centred pickers and the rest are used inside `PickerFrame` consumers.

Three of the four leads I handed the pass were disproved, and the disproofs are worth keeping so they are not re-raised. The palette and autocomplete do **not** carry the body-padding overspend that broke the agents pane: both lay rows out with flex (`autocomplete-popup.tsx:108-135`, `command-palette.tsx:434-451`), so an overspend truncates inside a box rather than wrapping a line, and neither does row-vs-rule arithmetic at all. Only two `truncate(left,width).padEnd(width)` + right-column rows exist in the whole TUI — `thread-view.client.tsx:393` and `agents-view.client.tsx:467` — and both are correct for their current frame; the only other `padEnd` hits are `main.tsx:542,550`, a fixed-column stdout table. And `pickerHeight` does not need the agents pane's `drawnLines` correction promoted into the shared module: rendered at 58x24 with a `Section` plus N body rows, items=1 drew h=6, items=2 h=7, items=3 h=8, items=6 h=11, with the Section and every item drawn and nothing clipped — the `+5` already covers a Section. Agents needs `+1` only because it interleaves per-section headings and a detail line. One pane needing a line count is one adapter, not a seam.

One new fact for T22's deferred row: `components/branch-picker.tsx:122-133` hand-rolls the Dock box itself and sets its own `paneHeight`/`rowWidth` (`:90-91`, a third width dialect at `width-8`), and its `ChromePanel.Error`/`Footer` (`:142-143`) sit in a raw `box` with no `DockContext` provider — so `claimDockRow` is provably inert there, and `bodyRows`, the prop T22 planned to use, has no caller at all.

Checked, not reported: the three centred `ChromePanel.Root` pickers repeat one geometry block with only the width cap differing (`prompt-search-palette.tsx:34-37` cap 80, `message-picker.tsx:43-46` cap 70, `routes/auth.tsx:529-532` cap 70) — four lines each, and `Root` already owns the positioning, so extracting would move complexity rather than concentrate it. `theme/context.tsx` carries no T35/T36 residue: `all` is live at `command-palette.tsx:105` and `ready` is gone (the `local-health.ts:73` `.all()` is an unrelated SQLite method).

Prior art, measured TUI-only for a like-for-like comparison the earlier passes did not have: gent `apps/tui/src` 22,058 LOC, opencode-v2 `packages/tui/src` 60,198 (its `ui/` frame layer alone 3,232), pi `packages/tui/src` 18,107. Earlier passes cited 21,917 / 36,724 / 99,396, which were whole-package figures. Nothing to add from either. opencode-v2's frame layer is `ui/dialog.tsx` (256 LOC) with `dialogWidth(size)` collapsing all sizing to three fixed widths plus a modal backdrop, `zIndex: 3000` and a dialog stack (`ui/dialog.tsx:13-18,33-53`) — the overlay model gent's pane decision rejects — and its `dialog-select.tsx` is 903 LOC against gent's `select-list.tsx` 423. Its one nameable piece is `ui/select-controller.ts`, 45 LOC of pure selection math, which gent already has factored as a reducer behind `SelectList` and tested at `tests/components/select-list.test.tsx`, so importing it would add a module without removing one. pi passes `maxVisible` in per call site (`components/select-list.ts:53-56`, `settings-list.ts:57-65`) with no shared geometry hook at all — less locality than gent's single `pickerHeight` rule — and its `settings-list.ts` (328) duplicates `select-list.ts` (273) rather than sharing. What to REMOVE relative to both: gent is the only one of the three carrying two frame dialects for the same job plus a context registry to derive one of their heights. That asymmetry is exactly T39, and it is the only place either prior art argues gent carries more concept than the capability needs.

T39 landed in three commits, each compiling: `2118d469` converts both panes onto `PickerFrame` and adds the shared line-count seam, `18c44917` deletes the Dock frame and its row-claiming registry, `1183aef6` drops T40's identity wrapper. `chrome-panel.tsx` went 309 -> 180 lines. Gone, after a zero-reference sweep across `apps/` and `packages/`: `ChromePanel.Dock`, `useDockGeometry`, `DockContext`/`DockRegistry`/`claimDockRow` with all five call sites, `DOCK_BODY_ROWS`, `DOCK_BORDER_ROWS`, `DockGeometry`, `ChromePanelDockProps`, the `bodyRows` prop. `Root`/`Body`/`Section`/`Error`/`Footer`/`Success` all survive. Gate exit 0 on main at `1183aef6`.

The consumer list I handed over held on every Dock symbol, and `bodyRows` did have no production caller. One correction the implementer made against itself, worth keeping as a grep receipt: it deleted `ChromePanel.Success` as collateral and was wrong — `routes/auth.tsx:571` uses it. The first sweep grepped the internal declaration name `ChromePanelSuccess`, which matches only its own declaration; the dotted member name `ChromePanel.Success` is what finds consumers. The internal name and the exported member name are two different grep targets and only the second finds callers.

Column arithmetic measured rather than reasoned, at 40/58/80/120 columns for both converted panes: rows drew 39/57/79/119 against rules of 40/58/80/120 — `delta = 1` constant across all eight readings, and the thread age stayed on its row line at every width. `usePickerGeometry`'s `width - 3` is right for both. Incidental gain: under the old Dock at 40 columns the footer wrapped onto two lines (`esc` / `close`); `PickerFrame`'s footer is `wrapMode="none" truncate`, so the conversion fixes that.

The line count became a real seam rather than a second hand-rolled copy. Thread-view interleaves session headings via `decoration()` **and** adds a detail line — both of the things that forced `drawnLines()` in the agents pane — so that is the second adapter, and `pickerLines(drawnItems, extraLines)` now lives in `picker-frame.tsx` with agents-view moved onto it and its private copy deleted. Settings-picker counts its query row through the same call. This is the promotion tui pass 8 explicitly declined to make on one adapter; a second one earned it.

Dock tests: two subject tests of the deleted mechanism were retired ("its derived height is the body plus the chrome rows it actually mounts", "a section outside a Dock counts nothing" — both assert the row-claiming registry itself), and the one vehicle test ("thread and settings panes keep the same body") was migrated into the new `tests/components/docked-pane-frame.test.tsx` as two height-rule tests under the ruled frame, plus new column-budget tests. 179 lines deleted, 229 added.

**A coverage gap disclosed rather than papered over — and narrower than first recorded, corrected here by pass 9: the settings-picker's _rendered row_ cannot witness an overspent column budget.** Probe B widened its budget and the test still passed; a rewritten probe B2 also failed to catch it. The cause is structural — the settings row has no right-edge-anchored field, so its detail is gap-padded to a computed width and then clamped by the row box, and an overspend still draws exactly one unwrapped line. The overspend is invisible in the output. What the settings test does guard is the shared `usePickerGeometry` seam: probe E broke the seam to `width - 7` and produced 6 failures across both files. So the number both panes consume is protected, but settings-picker alone misusing it would not be caught. Thread-view is fully guarded because its age is edge-anchored. **Correction (pass 9): the settings budget IS asserted** — `tests/components/docked-pane-frame.test.tsx:185-227` renders `SettingsPicker` at width 58 behind a `Probe` that yields `usePickerGeometry` directly and pins `expect(seen[0]).toEqual({ row: 55, section: 56 })` at `:220`, with a comment at `:186-191` stating the reasoning: the drawn row cannot witness an overspend, so the budget itself is the only place the spend stays visible. That is the same technique the agents pane uses (`agents-pane.test.tsx:598-599`). So the seam that decides the width is guarded and settings-picker misusing it would be caught; only a rendered-output witness is missing. Giving settings rows a right-anchored field to make the overspend render-observable is therefore **not** the follow-up it looked like — it would add a product-visible column to `/model` and `/think` purely to serve a test, relocating an assertion that already exists at the seam. Deletion test: moves complexity.

Probes: A (thread `rowWidth` reverted to the bordered `width - 7`) 0 pass / 1 fail; C (thread `pickerLines(..., 0)`) 0 pass / 1 fail; D (settings drops its query row) 0 pass / 1 fail; E (the `picker-frame` seam broken to `width - 7`) 11 pass / 6 fail. The empty-fixture trap that hid the agents-pane wrap for a whole pass was avoided deliberately: the thread fixture resolves `updatedAt` from `Clock.currentTimeMillis`, so the age renders as `2d` and rows can genuinely overflow.

`branch-picker.tsx` (T22, untouched) compiles clean. It only used `ChromePanel.Error`/`Footer`, both surviving, and its provably-inert `claimDockRow` calls are simply gone now that those two components no longer make them — no behaviour change. Its third width dialect (`width - 8`) and hand-rolled box remain for T22, which is now a smaller job: one pane, one dialect, against a single surviving frame.

T39 pane check PASSED at `wZ:p18`, binary rebuilt 18:32 from gated main at `1183aef6`. The pane was ~20 columns at the time (it shares tab `wZ:t1` with another pane, so it is a vertical split) — a harsher width than the 58 the fix was measured at, which makes the row results stronger rather than weaker.

`/model` drew the ruled frame: a rule above and below with no rounded box, title `Model · 103`, the 6-row cap visible with the rest scrolling, `●` marking the active model, and the footer on one line. `/thread` is the one that mattered, because its row is the `truncate(left, width).padEnd(width)` + right-aligned-age shape that broke twice. It rendered `Thread · … 2 windows` with two session headings (`New Chat (1 wind…`, `main: Task 1: Fi…`), two window rows and the detail line `agent-run:01a0a5f…`, and **both ages sat right-aligned on their own row's line**, with the labels truncated by `…` rather than wrapping. Section headings and the detail line all fit, so `pickerLines` budgeted correctly with `decoration()` interleaved. The agents pane was re-checked after being moved onto the shared `pickerLines` seam and also held: `Idle (1)` and `Inactive (371)` headings present, detail line present, and `1m`/`5h`/`5h` each on its own row's line.

Reaching a thread with windows took a session switch: `/thread` on the gamut session reads `0 windows` and renders `no windows`, so the row width is untestable there. A delegate session from the earlier gamut run (`- main: Task 1: …`) has the parent chain that produces windows.

## tui — pass 9 (2026-09-15, after `7074a773`) — **CLOSED**

src 21,978 LOC (was 22,058 at pass 8; the frame collapse was net −80). Scoped to what T39 had just changed, on the reasoning that a large deletion leaves residue: `components/chrome-panel.tsx`, `components/picker-frame.tsx`, `components/settings-picker.tsx`, `extensions/builtins/thread-view.client.tsx`. Read-only pass. Typecheck green at a clean tree.

**No Strong and no Worth-exploring finding. tui CLOSES — and with it the whole loop.** All four packages are now closed: extensions (pass 5, `5b768a29`), sdk/server/tooling/e2e (pass 6, `a9e32e01`), core (pass 8), tui (pass 9).

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verdict     | Status          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------- |
| T41 | `ChromePanel.Root`'s `bottom` prop is dead: declared `chrome-panel.tsx:36`, forwarded once at `:61`, and no call site passes it — all four production `Root` sites pass `top` instead (`btw.client.tsx:158`, `prompt-search-palette.tsx:80`, `message-picker.tsx:80`, `auth.tsx:568`). The six exported prop interfaces (`ChromePanelRootProps:30`, `BodyProps:79`, `FooterProps:103`, `SectionProps:119`, `ErrorProps:133`, `SuccessProps:153`) each have exactly two references, both inside `chrome-panel.tsx` — the declaration and its own signature. Nothing outside the file names them. | Speculative | done `82f99951` |

Verified independently before recording: `command grep -rn "bottom" apps/tui/src/components/chrome-panel.tsx` returns only `:20` (a doc line about `Footer`), `:36` and `:61`, and no `bottom=` appears anywhere in `apps/tui/src`.

**`ChromePanel` still earns its keep after the collapse.** Pass 9 was explicitly invited to find it had become a bag of small boxes now that its height mechanism is gone, and the honest answer is that it survives. `Body` is not reached directly by the panes — it lives inside `SelectList` (`select-list.tsx:405-420`), so all five `PickerFrame` consumers reach it transitively. `Section` has 17 dotted references spanning both frames: inside `PickerFrame` (`autocomplete-popup.tsx:153`, `command-palette.tsx:469`, `agents-view.client.tsx:554`, `thread-view.client.tsx:474`, `select-list.tsx:396`) and inside `Root` (`btw.client.tsx:190`, `auth.tsx:609,620`). `Error` has 5 and `Footer` 9, likewise spanning both. `Success` has one live consumer at `auth.tsx:571`, confirmed via the dotted member name — the check that nearly went wrong during T39. Deletion test: **scatters.** Deleting `ChromePanel` would copy the same padded-box body into `select-list.tsx`, `picker-frame.tsx`, `branch-picker.tsx`, `auth.tsx` and `btw.client.tsx`. It is the one row vocabulary both frames share, which is why the collapse left it standing.

Three more leads evaluated and rejected, with receipts, so they are not re-raised. The centred-geometry extraction stays rejected and is now better supported than in pass 8: there are **four** `ChromePanel.Root` sites, not three, and the fourth does not share the block — `btw.client.tsx:147-149` is `Math.min(width-4, 100)` / `Math.max(8, height-4)` with `left` only and a literal `top={2}`, no vertical centring. A shared hook would serve three of four call sites with two different caps, forcing a parameter per axis: an interface nearly as large as the four lines it replaces. Deletion test: moves complexity. And there is no second `PickerFrame`-shaped thing waiting to become a consumer — the four `Root` sites are genuine overlays (`auth.tsx:562` mounts inside an absolute full-screen box; the other three float over the transcript), so converting any would turn an overlay into a docked pane, a product change the pane decision does not ask for. `composer-frame.tsx` is a label ribbon with no list and no rule, and `mermaid-viewer.tsx:150-178` is a full-screen viewer. `picker-frame.tsx:92` is the only place in the TUI that draws `border={["top","bottom"]}`.

Material for T22's deferred row, recorded rather than re-proposed as a finding. Converting `branch-picker.tsx` onto `PickerFrame` would delete precisely: the hand-rolled rounded box at `:122-133` (11 lines of `backgroundColor`/`border`/`borderStyle`/`borderColor`/`title`/`marginLeft`/`marginRight`/`alignSelf`), the third width dialect `rowWidth = width - 8` at `:91`, and the fourth height dialect `paneHeight = Math.min(16, Math.max(6, height - 8))` at `:90`. It would gain `usePickerGeometry()` and `pickerHeight(...)`, and its `ChromePanel.Error`/`Footer` at `:142-143` carry over unchanged. Net: −13 lines, −2 geometry dialects, −1 rounded box, leaving `ChromePanel.Root` as the only rounded frame. It is a docked pane mounted in the composer column (`session.tsx:297`), not an overlay, so the framing change is consistent with the pane decision. If T22 is taken up it owes a pane check on a resumed multi-branch session, because it changes a rounded box into a ruled pane on the boot path.

Prior art, re-measured at `7074a773`: gent `apps/tui/src` 21,978 LOC, opencode-v2 `packages/tui/src` 60,198, pi `packages/tui/src` 18,107. **Nothing left to add from either, and nothing left to remove that either argues for.** opencode-v2's frame layer is `ui/` at 3,232 LOC across 19 files and is the shape gent's pane decision rejects: `ui/dialog.tsx:44-53` is a full-screen `zIndex: 3000` backdrop with `RGBA.fromInts(0,0,0,150)` dimming and mouse-dismiss, and `dialogWidth(size)` at `:14-18` collapses sizing to three fixed widths. Its `dialog-select.tsx` is 903 LOC against gent's `select-list.tsx` 423, and gent's already owns the selection reducer, scroll sync, sticky anchor and key table behind four required props. Its one nameable piece, `ui/select-controller.ts` (45 LOC of selection math), is already factored in gent as `SelectListEvent`/`transitionSelectList` and tested, so importing it would add a module without removing one. pi is the minimalism reference and gent is now within 3,871 LOC of it while carrying panes, extensions, themes and a command palette pi has no equivalent of — and on the axis under review pi has _less_ locality: `maxVisible` is passed in per construction site (`select-list.ts:6,14,17`) with no shared geometry rule, and `settings-list.ts` (328) duplicates `select-list.ts` (273) rather than sharing. That is precisely the two-dialect asymmetry gent just deleted. Pass 8 named exactly one thing either prior art argued for removing, and T39 removed it. Post-collapse gent has one frame module with one height rule (`pickerHeight`), one line rule (`pickerLines`), one column budget (`usePickerGeometry`) and one shared row vocabulary (`ChromePanel`).

## Loop closed (2026-09-15)

Every package has been run until a pass yielded no Strong and no Worth-exploring finding: extensions at pass 5, sdk/server/tooling/e2e at pass 6, core at pass 8, tui at pass 9. The goal is met.

Open items deliberately left, each with its reason: **T22** (branch picker, deferred; pass 9 priced it at −13 lines and −2 geometry dialects, and it owes a pane check), **T41** (one dead optional prop and six needless `export` keywords, Speculative), **S10** (deferred, revisit condition unmet — a second debug provider), **S15** (skipped), `findPersistedEvent`'s three cold-path scans (adjudicated Speculative in passes 7 and 8, the bounded id-range pattern exists if it is ever wanted), and the alias guard's `SRC_PREFIX = "packages/"` leaving two real `apps/tui` implementations out of range.

## T22 — the branch picker joins the one frame (2026-09-15, `869f5ccb`)

T22 had been deferred since pass 4 and was priced by pass 9. It landed with T41 in one rift, because both edit `chrome-panel.tsx` and would collide as separate branches.

The priced deletion list held on every item. Gone from `components/branch-picker.tsx`: the hand-rolled rounded box at `:119-133` with all ten of its props (`height`, `alignSelf`, `marginLeft`, `marginRight`, `backgroundColor`, `border`, `borderStyle="rounded"`, `borderColor`, `flexDirection`, `title`), the fourth height dialect `Math.min(16, Math.max(6, height - 8))` at `:90`, and the third width dialect `Math.max(8, width - 8)` at `:91`. Net −13 lines on the source file, matching the estimate. **`ChromePanel.Root` is now the only rounded frame in the TUI** — `border` + `borderStyle="rounded"` survives in `chrome-panel.tsx` alone.

One deviation from the brief, in its favour: `ChromePanel.Footer` did **not** carry over as priced. `PickerFrame` takes `footer` as a prop, so the footer text moved there and the element went away. `ChromePanel.Error` carried over unchanged. The footer still renders — from the frame rather than as a claimed chrome row.

**The old dialect was a live defect, not a redundancy.** Measured by rendering a 400-character branch label and counting drawn columns against the rule: the converted pane draws `delta = 1` at 40/58/80/120 (rule 40/58/80/120, row 39/57/79/119), reproducing the signature both prior width fixes produced. The old `width - 8` stopped **five columns short** at 58 — 52 against a 57-column budget. Every branch label had been truncated early on every terminal since the pane was written.

`pickerHeight`, not `pickerLines`, and the reasoning is recorded so it is not second-guessed: a branch row is one line per branch, with no heading opening a group and no detail line following the list — unlike `thread-view.client.tsx:449` and `agents-view.client.tsx:519`, which pass `pickerLines(n, 1)` for a heading plus a detail line, and unlike settings, which passes `visible().length + 1` for its query row. Drawn items equal held items, so `pickerLines` would be an identity here. The 6-row cap was left untouched, per the user decision.

**An honest limit on what the test can witness, stated rather than papered over: a branch row has no right-edge-anchored field.** `formatBranchLabel` draws one left-aligned `name (count)` and stops — unlike a thread row's age or a settings row's gap-padded detail — so a branch row can be cut short but can never wrap a tail onto its own line. The rendered frame therefore cannot witness an overspent budget by itself. No fixture was invented to fake one. The column test pins the budget directly through the `Probe` technique the file already established, which is why that assertion is load-bearing rather than the rendered length. This is also why the fixture trap that hid the agents-pane defect for a whole pass does not apply here.

Probes: A (`rowWidth` reverted to `width - 8`) 6 pass / 1 fail, expected 57 received 52; B (`paneHeight` reverted to `Math.min(16, …)`) 6 pass / 1 fail, expected 7 received 16; C (`bottom={2}` re-added at a call site) rejected by typecheck with `TS2322 Property 'bottom' does not exist`. Suite back to 7 pass / 0 fail. Gate exit 0.

T41 landed alongside at `82f99951`: `Root.bottom` removed at both its declaration and its forwarding line, and the six prop interfaces lost their `export` while keeping the interfaces. The T39 near-miss was avoided deliberately — grepping the **dotted** member name found `ChromePanel.Success` live at `routes/auth.tsx:571`, so the component stays and only the `export` on its props interface was dropped.

T22 pane check PASSED at `wZ:p18`, binary rebuilt 22:07 from gated main at `82f99951`. All five points hold. The frame is ruled top and bottom at full width with no side borders and no rounded corners, sitting under the composer's label line as a continuation rather than a floating dialog. The title reads `Resume: New Chat`. Both rows — `01a0a733 (0)` and `01a0a7f3 (0)` — draw on one line each, neither wrapping a tail nor stopping visibly short of the rule. The footer reads `↑↓ move   ↵ resume branch   esc close` on one line below the bottom rule. With two branches the six-row cap is not exercised, so the frame sized to its content.

Reaching the picker at all took three attempts and the dead ends are worth recording. The palette's "Branches" row does **not** open it: `command-palette.tsx:269` calls `pushLevel(branchesLevel())`, a palette level that switches branches inline and renders palette chrome (`Branches 2 [All] General`, `Tab Category`, `Esc Back`). `BranchPicker` is boot-only. `session-controller.ts:194` opens it from `props.initialBranches`, which `app-bootstrap.ts:161` fills solely on the `branchPicker`-tagged bootstrap state, and that state is emitted at `app-bootstrap.ts:308-322` (explicit `-s <id>`) or `:345-355` (resume-by-cwd) **only when `branches.length > 1`**. The gamut cwd resumes the most recently updated session, which had one branch, so the resume path silently took the `session` arm. `gent -s 01a0a733-30a3-715f-8db3-714e8f3f72f4` is what renders it — a session prepared earlier by creating a second branch through the palette.

## Slash commands run on the first Enter (2026-09-15, `370c3ed4`)

Reported by the user: pressing Enter on a slash command inserted a space, and a second Enter opened the pane. The ask was one Enter, "unless the slash command requires arguments".

My diagnosis was half right. The trailing space was exactly where I said — `use-composer-controller.ts:168` hard-coded `` `${type}${value} ` ``. The Enter routing was not: `handleAutocompleteKey:367-370` returns `Option.some(false)`, which means "not handled, propagate", not "consumed". The key that actually eats Enter is `select-list.tsx:335-340`, bound while `open={hasItems()}` at `autocomplete-popup.tsx:161-177`, which calls `onSelect`. The user-visible effect matched my description; the actor did not.

**No argument metadata was added to `Command`, and that is the finding.** The obvious design — `args?: "none" | "optional" | "required"` — fails the deletion test: **no command in this repo requires an argument**, so all 13 registration sites would set `"none"` or `"optional"` and no site would ever change behaviour. A flag scattered across 13 files that decides nothing. Every arg-aware command (`/model`, `/think`, `/goal`, `/driver`, `/loop`, `/btw`) treats an empty arg as "open my picker" or "show usage", so naming a command is always a complete invocation. The decision concentrates in one predicate instead: `isSlashCommandName` at `commands/slash-commands.ts:70-86`, called from one branch of `handleAutocompleteSelect`. It matches on `slash` and `aliases` case-insensitively, the same way `executeSlashCommand` does, and never consults `onSlash` — `onSlash`-presence was the wrong test, since `/model` and `/think` carry it while taking optional args.

The dispatch branch is guarded three ways: `state.value.type === "/"`, `Option.isNone(formatInsertion)`, and `beforeTrigger.length === 0`. So `@`/`$` keep their insert semantics, an extension supplying `formatInsertion` for `/` keeps its own, and a mid-text trigger does not fire. An unregistered name fails the predicate and falls through to the old insert path. Extension-contributed commands needed no change: `context.tsx:255-258` sets `slash` and `onSlash: run`, and `run("")` is what `onSelect` already called.

**A near-miss worth recording, because it is the fixture trap in a new costume.** The first test version passed 4 of 5 for the wrong reason — the mock controller copied from `composer-render.test.tsx:41-42` calls `transitionComposerInteraction(current, event)` with no contributions, defaulting to `[]`, so no popup ever opened and Enter reached the plain submit path that already worked. Only `session-controller.ts:389` threads `ext.autocompleteItems()`. The harness was fixed to thread them. **The existing `composer-render.test.tsx` slash test still has this hole** — it asserts only `frame).toContain("/")` and never opens a popup.

Probes on `apps/tui/tests/components/composer-slash-enter.test.tsx` (baseline 5 pass / 0 fail): A (dispatch branch deleted) 2 pass / 3 fail; B (`isSlashCommandName` forced `false`) 2 pass / 3 fail — same signature, isolating the predicate. Restored from a `command cp -f` snapshot after each, tree verified clean.

Left alone as out of scope: a slash name matching no registered command still needs two Enters, the first swallowed by the open popup, the second reporting "Unknown command". Pre-existing, unchanged.

## The model list orders by release date (2026-09-15, `ea9428a7`)

Requested by the user: order the model list by release date using models.dev data. The data was already being fetched and dropped — `ModelsDevModel` at `model-registry.ts:26-30` parsed only `name`, `cost` and `limit`.

The sort lives in `ModelRegistry.load` (`model-registry.ts:194-197`), after the driver catalog filters, not in `parseModelsDev` or at the TUI call site. Three paths leave the service — disk cache, remote fetch, driver-filtered catalogs — and only `load` covers all three; sorting at ingest would leave old cache reads unsorted. The comparator `byReleaseDateDesc` is at `domain/model.ts:36-51`. Both consumers were checked and neither sorts: `routes/session.tsx:275` and `routes/session-command-registry.ts:179`.

**Two measurements decided the schema, and one of them was not mine.** I measured the live payload before delegating: 7,823 models, **0 missing `release_date`**, which killed the placement-policy question for undated models before it was designed — undated sorts last and nothing more was built. The agent then found what I had missed: **213 of 7,823 carry a partial date** like `2025-04`, year-month with no day. That ruled out `Schema.Date`, which would fail or coerce on decode and rewrite values on cache round-trip. The field stores the raw string and compares lexicographically, which is correct for ISO-8601 prefixes — a partial sorts just ahead of fuller dates in the same month.

**The cache trap was real and was proven, not argued.** `releaseDate` had to be `Schema.optional`: `CachedModelsJson` decodes `~/.gent/models.json`, and on decode failure `readCachedModels` returns empty and falls through to `fetchRemote` — so a required field would have degraded `/model` to an empty list until a network round trip landed. Probe 4 (field made required) failed 11 of 12 tests. Verified against the real pre-change 7,823-model cache: decodes clean, `releaseDate` reads `undefined`, re-encoding does not inject the key.

Probes (baseline 12 pass / 0 fail, tree verified clean after each restore): 1 (drop `release_date` from parse) 10/2; 2 (remove sort from `load`) 11/1; 3 (undated sorts first) 11/1; 4 (field required) 1/11.

`resolveModelQuery` does not depend on order for which result it returns — exact-id match wins first and the single-candidate shortcut is count-based, not position-based. Only the display order of `Ambiguous` candidates changed.

## Pane receipts for both (2026-09-15, `wZ:p18`)

Binary rebuilt 23:49 from gated main at `370c3ed4`, `GATE EXIT 0` sole occurrence at line 681. Five checks, all PASS, in `/private/tmp/gent-gamut` (pid 72177).

`/agents` + one Enter opened the Agents pane, and the composer returned to a bare `┃` with no `/agents` left behind — `submitSlashCommand` calls `clearInput()` at `:287` before dispatching. No doubled mount. `/model` + one Enter opened the picker. `/model sonnet` + one Enter dispatched with the argument and did **not** open the picker, reporting `"sonnet" matches 4 models` from `resolveModelQuery`. `@gam` + one Enter inserted `@gamut.ts` and left the composer open without submitting, so the `@` path kept its insert semantics.

**Ordering was verified against the data, not by eye.** The picker showed 103 models — more providers are authed on this machine than the agent assumed when it predicted a 14-model Anthropic-only list. Head of list: GPT-5.6 Luna, GPT-5.6, GPT-5.6 Terra (all `2026-07-09`), then Claude Sonnet 5 (`2026-06-29`). Descending, confirmed by reading the cached `releaseDate` for those exact ids. The gateway-prefixed `2026-09-15` entries that top the raw payload are correctly absent, since those providers are not wired. Ties are stable rather than ordered — `Claude Opus 4.5` and `Claude Opus 4.5 (latest)` share `2025-11-24` — which the comparator's `if (l === r) return 0` intends.

The picker does not render dates, so the order is implicit to a user reading it.

## Tab ran commands instead of completing them (2026-09-16, `6d5279f7`)

**A regression I shipped at `370c3ed4` and signed off with a pane check that could not see it.** Caught on the next pane check, not by the gate.

`370c3ed4` put the slash-dispatch branch inside `handleAutocompleteSelect`. Tab and Enter share that exact callback: `autocomplete-popup.tsx:167-174` (Tab's `extraKeys`) and `:175` (Enter's `onSelect`) both call `props.onSelect(item.id)`, both wired to `ctx.handleAutocompleteSelect` at `composer.tsx:158`. So Tab inherited dispatch and there was no code path by which it could complete-without-running. Live proof: `/ag` + Tab executed `/fork` and printed `No messages to fork`, leaving the composer empty.

The loss was real, not cosmetic. Tab-completes is how a reader builds `/model sonnet` — complete the name, then type the argument. A Tab that dispatches leaves no way to reach an argument at all.

**Why my earlier check missed it.** I typed `/ag` + Tab on the `370c3ed4` binary and recorded Tab as unchanged. The selected row happened to be `/agents`, so dispatching it looked exactly like completing-then-running. The check was real; it could not discriminate. The agent that wrote `370c3ed4` reported "Tab behaviour unchanged" and cited the correct file — it read the lines it quoted but did not follow the callback into the branch it had just added one screen above.

**The fix is a second prop, not a boolean parameter on one callback.** `AutocompletePopupProps` gained `onComplete` beside `onSelect`. The popup is the last place in the chain that still knows which key arrived — `extraKeys` fires only for Tab — so that is where the two intents part company. Behind the seam, `use-composer-controller.ts` keeps one insert core, `completeAutocomplete(value, dispatch)`, with two named entry points; the boolean is private to the module and no key name travels downstream. Deletion test: rewiring Tab back to `props.onSelect` fails 4 tests, so the prop is load-bearing.

The dispatch guard now reads `dispatch && type === "/" && Option.isNone(formatInsertion) && beforeTrigger.length === 0 && isSlashCommandName(...)`, so the `@` and `formatInsertion` paths are untouched on both keys.

Probes: baseline 9 pass / 0 fail across the two neighbour files; with the fix 13 pass / 0 fail; Tab rewired to `props.onSelect` (the exact pre-fix wiring) 9 pass / **4 fail**. The fifth new test, `inserts a file reference on Tab`, correctly still passes under the break — `@` never dispatched, so the regression cannot reach it. That is the "do not regress `@`" constraint holding, not a coverage gap.

One deliberate change beyond the ask: the popup footer read `Enter Select`, which is now false. It reads `↑↓ Navigate   Enter Run   Tab Complete   Esc Close` (narrow: `↑↓ Move · ↵ Run · ⇥ Complete · Esc`). The first gate run was `GATE EXIT 1` on `composer-render.test.tsx:162`, which asserted the old label; the assertion was updated rather than the footer reverted, because the footer is where a reader learns the two keys differ.

**A row-ordering fact that made the bug visible.** `slashAutocompleteItems` (`session-command-registry.ts:41-66`) does no relevance sorting — unlike the command palette, it walks `commands` in registration order and keeps anything matching slash name **or title**. Under filter `ag`, "Fork from Message" and "Manage API Keys" both match, so `/fork` and `/auth` precede `/agents`. That is why Tab ran `/fork`.

## Pane receipts, Tab split (2026-09-16, `wZ:p18`)

Binary rebuilt 00:34 from gated main at `6d5279f7`, `GATE EXIT 0`. Seven checks, all PASS.

`/ag` + Tab → composer reads `/fork `, nothing runs. `/model` + Tab → composer reads `/model `, no picker; typing `sonnet` + Enter then switches the model — the destroyed affordance, restored. `/agents` + one Enter → pane opens. `/mod` + one Enter → Model picker opens, 103 rows, still ordered newest-first (GPT-5.6 Luna `2026-07-09` → Claude Sonnet 5 `2026-06-29`). `/xyz` + one Enter → `Unknown command: /xyz`, composer clears. `@gam` + Tab and `@gam` + Enter → both insert `@gamut.ts` and neither submits. Footer renders `↑↓ Navigate   Enter Run   Tab Complete   Esc Close`.

**Two lessons about reading a pane, both of which produced a false reading this session.**

The popup marks its selected row with `fg: theme.primary` plus `bold` and **no marker glyph** (`autocomplete-popup.tsx:98-119`), unlike `/model` and `/agents`, which draw `●`/`›`. `herdr pane read` strips color, so two captures across an arrow press are byte-identical and the selection appears frozen. It is not: reading with `--format ansi` showed the selected row at `238;238;238` against `208;208;208` for the rest, moving from `/fork` to `/think` on one Down. **A capture that cannot witness the thing under test is not evidence of absence.**

Separately, a `--lines 6` window over a tall open picker returned an empty capture, which reads as "nothing happened" and is indistinguishable from a real failure. Re-run at `--lines 20` showed the picker open and correct. Size the window to the surface, and re-run an empty capture before believing it.

## Open, not oversight (2026-09-16)

**Cmd/Meta+Enter over an open popup now submits**, where before it silently did nothing. `5d39e8a7` removed two guards from `handleTextareaKeyDown`: the interject branch's own `Option.isSome(autocompleteOption())` bail and a separate blanket swallow below it. Nothing binds Cmd+Enter to a row — `SelectList` claims plain `return`, never the meta variant — so a Cmd+Enter reaching the composer is one no popup was going to consume, and the old bail made interject a dead key whenever a `/` popup happened to be open. The edge worth naming: `/mod` + Cmd+Enter now submits the raw draft and reports `Unknown command: /mod` rather than resolving to `/model`. Untested either way, left for the user to decide.

## Ghost completion and ranked autocomplete (2026-09-16, `34ff5e26`)

User ask: "add autocomplete to the composer, so for skills, slash commands, file paths etc, add a little muted autocomplete you can press tab to complete - using FFF for each."

**"FFF for each" could not be taken literally, and the measurement is why.** `@ff-labs/fff-bun@0.10.6` (latest; no upgrade exists) exports `binaryExists`, `findBinary`, `FileFinder` and types only. `src/fff-api.ts` is file-shaped throughout — glob prefiltering, `accessFrecencyScore`, `modificationFrecencyScore`, filename-match bonuses. There is no arbitrary-string matcher, and slash commands and skills have no files to index. So `@` keeps FFF untouched; `/` and `$` needed a different matcher.

**`match-sorter` was the obvious candidate and lost on measurement.** It already ships and already ranks the command palette, so it was the no-new-dependency default. The user pushed back that it is slow; benchmarking agreed, per keystroke, one query against the whole corpus, warmed:

| corpus              | match-sorter | subsequence scorer |
| ------------------- | ------------ | ------------------ |
| 30 slash commands   | 18.52 µs     | **2.25 µs**        |
| 62 skills           | 35.63 µs     | **4.20 µs**        |
| 2000 items (stress) | 959.10 µs    | **95.59 µs**       |

~8-10x at every size. Quality decided it more firmly than speed: ranking `ag` over the real command set, `match-sorter` answered `/agents /goal /config /new` because it matches inside _descriptions_ ("Fork from Mess**ag**e", "Man**ag**e API Keys"), while the subsequence scorer answered `/agents` alone. For ghost text that is decisive — the ghost shows the top candidate, so a matcher that pads the list surfaces a wrong ghost. Shipped as `components/autocomplete-ranking.ts`, no new dependency. The benchmark's weights were a sketch; the agent tuned its own against tests over the real corpora.

Ranking, before and after: `/ag` was `/fork /auth /agents` (registration order, `includes` on name _or_ title) and is now `/agents` with `/fork` and `/auth` dropped entirely. `$te` puts `test` first. `@` is byte-identical — FFF already ranks it well.

**The inline ghost was not delivered, and the reason is a hard limit, not a shortcut.** `TextareaRenderable.placeholder` is empty-buffer-only with a private `applyPlaceholder` and no offset, so it cannot draw a suffix after typed text. Virtual extmarks were the only other inline mechanism and failed two empirical probes against a real renderable: marks created with `virtual: true` are stored and returned by `getVirtual()` but **never reach the screen** in `@opentui/core` 0.5.11 — tried with zero-width and non-empty ranges, with a `styleId`, and with three data key spellings — and an undo-corruption claim that did **not** survive my own probe: with the cursor correctly positioned, a control with no extmark behaved identically to a marked one (`base="/ag"`, `typed="/agX"`, `undone="/agX"` in both), so undo is inert in that harness with or without a mark, and the mark's 3->4 offset shift after an insertion is correct bookkeeping rather than corruption. **This is an unimplemented feature, not a bug:** `extmarks` is absent from the package index, reachable only through a renderable getter and typed `any` on `EditorView`; the docs never mention it; and the native layer has no virtual-text draw entry at all — `editorViewGetVirtualLineCount` counts wrapped display lines, an unrelated sense of "virtual". The upstream move is a feature request, not a bug report. A grep for render-side use of `getVirtual()` in the compiled core found none, and 0.5.11 is the latest published version, so upstream offers no escape today. Drawing beside the textarea failed differently: a shrink-to-fit input hands the ghost whatever columns remain on each wrapped row, splitting it mid-word on a long draft.

What shipped is a muted row of its own under the input (`composer.tsx`, `theme.textMuted`, trailing `⇥`). It survives wrapping at every width, and it has a property the inline version would not: **the ghost is never in the buffer, so no submit path can carry it.** Inline ghost text needs an upstream `@opentui/core` change to render virtual extmarks.

**A disclosed weakness, verified rather than taken on trust.** The scorer's shortest-name-wins bias makes one- and two-character filters unreliable: `$t` ranks `tdd` above `test`, `/t` and `/th` reach `think` and never `thread`, `$p` prefers `pr` over `prototype`. Confirmed live at `$t`. It becomes reliable at three characters. The mitigation is that the ghost appears only when the top row _extends_ what was typed, so a scattered subsequence match never produces one.

Probe counts (baseline 13 pass / 0 fail on the two prior regression files): A (registry stops ranking) 8/3; B (ghost row stops rendering) 2/2; C (skills stop ranking) 10/1; D (scorer returns everything unranked) 11/9 and seam 4/7. The agent's first probe run proved nothing for two of three fixes — the tests called the scorer directly and so could not detect a _caller_ that stopped calling it; seam tests were added and it re-probed. Final: seam 11/0, scorer 20/0, ghost 4/0, prior regressions 13/0 unchanged.

## Pane receipts, ghost completion (2026-09-16, `wZ:p18`)

Binary rebuilt 01:13 from gated main at `34ff5e26`, `GATE EXIT 0` at log line 679. Nine checks, all PASS.

`/ag` → popup lists `/agents` first (was `/fork`) and a muted `agents ⇥` row renders under the composer; ANSI confirms it is genuinely muted at `138;138;138` against the selected row's `238;238;238`. Tab → draft becomes `/agents `, nothing runs, ghost clears. `/agzz` → "No matches", **no ghost row**. `/agents` fully typed → no ghost, nothing left to offer. Plain prose `hello wor` → no ghost row, and Enter sent exactly `hello wor`. `/agents` + one Enter → agents pane opens, so the first-Enter dispatch survives the ranking change. `$te` → skills popup with `test` first and ghost `test ⇥`. `$t` → `tdd` above `test`, the disclosed limit, reproduced. `@gam` → FFF order unchanged, `gamut.ts` first, ghost `gamut.ts ⇥`.

## Frecency ranking over the picks you make (2026-09-16, `7394005d`)

User ask: "we can also do frecency by creating some sort of view over the session dbs, since it should log when skills are/commands are used maybe?" then "ok lets record, and make a read-only for derived ranking".

**The session-DB premise did not survive a check of the live data.** The gamut session DB holds 88,865 events and contains **no slash-command events and no `skill` tool** — slash commands are resolved entirely in the TUI and never reach the runtime as events, and skills arrive as ordinary prompt text. A view over the session DBs would have ranked nothing. So the recording had to be added at the point of the pick, and a new store was the only place to put it.

`components/autocomplete-frecency-store.ts` is the module: a 14-day half-life over `count * 0.5^(age/HALF_LIFE_MS)`, capped at `MAX_ENTRIES = 200`, persisted to `~/.cache/gent/autocomplete-frecency.json`. Ranking reads a module-level snapshot so scoring stays synchronous inside the scorer's hot path; only writes go through Effect.

**`FRECENCY_MAX = 8` sits deliberately below `BOUNDARY = 12`.** Frecency can reorder rows that already score comparably, and cannot lift a poor subsequence match above a word-boundary hit. A frequently-picked `/thread` does not outrank an exact `/tree` prefix.

Recording happens in `completeAutocomplete` (`use-composer-controller.ts:173`), above the dispatch branch, so **both Enter and Tab record**. That is intended: Tab is a use of the item. Verified live — Tab on `/tree` moved its count from 1 to 1.9999 (the fractional part is the half-life decay on the prior entry, not an error).

## The second writer silently dropped the first's picks (2026-09-16, `316e494f`)

Found in my own pane check of the shipped feature, reproduced four times before I trusted it.

`use-autocomplete-frecency.ts` held the store in a module singleton behind a `cell.loaded` guard. The guard meant a **write that landed after the first load was invisible to the hook**, so the next `/` pick serialized a stale in-memory snapshot over the file. The `$` popup (`extensions/builtins/index.ts`) was a second, independent writer. Picking `/tree`, then `$triage`, then `/thread` produced `["/tree","/thread"]` — `$triage` was silently gone.

Two writers, one file, no shared fold. The fix makes the store module the single writer: both surfaces call `recordFrecencyPick`, a `Semaphore.makeUnsafe(1)` serializes the read-fold-write, and `writeFrecencyStore` no longer appears outside the module. `tests/autocomplete-frecency-cross-writer.test.ts` covers it, 6/0; regression proof against the two original writer strategies is 0 pass / 1 fail, naming the missing `$triage`.

**Scope stated honestly: this is within-process safety only.** Two `gent` processes sharing a home each hold their own semaphore and can still interleave. That exposure predates this fix, is untested and unfixed, and is the next thing to close if it matters.

## Pane receipts, frecency and the lost write (2026-09-16, `wZ:p18`)

Binary rebuilt from gated main at `316e494f`, `GATE EXIT 0` at log line 680.

The clobber sequence, from `rm -f ~/.cache/gent/autocomplete-frecency.json`: `/tree` → `["/tree"]`; `$triage` → `["/tree","$triage"]`; `/thread` → **`["/tree","$triage","/thread"]`**. On the old build step 3 returned `["/tree","/thread"]`. Reverse order from a second cold start: `$triage` → `["$triage"]`; `/tree` → `["$triage","/tree"]`. The fold holds in both directions.

Derived ranking confirmed on the same store: `/t` returned `/tree /thread /think`, lifting the two picked commands above `/think`, which had sorted first on `/th` when nothing had been picked. Tab on `/tree` completed the draft to `/tree` and **did not** open the tree pane, so the `6d5279f7` split survives the frecency change.

One pane-reading note worth keeping: the split pane is 23 columns, which truncated every capture mid-word and made popup rows unreadable. `herdr pane zoom --on` widened it to the full terminal and the rows resolved. Check the pane's own width before trusting a narrow capture — a truncated row is not a short row.

## Durable picks, a floor under frecency, and a reordered status row (2026-09-16, `8400fd25`)

Three asks in one batch, plus one the user raised mid-flight.

**The write is atomic against other processes.** `writeFrecencyStore` replaced the file in place, so the single-permit gate from `316e494f` protected one TUI and nothing else: a second `gent` sharing a home could still interleave. Writes now land on a pid-named temp file and `rename` onto the target. The pid is in the name because two processes sharing one temp path would corrupt each other rather than merely race.

**What I could not prove, stated rather than papered over.** I wrote a test asserting a reader never observes a half-written file, and it passed with the fix reverted — so it proved nothing. A direct probe settled why: 20,000 concurrent reads against 200 overwrites of a megabyte store produced **zero** torn reads, because Bun's `write` is not observably partial to a same-process reader. The test was deleted rather than kept for the look of coverage, and the module comment now carries the justification instead: `rename(2)` is atomic against readers in _other_ processes, which is the case the in-process semaphore cannot reach. Two tests that do discriminate were kept — the temp file never survives a write, and a pick written behind this process's back is folded rather than clobbered.

**Frecency now has a floor at three characters** (`FRECENCY_MIN_FILTER`). At one or two characters the matcher separates rows by hundredths of a point — `$t` splits `tdd` from `test` by 0.08 — so a single past pick decided the row under the cursor for a filter naming almost nothing, and the ghost line offered it. Below the floor rows rank exactly as they did before frecency existed. This deliberately gives back the short-filter tie-break that frecency was introduced to fix; the user chose that trade.

Five existing tests asserted the superseded contract and were rewritten, not relaxed: four proved the seam using a one-character filter, which the floor now forbids, so they moved to three characters where the seam is still real. A new case pins the floor itself — fifty picks of `test` still leave `$t` answering `tdd`.

**`/frecency-reset` forgets every pick**, on disk and in memory, inside the write gate so a concurrent pick cannot re-create what was just removed. It removes the file rather than writing an empty store, keeping "never picked" and "picked then cleared" the same state.

**The status row reads phase, cwd, model, effort, context, cost.** Cost moved out of the top-left group to the far end, effort moved ahead of the context gauge to sit beside the model name, and the cwd came out from behind `props.debugMode` — several concurrent sessions were otherwise indistinguishable.

Four deletion probes, each run against its own fix: the floor removed → 1 fail; effort-before-ctx reverted → 2 fail; the reset's snapshot clear removed → 1 fail; the atomic write reverted → **0 fail**, which is what condemned that test. Gate `GATE EXIT 0` on the rift and again on main.

## The status row drops what it cannot fit (2026-09-16, `cef8a706`)

Shipping the reordered row at `8400fd25` broke it, and the pane check caught it: the live row read `idle · gent-gamut (main) · Claude Sonnet 5` with the effort, the context gauge and the running total simply gone.

`ComposerFrame` spent one left-to-right width budget and `break`ed at the first label that did not fit. Every label after that vanished with no ellipsis and no indication — so adding the cwd near the front silently deleted the three labels at the end. The arithmetic, reproduced: at 58 columns the old order dropped `$12.34`; the same row without the cwd fit everything.

The row now has two groups. The right group — the context gauge and the cost — is laid out first from the right edge inward and keeps its columns; the left group spends what remains and truncates. The user chose this over shortening the cwd, because the anchored labels are the ones a reader checks without reading the row.

The gauge also moved out of `buildTopRightLabels` into its own `buildContextLabels`. Effort names how the model is configured and belongs beside the model name; the gauge reports what the session has spent and belongs beside the total. One helper emitting both could not put them at opposite ends.

Deletion probe: with the right group unreserved, the anchor test fails with `$12.34` absent — the exact silent drop. Eight ctx cases moved from the `buildTopRightLabels` suite to a new `buildContextLabels` suite rather than being deleted.

## A scroll bug I could not reproduce, and three theories that died (2026-09-16)

The user reported that scrolling up in gent was stuck. **Nothing shipped for it**, because every mechanism I proposed failed its own test.

First: commits only happen when the live transcript overflows the viewport, so a short session has genuinely nothing in terminal scrollback. True, and it explains a _short_ session — but the user confirmed a long one, so it is not their bug.

Second: that a committed item's own late fields break the prefix check. `committed` was keyed by a JSON fingerprint of each item, and a tool call gains `durationMs`, a summary, an output and a terminal `status` after its message commits — so the fingerprint of a settled item changes under it, `prefixMatches` fails, `requestReplay` clears `committed`, and scrollback never accumulates. The story is coherent and it is **wrong**: instrumenting the check under both keyings showed `prefixMatches` stayed true on every pass and `committedLen` climbed 1→2→3 either way. The test I wrote for it passed with the fix reverted, which is the same worthless shape as the atomicity test earlier this session. Both the change and its test were reverted rather than merged on a story.

Third: that an overlay or pane leaves `renderer.useMouse` true, swallowing the wheel. `overlayOpen` and `transcriptExpanded` are both read reactively inside the effect that owns `useMouse`, so closing either re-runs it and restores `false`; a docked pane is not an overlay at all and never touches it. There are exactly four `useMouse` writers, all in one file.

The user scrolls with the **mouse wheel**, which depends on gent leaving mouse tracking off so the terminal receives it. That is where the next attempt should start, and it should start from an observation of the live terminal rather than another reading of the code — three readings produced three refuted theories.

## The buffer kept its rows, then lost them to a key order (2026-09-16, `4c043775`)

The observation the last section asked for. Five turns on the live pane, each one long enough to overflow the viewport, with the offering pass instrumented to a file: `max_offset_from_bottom` grew 23 → 32 → 41 → 50 → 59 and the markers from every turn stayed in the buffer together. Scrollback accumulates. Two of my earlier readings were measurement error — a capture taken before the commit queue drained reported a frozen buffer, and re-reading after the queue settled showed the rows had landed.

What the instrumentation did catch, once, is the defect. One pass recorded `prefixMatches: false` at index 1 with the same message id on both sides, `oldFp` beginning `{"_tag":"regular-message","id":…` and `newFp` beginning `{"id":…`. Same message, same text, different string.

**The feed builds one message two ways.** `createAssistantMessage` writes `_tag` first and carries no `segments` and no `metadata`; `buildMessages` spreads the body and appends `_tag` last. `fingerprint` was a JSON encode of the whole item, so it carried key order and key presence. Reduced to two literals outside the app: `EQUAL false`, `SORTED EQUAL true` — the objects differ by nothing but the order of their keys.

A rebuilt message therefore broke the committed prefix, `requestReplay` cleared `committed`, and `resetSplitFooterForReplay({ clearSavedLines: true })` discarded the terminal's saved lines. The session above the fold disappeared.

`transcriptFingerprint` now names the drawn fields in a fixed order, for messages and for all four session events. A rebuild is silent; new text, a completed tool call, and a changed event still change the value.

**This corrects the second theory in the section above.** The mechanism was right — a committed item's fingerprint changing under it — and the field I blamed was wrong. I accused the tool-call fields that arrive late, tested that with synthetic plain objects, watched the test pass with the fix reverted, and reverted the change. Plain objects cannot reproduce it: the divergence needs the two real construction sites. The instrument had to run against the live feed to see it.

The first regression test repeated the same error in a new shape. It called `transcriptFingerprint` directly, so it passed with the call site returned to the whole-object encode — green against the bug it was written for. The test that ships drives the component: it commits an item, hands the transcript the same messages rebuilt in the other key order, and asserts the committed rows do not grow. Reverted, it fails 4 → 8, the rows written a second time.

Gate `GATE EXIT 0` on the rift and again on main.

## A thread is a column, not a walk (2026-09-16, `88db927c`)

The thread pane showed the parent line only, so a session that handed off twice
showed one of its two children and the sibling work was invisible from inside
the thread it belonged to. The first fix walked `parentSessionId` to the root
and descended the tree. The user then asked that handoffs be marked "so legit
forks or `/btw` doesn't pollute the thread", and I recommended deriving that at
read time from the spawn receipt: `admitChildSession` files an `agent.start`
row naming its child, a compaction handoff files none, so a `LEFT JOIN` with
`WHERE receipt IS NULL` separates them with no new state.

**That recommendation was wrong, and the user's "thread_id? maybe idk" was
right.** `parent_session_id` carries two meanings — "continued from" and
"spawned by" — and the receipt separates them only from the perspective of the
thread being asked about. A delegate that later compacts asks for its own
thread, climbs `parent_session_id` into its parent's tree, and is then filtered
out of that tree by its own receipt: it gets back a thread it is not in. I had
written that exact case as the third storage test and asserted `["root"]` as
correct. The bug was encoded as an expectation, which is why the derivation
looked green.

A column answers both halves. A handoff inherits the parent's thread; every
other create says nothing and storage roots a new thread at the session itself.
It is barely state: `createSession` is the only `INSERT INTO sessions`, so
there is one writer, and the value is derived once at write time — when the
writer already knows which kind of child it is making — instead of on every
read. The read collapsed from a two-stage recursive CTE plus a `json_extract`
anti-join to one indexed lookup.

The receipt stays the discriminator; it moved to the one place that does not
need to infer it. Migration 019 still uses it, correctly, for the one-time
backfill: an existing session is a spawn exactly when a receipt names it, and
everything else inherits the nearest non-spawn ancestor's thread. Probed
out-of-band against a rebuilt pre-019 schema — `root/handoff1/handoff2 → root`,
`delegate/delegate-handoff → delegate`.

`threadChain` lost its tree walk rather than keeping a client-side copy of the
rule. The walk is not merely redundant now, it is wrong: a spawn's own handoff
shares no parent link with its thread's root, so a DFS would drop a row the
server correctly returned.

Deletion probe: with the write-time distinction removed, so that every child
inherits its parent's thread, the two tests that encode it fail — the spawn
reappears in the thread it was launched from, and the spawn's own thread gains
`"root"`. The other two stay green, which is right; they do not exercise it.

Two failures the targeted runs missed and the gate caught: the pinned migration
list needed `session_thread` in **both** assertions, since the test asserts the
same set before and after a reboot, and `ADD COLUMN` needed
`ignoreAlreadyAppliedSqliteError` like `003` — the schema-rewind test deletes
migrations `>= 11` and replays them against a database that already has the
column. A misplaced `yield* RelationshipStorage`, anchored into
`getSessionSnapshot` instead of the handler factory, produced sixteen
`TS377030` errors in six files that never mention it; `rpc-handlers.ts` says
why in a comment above its own yields — `RpcGroup.toLayer` erases handler
residual requirements, so a Tag yielded anywhere else degrades the layer.

Gate `GATE EXIT 0` on the rift and again on main.

## One verb to come back, and side work you can see (2026-09-16, `cff6c392`)

The thread column shipped in `88db927c` was verified by its tests and by the
gate. This entry is the live half, in a real terminal, plus the two CLI changes
that came out of running it.

**The thread, measured against the real database.** A delegate run, a `/btw`
side question, and a forced `/handoff`, in one session in the gamut pane. The
delegate's child was written `thread_id = id` with a parent — a spawn, by the
new write path, not the backfill. `/btw` left no row at all: it is private and
its session is deleted. The handoff child was written `thread_id != id`, and
the thread went from one member to two. `/thread` agreed with the query at
every step: `Thread · 1 session` before the handoff, `Thread · 2 sessions`
after, with the spawn absent from both.

Migration 019 also ran for real, against 571 existing sessions: 21 spawns, 301
handoffs, the rest roots. Handoffs outnumbering spawns fifteen to one is the
right shape — compaction is routine, delegation is occasional — and is evidence
the receipt backfill did not mistake chains for side work.

**`-c` could only say "the last one".** The session id is the only handle on a
conversation once the TUI exits, and nothing printed it. Exiting now prints
`to resume: gent resume <id>`; `gent resume <id>` opens that session, and
`gent resume` with no argument opens the last one in this directory. That is
the whole of what `--continue` meant, so the flag retires. `gent` and
`gent resume` differ only in how they name the session to open, so the entry
body became `runGent` and both commands call it.

Printing the line needs `process.stdout` after `renderer.destroy()` — outside
Effect by construction, since the renderer is gone. `noGlobals` is disabled on
that one line with the reason, the way `main.tsx` already does for the CLI
teardown's `process.exit`.

**The picker marks side work.** The sessions palette nests children under
parents, so a delegate run and a `/btw` sat in the list looking like
continuations. A session whose thread is itself while still having a parent is
side work, so those rows now carry "side thread" in the description column. It
is a pure function of fields the row already has; no new query.

Deletion probe: remove `description: spawnLabel` from the row and the test
fails with `Received: "    - Delegate"` — no marker. The assertion reads the
rendered row, not `isSpawn`, so deleting the wiring fails it. A unit test on
the predicate alone would have passed, which is the vacuous shape this ledger
recorded two entries ago.

Two things the pane taught that the gate could not: a double-tap Esc needs both
presses inside one second, so sending them in separate calls a second apart
only resets the timer and never quits; and the resume line printed the handoff
child after a handoff and the root after resuming the root — correct both
times, because it names the session that was live, not the thread.

Gate `GATE EXIT 0` on the rift and again on main.
