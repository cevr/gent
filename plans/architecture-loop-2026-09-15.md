# Per-package architecture loop (2026-09-15 →)

Goal (user): run `/improve-codebase-architecture` on each package until no
more findings, compared against opencode v2 (Effect) and pi (minimalism);
add and remove while keeping capabilities. Effect-native, actor model, lean
core, fully extensible. Baseline `74b1b5d5`: core 27.6k, tui 24.1k,
extensions 16.5k, tooling 2.3k, sdk 2.0k, server 0.7k, e2e 0.3k src LOC.
pi has no `v2` branch on its origin; opencode's `v2` worktree is the prior
the Effect comparison uses.

Each row: the finding, the deletion-test verdict after my own read, and
what happened. A pass ends for a package when a run yields nothing Strong
or Worth exploring.

## sdk / server / tooling / e2e — pass 1

| #   | Finding                                                                                                                                     | Verdict                                                             | Status                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1a | `apps/server/src/debug/scenario.ts` (520) unreachable: `GENT_DEBUG_MODE` read once, set nowhere; `debug-failing`/`debug-slow` had no setter | confirmed by grep                                                   | landed `0ec4f226`                                                                                                                                                                   |
| S1b | `apps/server/src/main.ts` re-implements `Gent.server` (SDK) as a second root                                                                | real; fixed port + idle shutdown must move onto `GentServerOptions` | landed `0a9aca6e` (main.ts 205 → 84; port/serverId/shell/idleShutdown on `GentServerOptions`; launcher guard)                                                                       |
| S2  | six `packages/e2e` files (1,110 LOC) spawn no process and sit outside the gate                                                              | confirmed (`transportCases` has one direct case)                    | landed `81a5281d` (e2e 2,172 → 930 LOC; six duplicates deleted, rest moved into core gate; pre-existing red: e2e auth PTY test waits for "ChatGPT Pro/Plus", fails on a3c424d5 too) |
| S3  | workspace id hashed twice (`transport-headers.ts` vs `platform.hash`)                                                                       | real, but `client.ts` uses it as a default parameter                | landed `871a2607` (`workspace-rpc.ts` owns `workspaceIdForCwd`; wire constant, not a host fact; third copy in a core test removed)                                                  |
| S4  | `shipped-extensions.ts` alias used by one of the two roots it claims to serve                                                               | pass-through                                                        | landed `0ec4f226`                                                                                                                                                                   |
| S5  | four tooling guard suites never ran (`package.json` named five files); six assertions had drifted to `code-cell`                            | confirmed                                                           | landed `0ec4f226` (`./tests/`; a lint fixture under `fixtures/` must stay out of the glob)                                                                                          |
| S6  | suppression inventory carried four kinds oxlint already bans, and scanned block disables twice                                              | confirmed (zero occurrences of each dead kind)                      | landed `0ec4f226`                                                                                                                                                                   |
| S7  | `core-dead-exports` scans core only; sdk surface pinned by a name snapshot                                                                  | Worth exploring                                                     | landed `b0424020` (`dead-exports.ts` scans core + sdk; snapshot kept, the two guards catch different plants; extensions not scanned, 123 false positives from Schema.Class)         |
| S8  | `runtime-boundary.ts` four-line trampoline for the `runPromise` lint rule                                                                   | Speculative; rule is load-bearing                                   | rejected                                                                                                                                                                            |

## tui — pass 1

| #   | Finding                                                                                                                           | Verdict                                                                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | `use-session-feed.ts` re-projects what `session.getSnapshot` already returns as `ProjectedMessage` (~500 LOC parallel projection) | Strong; pi single-sources, opencode duplicates and is larger                                                                                                                                                                                                                                         | landed `cfe8fdcb`/`ecbaaaa4`/`ea6835c2`: `ProjectedMessage.segments` in core; duplicate replay/live handler collapsed; hook 978 → 933 (live overlay + UI state stay); `AssistantMessageLegacy` is test-vehicle-only, migrate 10 fixtures before deleting |
| T2  | `atom-solid/` (684) has no production consumer beyond a `RegistryProvider` mount and one `Result` type                            | confirmed by grep                                                                                                                                                                                                                                                                                    | in flight (rift `tui-dead`)                                                                                                                                                                                                                              |
| T3  | six panes hand-write the same keyboard/scroll/filter block; `filter-list-state.ts` too shallow                                    | Strong; pi has one `showSelector`                                                                                                                                                                                                                                                                    | DONE 37381f3f: `SelectList` takes the block; five panes on it, `filter-list-state.ts` deleted; wake and btw own no list                                                                                                                                  |
| T4  | `composerSurface`, `ClientComposer`, `paletteLevel` seams have zero adapters                                                      | confirmed by grep                                                                                                                                                                                                                                                                                    | in flight (rift `tui-dead`)                                                                                                                                                                                                                              |
| T5  | 180-LOC router with history for two routes; branch picker is a boot-time pane                                                     | Strong                                                                                                                                                                                                                                                                                               | landed `b6844f84` (router deleted; `navigateToBranchPicker` had zero callers so history was always empty; branch picker is a boot-time docked pane over the mounted session)                                                                             |
| T6  | four-way client context split re-merged by every consumer                                                                         | Worth exploring; order with T1                                                                                                                                                                                                                                                                       | landed `d2faf1cb`: one `ClientContext` + `useClient()`, value built once (identity-stable); six facet hooks removed; helper modules stay (pure, tested subjects); client/ 1,339 → 1,259                                                                  |
| T7  | auth is 1,236 LOC + 1,706 test LOC for an ops-time flow                                                                           | Worth exploring; High risk; after T3                                                                                                                                                                                                                                                                 | landed `d65df2ad`: both pickers on `SelectList`, dead in-flight flags and cursor events removed, catalog is `Option`; src 1,136 → 675 code lines; e2e auth PTY red fixed (label is "Claude Code", not "ChatGPT Pro/Plus")                                |
| T8  | child-session tracker is a third live-agent catalog                                                                               | **rejected**: the tracker is a push projection over `session.events` (0 req/min, one stream per running child) keyed by `toolCallId` with `toolCalls`/`streamText`; the two polled catalogs (`list-agents`, ≤30 req/min ceiling, gated) carry neither. Folding would put live text behind a 2 s poll | —                                                                                                                                                                                                                                                        |

## core — pass 1

| #   | Finding                                                                                                                                                                             | Verdict                                                                                                                                                                                                                                                            | Status                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | turn position inferred by probing 200 message ids; three stacked replay mechanisms (durable bindings, process-local map, persisted results); pi/opencode write the position durably | Strong; the structural one                                                                                                                                                                                                                                         | probe replaced: landed `b86a1040`/`80ea0b9f`/`af2b5441` (`turn_records` table, migration 018, written with the step's messages; resume reads one row). Replay collapse **rejected on evidence**: bindings (4 tests), process-local (15), persisted results (1, newly covered: a cut two-call step) each carry a capability the record cannot express. Gamut run 18 green (19 tests, 3m10s, 8 records written) |
| C2  | `SessionRuntime` is a validating pass-through to the actor                                                                                                                          | **rejected**: eight in-core consumers (`agent-runner`, `child-completion`, `session-mutations-live`, `rpc-handlers`, tests) plus the extension host; it is the actor client, and the message-id/commandId minting lives there                                      | —                                                                                                                                                                                                                                                                                                                                                                                                             |
| C3  | `StepResult.Stop` re-encodes `StepOutcome` as three booleans                                                                                                                        | **rejected as stated**: the booleans are the turn-level end state after continuation policy (a `Failed` step can `Continue`), and `TurnCompleted` carries them; a `TurnEnding` tag would save ~40 LOC, not 120                                                     | —                                                                                                                                                                                                                                                                                                                                                                                                             |
| C4  | `Session`/`Files` facets facaded twice (`extension-services.ts` + `make-extension-host-context.ts`)                                                                                 | Worth exploring                                                                                                                                                                                                                                                    | landed `e40a1e4b`: one build site per facet; four facets were rebuilt per leaf, `Session` alone was double-wrapped; workspace pin now read from the actor entity id (follow-up: pin `Interaction` too)                                                                                                                                                                                                        |
| C5  | queue is three untagged arrays; `startsWith("follow-up:")` policy in `agent-loop.state.ts:119`; both priors use one tagged inbox                                                    | Strong on the prefix check; the merge heuristic is product behaviour                                                                                                                                                                                               | prefix gone: `QueuedTurnItem.keyed` replaces the id-prefix test; three arrays stay (product heuristic)                                                                                                                                                                                                                                                                                                        |
| C6  | six single-importer modules (2,970 LOC) in the turn path                                                                                                                            | Worth exploring; both priors accept flat files                                                                                                                                                                                                                     | landed `b8449ddd`/`236f9a2a`/`cc80c72a`: queue, handlers, actor-state, turn-source folded into their one importer (bodies byte-identical); `worker` and `turn-tool-execution` stay (test subjects); the other seven have 3+ importers (ledger count was stale); 29 → 25 files. Next real seam: `agent-loop.turn-execution.ts` is 1,992 LOC                                                                    |
| C7  | `AgentRunnerService` Tag has one adapter; child policy (depth, visibility, caps) in core                                                                                            | Tag removal **rejected**: `stubAgentRunnerLayer` (test-root.ts) is a second adapter installed by every tool harness and 40 of 48 e2e harnesses; live-runner probe fails `delegate-rpc.test.ts:65` (prompt propagation). Child policy in core stays Worth exploring | open (policy half only)                                                                                                                                                                                                                                                                                                                                                                                       |
| C8  | `transactWithEvent` twice; `makeInteractionService` factory with one instance                                                                                                       | **rejected**: the two helpers differ in shape (fixed events vs mutation-decided envelope); inlining the factory costs a 343-line test rewrite for ~30 LOC                                                                                                          | —                                                                                                                                                                                                                                                                                                                                                                                                             |

## extensions — pass 1

The agent's original E5/E7/E8 text was lost with the transcript; the rows
below record what was verified and acted on.

| #   | Finding                                                                                                                         | Verdict                                                                                                                                                                                                                                                                           | Status                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | `promptGuidelines` on read/grep/bash repeated the same "use the fs tools" prose three times                                     | confirmed                                                                                                                                                                                                                                                                         | landed `56ca6bcc` (one bash guideline; read/grep carry none)                                                                                                        |
| E2  | bash read-only detection was a prefix check; compound commands (`ls; rm`, `$(...)`, `xargs`, `-exec`) slipped past as read-only | Strong                                                                                                                                                                                                                                                                            | landed `56ca6bcc` (`classifyBashCommand`: every segment read-only and no hidden command)                                                                            |
| E3  | anthropic and openai provider modules each own a credential cache with the same persist/invalidate shape                        | Strong; one `makeCredentialCache<C>` behind both Tags                                                                                                                                                                                                                             | landed `7746f9d0`                                                                                                                                                   |
| E4  | ACP adapter could sit on the vendor SDK instead of the hand-rolled protocol half                                                | **rejected**: `@agentclientprotocol/sdk` 1.4.0 is +2 packages (~6 MB) and brings zod beside effect/Schema; only `protocol.ts`+`schema.ts` (685 LOC) are replaceable and the seam (`AcpSessionManager`/`TurnExecutor`) is already right; no fake peer exists to make the swap safe | bug found: `response-finish.ts` switches on AI finish reasons, ACP's `end_turn`/`max_tokens`/… never match → landed (fix + in-memory peer: 36 ACP tests, 25 probes) |
| E5  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E6  | eight cell test files gated on `process.platform === "darwin"` although the cell has no darwin-only dependency                  | confirmed (suite green ungated)                                                                                                                                                                                                                                                   | landed `56ca6bcc`                                                                                                                                                   |
| E7  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E8  | (text lost)                                                                                                                     | —                                                                                                                                                                                                                                                                                 | re-run on pass 2                                                                                                                                                    |
| E+  | model compaction sent whole messages to the summarizer; one oversized tool result starved the older turns                       | Strong (found while reading E1)                                                                                                                                                                                                                                                   | landed `c5858158` (`MODEL_COMPACTION_MESSAGE_CHARS` clip per message)                                                                                               |

## extensions — pass 2

Read-only agent run after 7746f9d0; E9 verified by hand against
`boundToolResultForModel` (core bounds every tool result and pages with
`context.read`). Cell subtree, `provider-credentials.ts`, `acp-agents/` and
`workflows.ts` reported clean.

| #   | Finding                                                                                                                       | Verdict                                                          | Status                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------ |
| E9  | bash truncates a second time (50/50 lines) and spills to `/tmp/gent/outputs`; core already bounds and pages every tool result | Strong; pi keeps one truncation policy                           | landed `716bd7ae` (−202 LOC; stderr now returned separately) |
| E10 | `websearch.ts` (202 LOC) parses an external wire format with no test                                                          | Strong (untested surface, not shallow)                           | landed `8b514dbe`                                            |
| E11 | `readOptionalEnv` defined twice; three drivers hand-build the same contribution literal                                       | dedup Strong; the retry literals are real per-provider facts     | landed `21701c3d` (dedup only)                               |
| E12 | `read` reports `truncated` with no continuation offset                                                                        | Worth exploring; `nextOffset` matches the cell vocabulary        | landed `9a9d0330`                                            |
| E13 | `read-session.ts` exports a `truncate` no caller uses                                                                         | confirmed by grep                                                | landed `21701c3d`                                            |
| E14 | `OutputBuffer.getFullText()` dead and self-documented as broken                                                               | confirmed (one hit: the definition)                              | landed `716bd7ae`                                            |
| E15 | `dispatching-tool-storage.ts` is a 17-line alias with one consumer                                                            | Speculative; the consumer is the core-feature-independence guard | rejected, the guard needs it                                 |

## storage — found while verifying T5 (2026-09-15)

Four `BranchSummarized` rows from a removed feature made session
`01a0836b` unloadable ("Failed to load session events" on every reconnect).
Landed `4e8b2334`: `listEvents` skips rows whose tag left the `AgentEvent`
union and logs `event.retired-tag-skipped`; a known tag with a bad payload
still fails as corruption. Verified in pane `wZ:p18` on the same session.

## batch review (2026-09-15, commits 629e300c..e40a1e4b)

Codex review: one "blocking" (bun.lock) was a false positive (frozen install
clean); eight should-fix items landed in one revision round:
`a348e32c` (transcript commit re-checks mode/generation, clear goes through
the queue, composer shell spill under `~/.gent/shell-output`, SelectList
resets an external filter and clears the cursor on unmount), `79c28c74`
(background bash notice bounded with the `context.read` locator),
`6fe86a21` (test `Files` facet uses the real `Path`), `f5e7f2e5`
(in-process clients count toward idle shutdown).

## tui — markdown history (user request, 2026-09-15)

`NativeTranscript` committed items with the synchronous
`writeSolidToScrollback`, before opentui's async tree-sitter highlight
concealed `##` and backticks; opencode awaits `ScrollbackSurface.settle()`
first. Landed `629e300c`: surface render, settle, commit, serialized on one
fiber chain with the replay reset. Verified in pane `wZ:p18` on session
`01a0a3c8-9c97-759e-ac97-4edefea5384f`.

## tui — pass 2 (2026-09-15, after `044ed18f`)

Claims re-checked by grep on main before acceptance.

| #   | Finding                                                                                                                                            | Verdict                                                                | Status                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------- |
| T9  | agents and thread panes hand-roll the docked chrome box and geometry; `CHROME_ROWS` disagrees (6 vs 5); `ChromePanel` owns only the floating arm   | Strong; add `ChromePanel.Dock`                                         | in flight (rift `tui-panes`)      |
| T10 | four truncation policies; only `picker-text.ts` measures grapheme width; `wake.client.tsx` re-declares `truncate`                                  | Strong; one width-aware primitive                                      | in flight (rift `tui-panes`)      |
| T11 | `SessionController` has 36 members; `client` re-exposes `useClient()`; five prompt-search accessors over one signal; test mock is a 30-key literal | Strong; drop `client`, bundle prompt search                            | in flight (rift `tui-controller`) |
| T12 | `ClientDriver` service + layer + facet union member + two wirings for one consumer (`driver.client.ts`); errors pre-mapped to `Error`              | Strong; three RPC wrappers on `ClientTransport`                        | in flight (rift `tui-runtime`)    |
| T13 | `requestExtension` has three overloads; the explicit-transport arm has zero production callers; one service-arm caller in `builtins/index.ts`      | Worth exploring; delete the overloads, caller uses `transport.request` | in flight (rift `tui-runtime`)    |
| T14 | `prompt-search-flow.ts` is a filter + rename with one caller; its effect type duplicates `SessionUiEffect`                                         | Strong; inline                                                         | in flight (rift `tui-controller`) |
| T15 | `main.tsx` (headless) and `extensions/context.tsx` build the same seven-layer client `ManagedRuntime`; headless stubs five shell callbacks         | Strong; one `makeClientRuntime` factory                                | in flight (rift `tui-runtime`)    |
| T16 | `Effect.catchEager(... client.setError(formatError(e)))` written ten times across three files                                                      | Worth exploring; one `surfaceError` combinator                         | in flight (rift `tui-controller`) |

Untested surfaces named by the pass: `use-composer-controller.ts` (577 LOC) and `session-command-registry.ts` (244 LOC) have no direct tests; `createPasteManager` and `describeAmbiguous`/`slashAutocompleteItems` are pure and testable. Sequenced after the `AssistantMessageLegacy` fixture migration.

## sdk / server / tooling / e2e — pass 2 (2026-09-15, after `044ed18f`)

`apps/server` (86 LOC) is a launcher; no finding.

| #   | Finding                                                                                                                                                                                                            | Verdict                                                                                                                                                                                                                                                              | Status                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| S9  | e2e `pty-fixture.ts` hardcodes `~/.gent/logs/Users-cvr-.../gent-client.log`; the TUI writes `/tmp/gent/logs/<hash>-<ts>-client.log`; `shutdown.watchdog-fired` is emitted by nothing, so the assertion cannot fail | Strong; delete fixture + test (exit-code half duplicates the double-ESC test)                                                                                                                                                                                        | in flight (rift `e2e-sdk-tests`)       |
| S10 | `packages/sdk/src/server.ts` is the only shipped file importing `test-utils` (`LanguageModelLayers`); `ProviderSpec.mock` puts four test knobs on the SDK option surface                                           | **deferred**: `--debug` and `GENT_PROVIDER_MODE=debug-scripted` are product modes with three shipped callers; moving the layer into the launchers needs a carve-out in `bannedLauncherPatterns`, tightened at `0a9aca6e`. Revisit if a second debug provider appears | —                                      |
| S11 | `canSignalServerLockEntry` is exported for tests only; `dead-exports.ts` counts test files as consumers while `core-public-api-consumers.ts` does not                                                              | Worth exploring; un-export, test through `signalIfIdentityOwned`                                                                                                                                                                                                     | in flight (rift `e2e-sdk-tests`)       |
| S12 | two `server-lifecycle.test.ts` suites (lines 103, 145) use no subprocess helper; one duplicates `server-lock.test.ts:142`                                                                                          | Worth exploring; move to sdk tests, then add the process-only guard                                                                                                                                                                                                  | in flight (rift `e2e-sdk-tests`)       |
| S13 | `dead-exports.ts`, `core-public-api-consumers.ts`, `core-public-exports.ts` answer "does this export have a consumer" with three scopes and three test policies                                                    | Worth exploring; one table-driven scan; carries the Schema.Class-aware extensions rule                                                                                                                                                                               | in flight (rift `tooling-export-scan`) |
| S14 | `workspace-test-runner.ts` hardcodes five packages; a new package is silently skipped                                                                                                                              | Worth exploring; derive from `scripts.test`                                                                                                                                                                                                                          | in flight (rift `e2e-sdk-tests`)       |
| S15 | `transport-headers.ts` is an 11-line re-export with three in-package importers                                                                                                                                     | Speculative; skipped                                                                                                                                                                                                                                                 | —                                      |
