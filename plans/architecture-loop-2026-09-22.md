# Architecture loop, fourth run: fewer concepts (2026-09-22 →)

Goal, verbatim from the session: "reduce concepts and code and files (use
single files as much as possible) without removing featureset unless the
features add no value. look to opencode (effect native and codemode), pi
(pico branch - minimal core) and prime-agent (RLM kernel) prior arts to
compare against. see how we can improve across these different
architectures. look at each package in the monorepo. the north stars are
effect-native, actor-model, lean core with maximal expressiveness via
plugins/extensions".

The third run (`architecture-loop-2026-09-18.md`) folded files to one per
concern. This run targets concepts: names an extension author or core
maintainer must learn, and core concepts an extension could own.

Work happens on the rift `arch-loop-4`.

## Baseline (HEAD `5d50b4dd`)

| Package    | Source lines | Source files |
| ---------- | ------------ | ------------ |
| core       | 26,628       | 33           |
| extensions | 17,313       | 27           |
| tui        | 23,403       | 30           |
| sdk        | 2,048        | 6            |
| tooling    | 3,661        | 4            |
| e2e        | 449          | 3            |
| server     | 59           | 1            |

Loop (`runtime/agent-loop.ts`, `runtime/turn.ts`, `runtime/tools.ts`,
`domain/agent-loop.ts`): 7,675 lines. Test files: 115.

Prior art, refreshed 2026-09-22: opencode `v2` at `5c53cfc342`, pi `pico` at
`9672462`, prime-agent `main` at `9fb6cde`.

## Coverage

Every source directory was swept by an earlier run. Pass 1 sweeps all six
areas again with the concept lens: core runtime, core domain/storage/server,
extensions, tui, sdk/tooling/e2e/server, and a cross-cutting prior-art
concept inventory.

## Pass 1 triage

Reports (session scratchpad): `pass1-core-runtime.md`, `pass1-core-domain.md`,
`pass1-extensions.md`, `pass1-tui.md`, `pass1-sdk-tooling.md`,
`pass1-prior-art.md`.

Headline from the prior-art inventory: gent already has the smallest hook
surface (3 kinds against pico3's 8, prime's 27, opencode's 15+), zero tools in
core, and children, compaction and scheduling as extensions. The weight is in
optional fields with no shipped user, second paths for one job, and the TUI's
extension surface (about 30 names, 5 Tags, 3 command stores).

### Batch A — core (rift `arch-loop-4`)

| #   | Finding                                                           | Source              |
| --- | ----------------------------------------------------------------- | ------------------- |
| A1  | Static `prompt` section on tool/request: 0 shipped users          | domain C1, pa C2    |
| A2  | `ToolPolicyFragment.exclude/overrideSet`: 0 shipped users         | domain C8, pa C3    |
| A3  | Resource `tag/start/stop`: tag unread, stop unused, start 1 user  | domain C3/C4, pa C4 |
| A4  | `hook`/`AnyExtensionHook` public, `host.source`, `getLatestEvent` | domain C2/C6/C7     |
| A5  | `DriverRegistry` second owner of drivers; dup `extensionHooks`    | runtime R1-R3       |
| A6  | Host context provider pass-through; `RuntimeEnvironment` overlap  | runtime R6/R7/R9    |
| A7  | `wide-event-boundary.ts` shallow module                           | runtime R4          |
| A8  | Delegate is the only `Interrupt` writer; switch to `Cancel`       | domain C11, pa C1   |
| A9  | Children may inherit the parent thread id (possible bug)          | runtime note        |
| A10 | Comment and ARCHITECTURE drift                                    | H1, C9, pa notes    |

### Batch T — tui (rift `arch-loop-4-tui`)

C1 command stores, C2 dead commands + `/driver` model-turn defect, C3 unread
UI members, C4 collision wipes every client extension, C5 headless registry,
C6 session query helper, C8 btw docked, C9 sessions → agents pane, C10/C11,
C13 loader resolver, C14 dead props, C15 comments. Deferred: C7 (one
ClientContext Tag), C12, C16, C17, C18.

### Batch E — extensions (after A merges)

C1 anthropic via Effect `Crypto` (removes the last shipped core-internal
import and two guard exemptions), C2 `monitor` readonly + no bash guardrail
(bug), C3 agents view labels every loop `main` (bug), C4 index re-exports,
C5 contradictory guidance, C8 drop `read_session` goal (the only blocking
child path; owner rule "children wake, never block"), C9 handoff folds into
workflows + interaction-tools, C10 anthropic `source` param, C11 models-dev
folds into providers, C12 one cell storage Tag, C6 OpenAI chat fallback
(deletion test), H comments.

### Batch S — sdk/tooling/root (after T merges)

S1 one retired-names table, S2 SDK `extract*` wrappers, S3 fold
namespaced-client, S4 two e2e files, S5 server-lock interface, S6 dead root
files (`scripts/fix-imports.ts`, `.husky/`, `PLAN.md`, bunfig/turbo lines),
S7 `createToolTestLayer` third root, W5 78 unused suppressions, W6 two guards
fail deletion test, W7 identity-encode guard blind + untested, W8 root edges,
W2 stale `--debug` seed, CI Bun version drift.

### Pass 2 candidates (design first)

- One delivery verb: `Session.send` with `delivery: steer | followUp`
  (pa C6, domain C14) — opencode and pico3 both have one verb.
- Typed tool namespace in the cell, `tools.delegate.start(...)`, with a
  rendered signature line; drop `tools.search` (pa C8, ext C14).
- Background bash as a one-shot wake job (ext C7): one owner for "run a
  command, wake with its outcome".
- Message-row renderer keyed by `customType` (pa C7).
- `Files`/`Process.run` facets vs raw platform services (pa C9).
- One `ClientContext` Tag (tui C7).
- Delete `@gent/core-internal` (sdk W3).

### Rejected this pass

| Candidate                                                             | Why                                                                                                                   |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| Delete `Interrupt` from `SteerCommand`                                | Steer is a persisted mailbox op; stored rows must decode. Writer removed instead (A8).                                |
| Stop writing `SessionStarted`/`BranchCreated`                         | Variants must stay for old rows; saves ~15 lines, no concept removed                                                  |
| `StreamEnded.interrupted`                                             | Persisted field, 2 lines                                                                                              |
| Resume legacy probe (R13)                                             | Persisted: pre-TurnRecord turns can exist on disk                                                                     |
| Fold the cell trio into one file                                      | Process entry + shared wire module                                                                                    |
| Hook kinds merge; tool vs request merge                               | Already the smallest surface of all four harnesses                                                                    |
| Fork-as-session (delete Branch), replay flag, agent registry collapse | Persisted formats with very high blast radius; owner decision                                                         |
| Host-owned extension state (pa C12), `write` delivery (pa C13)        | Persisted format change; revisit after the delivery verb                                                              |
| Delete `@gent/handoff` (pa C10)                                       | Removes "continue in a new session"; memory keeps `/handoff` as the explicit new-session action. Fold instead (E C9). |

### Pass 1 results (merged to main at `708d365a` and `6efcd691`)

`git diff --stat 5d50b4dd..6efcd691`: 117 files, +4,820 / −6,843 (net −2,023),
source files 109 → 106 (`wide-event-boundary.ts`, `handoff.ts`,
`models-dev.ts`). Gate green on main; gamut `sonnet-sonnet` on main: six
delegate children, completions reached the parent, red app `17 pass, 0 fail`.

| #   | Status                                                                                                                                                                                                                                                               |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | done `64a4f843`                                                                                                                                                                                                                                                      |
| A2  | done `80a6f8d8`                                                                                                                                                                                                                                                      |
| A3  | done `8bd4d9ba` (exec-tools reconcile runs in its layer; a process-resource failure still rejects its extension)                                                                                                                                                     |
| A4  | done `a818c027`; the session-tools naming line stays on `systemPrompt` (moving it would put it before the cell's Host Tools catalog)                                                                                                                                 |
| A5  | done `887c6aa6`                                                                                                                                                                                                                                                      |
| A6  | done `0ef9ffcb`; R5 (one turn-profile path) skipped: ~38 actor test roots have no profile cache, so it adds wiring rather than deleting it                                                                                                                           |
| A7  | done `15972830`                                                                                                                                                                                                                                                      |
| A8  | done `e4195480`; `Interrupt` is decode-only                                                                                                                                                                                                                          |
| A9  | done `bded8dce` (children started in the parent thread; red first); review P1: `/handoff` then lost its thread, fixed `d1024792` (`continueThread` on `session.create`, set only by the TUI handoff)                                                                 |
| A10 | done `a289d998`, review P3 (branch resource failure fails its loop) in `d1024792`                                                                                                                                                                                    |
| T   | done `b82915cd` … `575d45a7` (11 commits); review fixes `71d30505` (notices got their own footer channel instead of `setError`; collisions decided before any transfer; btw takes paste). C9 skipped: the palette's side-thread mark needs `threadId` on agents rows |
| E   | done `bae12d8d` … `9a5f0579` (12 commits); review fixes `17617db9` (stored risky monitors need a clearance mark on re-arm; stored private children are dropped, never delivered; handoff renderer moved under `@gent/interaction-tools`)                             |
| S   | in progress on `arch-loop-4-sdk`                                                                                                                                                                                                                                     |

Receipts on the way: the `@gent/core-internal` guard for shipped extensions
now has zero exceptions (`bae12d8d`). Live run found a gap, not a
regression: `read_session` cannot find the caller's own session id.

### Pass 2 results

| Candidate                                                               | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Delivery verb                                                           | done `29e75d69`, merged `6c5c4454`: `Session.send` takes `delivery: "turn" \| "queue" \| "steer"`, `Session.stop` writes `Cancel`; facade verbs `steer` and `queueFollowUp` gone. Review P2 (no input decode) fixed `da6ba1f1`, red first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| sdk/tooling (S)                                                         | done `02ab1d0f` … `d37cb946` (12 commits), merged `9e63c0f0`: 64 files, +1,539 / −2,611. Review P2s (identity guard skipped a line on any `[`; hook guard accepted a comment) fixed `8f7e0718`, red first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Message rows + ClientContext                                            | done `b7bde7e6`, `5ee160bc`, merged `e42bd6ec`: extension clients render goal, wake and session-message rows by `customType`; five client Tags → one `ClientContext`. Review P2 (native history committed rows before client extensions loaded) fixed `624d7eb9`; the test that raced the load under `--parallel` fixed `a7d5ba0a`. Gamut `sonnet-sonnet`: six children, red app `19 pass, 0 fail`, wake row rendered live and after restart                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Cell namespace                                                          | done `2f8880e2`, merged `c45cc305`: `tools.delegate.start(...)` paths with signature lines; `tools.call` and `tools.search` gone; replay keys unchanged. Review P2s (probe keys, `null` input, `describe` shadowing, optional marks, prompt size) fixed `3df1cc0c`. Found on the way: source runs used a stale 8 Sept worker from `packages/core/dist/gent-cell` — the cell now owns its worker launch and `siblingBinaryPath` is gone (`9af618a1`); source workers skip project `bunfig.toml` and `.env` (`613ba441`); a cell starts in its session's directory, not the host's (`e95ea3b3`). Net +903 lines: a capability, not a reduction                                                                                                                                                                                                                                             |
| gamut wait                                                              | `e03caae1`: `wait` also requires no open turn in the run's `data.db`; it returned while children worked because the agents tray was off screen. Live: held 3 min 49 s until all six completions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Background bash as a wake job                                           | owner decision: adds a persisted `WakeEntry` tag and abandons `background_bash_jobs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `Files`/`Process` facets                                                | done `c396dc59`, guard `f7b905d7`, merged `aca54a47`: 35 files, +440 / −751. `ExtensionContext.Files`/`.Process`, `ExtensionHost.Process`, `GentPlatform.env`, core `makeFileWriter` gone; extensions yield `FileSystem`, `Path`, `ChildProcessSpawner`, `Crypto`; `writeFileAtomic` has one owner in `fs-tools.ts`; relative paths resolve against the session cwd. Counsel clean. Live: six children, red app `19 pass, 0 fail`; a wake monitor fired when its flag file appeared                                                                                                                                                                                                                                                                                                                                                                                                      |
| Delete `@gent/core-internal` (W3) + one enforcement point per rule (W4) | done `f5e2a56f`, `8af1f5db`, merged `bea0f5e5`: 128 files, +2,414 / −2,480, one package gone. Core has two explicit entries beside `protocol` and `extensions/api`: `@gent/core/host` (product callers only, guarded) and `@gent/core/test-utils` (harness operations; a short list of core services tests compose). One oxlint rule, `gent/core-entry-boundary`, replaces the core-internal guard rows and rule, and resolves relative paths. W4: `withX` style, extension imports and host facts each have one oxlint rule; planted violations proved every behavior the deleted guards caught. Review (2 P2, 2 P3) fixed `1e1d83f5` … `eef1c62d`: relative imports resolved; `Effect.fn` wrappers seen; core-behavior tests moved into core; test-only names left `host`. Live: headless `-H` and `sessions`, gamut seven children, red app `19 pass, 0 fail`, `/thread` pane renders |

## Pass 3 (sweep of main at `33c20eb8`)

Four read-only sweeps, one per package group. Reports: TUI 11 findings, core 25, extensions 32 (X tools, P providers), sdk/tooling/root 16. The pass found real defects, so it is not polish-only.

### Pass 3 triage

| Batch                  | Rift               | Findings                                                                                                                                                               |
| ---------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C9 (pass 2 carry)      | `sessions-pane`    | agents pane is the one session browser                                                                                                                                 |
| TUI F1                 | `interaction-load` | pending interaction auto-denied before client extensions load                                                                                                          |
| Core                   | `p3-core`          | bugs P8, P9, P5, P1; P2 if no persisted change; reductions P3, P4, P6, P7                                                                                              |
| Extension tools        | `p3-ext`           | bugs X1 (bash cwd), X3 (grep glob), X4 (monitor timeout); X2 cell deadline vs prompt; X6–X13 where confirmed. X14 parked (background bash owner decision)              |
| Providers              | `p3-providers`     | bugs P1, P3–P7, P2; P9 double retry; P13 dead ACP permission handler; P8, P10–P17 where confirmed                                                                      |
| sdk/tooling/root       | `p3-tooling`       | bugs F2 (two fingerprints), F3 (dbPath attach); F1 core→SDK cycle; F4+X5 examples gate; F5 lint plugin typecheck; F7, F8; F6 docs. F11 (W1 shared mode) owner decision |
| TUI rest (after above) | —                  | F2 delegate child tree to a `delegate.client` extension; F3 border-label positions; F5 connection widget; F4 one pane owner; F10 wake tray pulses                      |

### Pass 3 results

| Candidate       | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C9              | done `259971af`, merged `421bc683`: `/sessions`, `/agents`, `/tree` and the palette "Sessions" item open the agents pane; `sideThread` row projection (no stored change); `sessionsLevel`, `buildSessionTree`, `session.sessions`, client `listSessions` gone; 11 files, +315 / −379. Review P2 (a child with only `parentSessionId` lost the mark) fixed `e6d5bcf7`, red first. Live: gamut `sonnet-sonnet`, six side-thread children before and after `gamut restart`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| TUI F1          | done `c5b951f1`, merged `ccd02717`: the composer waits on `ext.loaded()`; the host draws an unmatched interaction with `PromptRenderer`; the `undefined` default key, `CancelInteraction` and its reducer arm gone. Red first; probes: original code and a dropped load gate both go red. Review P1 (a hung extension hides the interaction for good) fixed `070c5fef`: each import and setup has a 10 s bound, and a timeout is a load failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Extension tools | done `3fe03ace`, `af91fd20`, `eb7bf98e`, merged `2b0d1a90`: bash and monitor run in the session directory (X1); a slash-free grep glob matches nested files (X3); a monitor check stops at its deadline (X4); the cell prompt names its 30 s deadline and sends builds and tests to bash (X2); delegate and session-tools own their prompt sections (X13); `handoff` only on request (X12); summary search is one pass (X10); turn-time delegate reconcile runs once per branch per process (X8). Review (4 P2: slash globs, a failed child delivery stayed undelivered, stored relative monitor cwd, a cut check matched `until`) fixed `a503149a`, red first. Open for the owner: X7 (state files and `GENT_DATA_DIR`), X6 in full (one transcript reader), X9 (`/btw` follower)                                                                                                                                                                                                                                                                                                                                                                                   |
| Core            | done `15f850f7` … `d47499d0`, merged `18ffe6c2`: a caller fails only on its own turn's failure (P8); an interrupt of a parked interaction gives the call a result (P9); an import failure reaches extension health (P5); `model.list`/`driver.*` read the caller's session registry (P1); usage totals are complete or absent (P2); `ExternalToolRunner`, `ExtensionContext.turn`, unread parked fields, `ServerRootPlatformLayer` and `ToolRunner.run` gone; last budgeted step writes no continuation (W8). Review (P1: a queued turn that failed before settle held the queue; P2: interrupted cold recovery wrote over stored results; P2: unknown usage dropped known charges) fixed `e1dbf970`, `82fe35e1`: `turnAfter` gets `usage: { known, complete }`; a budgeted goal pauses with a visible reason when usage is incomplete (`GoalState.pausedReason`, additive). Semantic conflict with the tools batch (`ctx.turn` removed) fixed on main `5a218a51`. Found: a batched follow-up's waiter returns before its content runs → rift `p3-followup`                                                                                                          |
| TUI rest        | done `b001de64` … `6a5954f2`, merged `4581f199`: delegate child tree is the `delegate.client` extension with a `sessionEvents` transport verb, and a lint rule keeps `@gent/extensions` out of core TUI files (F2); one status row, no label positions (F3); connection widget is host chrome (F5); one pane slot owned by the session view, `shell.pane.open/close/isOpen` (F4); extension cleanups run at shutdown. Review P2 (an old `borderLabels` key dropped labels silently) fixed `0cc53e06`: an unknown contribution key fails the extension by name. Owner: a cell draws each op through its registered tool renderer (`48d4ec44`); a guard test fails on a renderer name that is no tool id; `MODEL_CHANGE_MESSAGE_TYPE` exported (`4b0935f8`). Live: the first cell-op build froze the TUI (expanded `read` ops, nested frame headers hidden); ops now draw collapsed and nested frames keep their header; six children drew under the cell row in the transcript view. Pass 4 candidates: the tree under a cell stays empty in native scrollback (rows commit before children run); cell receipts carry no op ids, so a reload shows receipt lines only |
| Providers       | done `bde01e6e` … `69dc354c`, merged `f1c0e778`: credential refresh has a 15 s timeout (P3); an abandoned OpenAI login is dropped after 5 min (P4); a failed redirect server fails the login (P5); the Anthropic refresh keeps its cause (P6); core is the one retry owner for 429/5xx/transport (P9); one `postOAuthForm` (P10); the OAuth error page escapes its text (P17). Review P2 (every refresh failure stopped retrying) fixed `6374bc9e`: temporary outages stay retryable, a rejected token stops the turn. Owner decisions: ACP removed with the core external-driver seam (`0ff258cf`, `74207789`, −5,700 lines); a stored `External` driver ref loads as no override and warns once; the keychain token is tried first (P15); a revoked sign-in shows only `ChatGPT sign-in expired: … Sign in again with /auth.` (`2fd6d745`, `2b0a304c`; credential failure travels as `AiError` metadata; `causeMessage` reads class-defined messages). Merge with core resolved in the rift (`c886577c`). Open: no end-to-end Anthropic revocation test (needs a stub `claude`)                                                                                    |
| Tooling         | done `34eaa19a` … `4a919ecf`, merged `f6e02dbb`: lock and identity name one build and one database (F2, F3); the in-process RPC client lives in core, so core no longer imports the SDK (F1); the examples and the lint plugin typecheck, one owner for host facts (F4+X5, F5, F7, F8); docs name only live surfaces, one table for retired names, unused deps dropped (F6, F12, F14, F15). Review P1 (a client started a second server while the lock holder lived) fixed `175fd552`: the client fails with `GentConnectionError`. Review P2s fixed `8acbdbe2`: a guard keeps imports inside their declared workspace, a cross-workspace relative-import guard, host-fact checks restored. Merge with main resolved in the rift (`0fdd56ec`); `createE2ELayer` registers configured agents beside extension inputs (`cd6fcf3b`). Open: F11 shared server mode (owner); a stale lock after a crash blocks startup                                                                                                                                                                                                                                                    |
| Follow-up queue | done `e5168067`, `ce0eb780`, merged `118931d2`: follow-ups never merge; each queued follow-up keeps its own id and parts and runs its own turn, so a caller waits for its own content (`mergedMessageIds` gone; an old merged row decodes as one message). Review P2 (a full queue rejected a retry of a queued id) fixed `ca0063a1`: the cap applies only when the queue grows; the restart test covers two waiting items in order                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Cell workers    | done `86807b68`, merged `62a53fd4`: a worker whose host dies exits (a parent watchdog thread, 250 ms), SIGTERM ends a worker whose cell holds the thread (no JavaScript signal handler), and a worker exits when its request pipe closes even with a cell timer open. Review: no findings. Lint on main flagged the adapter globals; marked `f57cad70`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Pass 4 (sweep of main at `6585cafc`)

Reports: `~/.cache/gent-pass4/pass4-{core,extensions,sub-providers,sub-cell,sub-delegate,sub-tools,tui,sdk-tooling}.md`.
Verdict: not polish. Core 5 P1 + 9 P2; extensions 4 P1 + 16 P2; TUI 1 P1 + 2 P2; sdk 1 P1 + 3 P2. No core reduction over 100 lines; the reductions are in the TUI delegate client (about 450 lines) and provider Tags.

### Pass 4 triage

| Batch             | Rift               | Items                                                                                                                                                             |
| ----------------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core loop         | `p4-core-loop`     | B1 recovered turn keeps agent/runSpec/interactive; S1 model notice through the loop; E2 branch resources from the session profile; B2, B3, B4, T1, T2; K1         |
| Core host         | `p4-core-host`     | S2 fork settles the prefix; E1 broken config reported, not fatal; S3, E3, E4                                                                                      |
| Delegate/cell/TUI | `p4-delegate-cell` | TUI D1–D3 child-completion renderer; delegate D1 deleted child frees its slot, D3; cell C1 reload ops, C2 recovery shape, C3–C6; pending-cap flake; session-notes |
| Extension tools   | `p4-tools`         | T1 edit `$` tokens; T2 skill YAML; T3 dangling skill link; T4 push guard; T5 UTF-8; T6 cd expansion; T7 finder leak; T8 walk; T9 verify                           |
| Providers         | `p4-providers`     | V1 second OpenAI sign-in; V2 beta cache by model; V3 no Anthropic store write; V5 builders, not Tags                                                              |
| Server lock       | `p4-lock`          | L1 kernel lock; L2 concurrent start; L3 doctor bound                                                                                                              |

Owner decisions (2026-09-23): D2 a child that arms a wake, monitor or goal sends later results to its parent with `session.send` (tools stay allowed); V4 one credential order for both drivers: stored sign-in, stored key, env key; L4 a different build fails with a message naming the pid, never SIGTERMs an open server; S4 the spawn depth cap counts child spawns only, not handoffs. Each went to its batch.
Main fixes during the sweep: `f57cad70` adapter globals marked; `75bf5a32` stalled-worker fixture writes a raw pid (ANSI color broke the parse outside the gate).

### Pass 4 results

| Batch             | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Server lock       | done `9dd3f535` … `8f6bc59f`, merged `61ae123c`: an OS-held kernel lock (`server.lock.db`, exclusive SQLite lock) decides the owner; a stale or reused pid no longer blocks startup (L1); two concurrent starts give one owner (L2); doctor probes the holder first (L3); another build fails with a message naming the pid, never a signal (L4, owner). Review P1 (a live older-build server without the kernel lock let a second owner start) and P2s (identity body unbounded; stale removal raced a new owner) fixed `b233e7c7`: an entry that answers the identity probe is alive; stale removal holds the kernel lock. `GENT_LOG_LEVEL` accepts only level names (`b67c86c4`, decided by make-impossible-states-unrepresentable; `warning` dropped, no shims). Docs/refactor polish D1–D3, P1x, P3x                                                                                                                                                                                                                                                                                                                  |
| Extension tools   | done `149bea06` … `2c1b8f3a`, merged `e301418f`: `edit` splices text literally, `$` patterns stay (T1); an empty `oldString` is rejected (it looped and used 24 GB); skill frontmatter is YAML (T2); an unreadable skill entry is skipped (T3); force push is destructive wherever the flag sits (T4); streamed UTF-8 (T5); no `cd` split on shell expansion (T6); one native finder per root, at most 4 (T7); the fallback walk skips `.git`, stops on link cycles, and has a bound (T8); compaction asks 768 tokens and marks a cut summary (T9); T10–T21 polish, `WakeEntryType` kept (a TUI reader). Review P1 (git global options, env prefixes, quotes and nested shells hid a force push) and P2s (eviction destroyed an in-use finder; decoded answers not aligned) fixed `b8559c26`: a shell-word parser classifies every git invocation; finders are reference-counted. Pass 5: summary request `reasoning: "none"` (core), TUI `run-effect-boundary.ts`, push deletions (`--delete`, `:ref`, `--mirror`)                                                                                                        |
| Providers         | done `123198da` … `3e1b81bb`, merged `6afe3698`: a new OpenAI sign-in replaces the held credential (V1); the Anthropic beta cache is keyed by model, `AnthropicBetaCache` Tag gone (V2); an Anthropic refresh no longer writes the unread gent auth store (V3); one credential order for both drivers, stored sign-in first (V4, owner); credential Tags replaced by one builder per provider (V5); V6–V10 polish. Review P2 (another workspace profile could refresh the old account over a new sign-in) fixed `37ec70d6`: `ProviderAuthInfo.update` reads and writes under a per-provider store lock (`serializeAuthStore`); a refresh adopts a changed stored credential; a removed sign-in is not restored. Open: another profile can serve the old access token for up to 30 s. Merge with tools resolved in the rift (`69dce01f`)                                                                                                                                                                                                                                                                                    |
| Delegate/cell/TUI | done `8b4cc463` … `ffa67299`, merged `a944ad1d`: a child's completion row draws its calls in native scrollback from additive completion details; the live tracker, `LiveChildTree` and the `sessionEvents` transport verb are gone (TUI D1–D3); a deleted child frees its cap slot (D1); the start row is written before its send, `runSpec` stored (D3); a child sends later results to its parent with `session.send` (D2, owner); a reloaded cell draws its operations from stored tool events (C1); recovery writes decodable receipts (C2, C3); headless prints each op once, one receipt schema (C4, C6); pending-cap flake gated; session notes keyed by session. Live gamut: the completion row and its calls in scrollback. Review P2s fixed `010e57d1`, `64480673`: projected ops keyed by message and call id, bounded to their row (no output); an old completion row reads its status from the envelope, never a false success mark. Merge with main resolved in the rift (`18ba103d`). Decided for pass 5 (make-impossible-states-unrepresentable): a child session keeps its agent on every turn            |
| Core host         | done `458d19fd` … `985ab213`, merged `f5a440ef`: a fork at a tool-call message copies only settled steps (S2); a broken config file is an extension-health failure, not a startup crash, and a user config that broke after startup is never overwritten (E1); an unreadable extension entry fails alone (E4); `auth.listProviders` resolves the session's registry and model, `AuthGuard` Tag gone (S3); only child spawns count toward the depth cap (S4, owner); unread host facts dropped (E8). Review P1s (approval replay could wait forever; a failed cell approval dropped the open native request) and P2s (identical questions took each other's answers; a failed project reload kept stale settings) fixed `bc47f22e`, `11acbe34`: E3 redesigned — an interaction's owner is (tool call id, occurrence); one open request per branch, owners queue in order, an answer its call does not take is settled as abandoned at call end; migration `021_interaction_owner` adds two nullable columns (additive). Open: an unanswered request whose owner is gone stays open; P3 E5, E6, E7, S5–S7, S10, R3 to pass 5 |
| Core loop         | done `70305d49` … `34a607f5`, merged `a17e5e34`: a recovered turn keeps its agent, run spec and interactive flag (B1, migration `022_turn_record_admission`, one nullable column); the loop writes the model-change notice at a step boundary, so `/model` during a tool no longer breaks the branch (S1); the last budgeted step reads its step-limit instruction (X1, new) and refuses its tool calls (T4); branch resources are built lazily from the session's profile (E2); B2, B3, B4 (`joinedTurn` metadata), T1, T2, T3, T5, T6; `keyed` removed (K1); dead wake drain, unreached recovery branch and redundant storage deletes removed. Review P1 (a switch during a stream lost its notice) and P2s (replay kept a stale notice; a removed agent left a recovered turn unanswered) fixed `7480420c`: the notice compares the last settled model with the step's model (derive-dont-sync), its id names both models, a removed agent settles the turn with an error naming it. Merge with main resolved in the rift (`5fec3211`). Open: B5 (two-permit race), S7 (index drop needs a migration), TT2              |

## Pass 5 (sweep of main at `c77c19cb`)

Reports: `~/.cache/gent-pass5/pass5-{core,extensions,tui-sdk,live}.md` and the `pass5-sub-*` reports. Verdict: not polish. Core 8 P1 + 9 P2; extensions 3 P1 + 10 P2; TUI/sdk 2 P2; live check 1 P1. Regressions from pass 4: grep in a gitignored directory (X1), op rows after a reload lost their output (R1), compaction anchors on the model-change notice (L1), and an env key no longer counts as a credential (V11).

### Pass 5 triage

| Batch         | Rift             | Items                                                                                                                                                                                                                                                                            |
| ------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Extensions    | `p5-ext`         | X1 grep ignored dir; X2 discard-changes guard; X3 redirections; X6 blank oldString; D7/D8 fork answer; D9; G1 goal budget; V12 refresh-token race; D10 dead `DelegateRpc.Children`                                                                                               |
| Interactions  | `p5-interaction` | I1 answer during a sibling call is lost; I2 a request ends with its turn; I3 a call never waits on itself; I4 cancel racing an answer; I5; I6                                                                                                                                    |
| Session agent | `p5-loop`        | L2 the agent is a session property (owner decision, migration 023); L1 compaction anchor; L3 repeated notice; L4/X7 summary `reasoning: "none"` in core and the OpenAI driver; L5 one actor test root; H5 durations keyed by message and call                                    |
| Host          | `p5-host`        | H1 config writes read fresh, fail on decode, write atomically; H2 a failing driver catalog is skipped; H3 tool/request id collision; H4 cross-process auth lock; H6 session update in one transaction; V11 env credential in the provider list; C8 tool `summary(input, output)` |
| TUI           | `p5-tui`         | live L1 status line and tray below the last row; R1 bounded op output in the snapshot; G1 headless test in the gate; G2 TUI run-effect-boundary; G3 aliased import guard; A1 tray activity; live P3s (config reason, doctor log path, column width)                              |

### Pass 5 results

| Batch         | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Interactions  | done `7cd93f3a`, `57915ceb`, merged `1d5e706e`: an answer that lands while a sibling call runs resumes the parked turn (I1); `InteractionService.endTurn` settles, dismisses and clears a turn's requests at every non-shutdown turn end (I2); a call keeps the answers it took and never waits on itself (I3); cancel sets the latch while Waiting and `respondInteraction` no longer begins a turn, so a cancel racing an answer holds and the Cancel RPC returns at once (I4); a changed question drops the old request (I5); a native ask with no owner is refused (I6, decided by make-impossible-states-unrepresentable). Review P1 (a restart lost the answers kept for a two-question call) and P2 (a cell receipt took an answer to a changed question) fixed `a84cd908`: a taken answer is a stored `taken` status (additive value, no migration) until its call or turn ends, and rehydrate restores it; `presentOwned` matches the question too; `InteractionResolved.dismissed` (additive optional) tells a dismissal from a decline (decided by make-impossible-states-unrepresentable). `InteractionStorage.listPending` renamed `listOpen`                                                                                                                                                                                              |
| Extensions    | done `55178b7b` … `e717070a`, merged `17b35aa4f`: discard-changes git commands ask (X2); a redirection ends a shell word, and its target is not an argument (X3); a push that can delete remote refs asks (X4); X5, X8, X9, X10; grep lists a gitignored target from its own root (X1); a blank fuzzy edit match finds nothing (X6); a child's completion reads only the child's own turn (D7/D8); D9 status line; every turn end is charged to the goal budget (G1); a refused refresh adopts another process's rotation (V12); an empty auth store means signed out (V14); `DelegateRpc.Children` and its read model deleted (D10); D5, D12. Review P1s (`git checkout HEAD <path>` / `--theirs` passed; `git restore --staged` was made safe) and P2s (a recovered completion read a later turn; a failed stream paused a spent goal; one trailer stopped the chain) fixed `b8513904` … `62e12706`: a checkout with two positionals or a conflict-side flag asks; every restore asks; the completion slice ends at the next user message or completed turn; a spent budget wins over a pause; trailers per commit. D11 (`to: "parent"` lands on the owning branch, `9135ec2b`) and X13 (the fallback index reads `.gitignore` on every listing, `11c165a0`) closed. C7 rejected: the delegate reads receipts leniently on purpose                    |
| Host          | done `49c03628` … `2ce691f9`, merged `9e9413f1d`: config writes decide on the file as it is now, atomically, and surface `ConfigWriteError` (H1); a failing driver catalog is skipped and reported as `ModelCatalogFailed` health (H2); an env key is listed as source `env` (V11); tools and requests share one id namespace (H3); auth-store writes are ordered across processes by an OS-released SQLite lock under `<authDir>/.locks` (H4); session rename, settings and active branch are column-scoped writes read inside one transaction (H6); a tool may summarize its own result for events and cell receipts, shipped for read, write, edit, grep and bash (C8); health names each failure's reason (live L2). Start `label` rejected: it would change the persisted message-part format. Review P2s (a duplicate id inside one extension; an auth-store read failure became UnknownModel) and P3s (health ran every catalog; recovered receipts lost the summary; a second atomic write) fixed `90cbf163` … `f2c818d3`: one extension naming a key twice fails; the auth read fails the catalog as an auth error; health reads `ModelCatalogRecord`, the last run per profile (derive-dont-sync); recovery resolves the recorded binding; `writeFileAtomic` lives in core, used by config, fs-tools, branch state and the TUI frecency store |
| Session agent | done `ca3f64c9` … `6b166608`, merged `424c83836`: the agent is a session property (L2, owner decision; decided by make-impossible-states-unrepresentable). `SessionAdmission` is stored on the session by migration `023_session_admission` (one nullable column, backfilled); one resolver serves the turn, the snapshot, the footer and auth (R3); per-turn admission removed from queue items, steer, turn records, `message.send` and `Session.send` (personal library, no shims); headless `--agent`/`--run-spec` shape a new session only. `/model` wins over the run spec (L10). Compaction anchors on the user's prompt, never a runtime line (L1); a model switch is announced once (L3); the summary request asks for no reasoning (L4/X7); durations keyed by message and call (H5); one actor test root, −347 lines (L5). Review P2s (the migration could give a queued turn an old agent; `gpt-6-astra` rejected `none`) fixed `b9403b18`, `52464e32`: the in-flight turn's admission wins, then steering, follow-ups, then the newest record; one effort-floor table per model. `selectAgent` deleted (subtract-before-you-add). Open: `turn_records.admission_json` is unused but kept (a drop breaks older binaries); no live OpenAI check of the effort table                                                                          |
| TUI           | done `ea49ccb5` … `b35c03fa`, merged `2ca9b6526`: the live tail is sized to OpenTUI's split-footer region, so the status line and tray stay on screen in a long session (live L1, P1; live check in gamut); a reloaded cell op keeps the input and output its row draws (R1); headless runner tests run in the gate (G1); scoped Effect tests replace the TUI run-effect boundary (G2); the guard credits aliased imports and namespace destructuring (G3); the tray names what a running child is doing (A1); a GENT_DATA_DIR run keeps its logs there, SDK `LOG_DIR` removed without shims (L3); server status columns fit their values (L4); a reloaded op draws the body the live feed drew (L5). Review P2s (no total op bound; a cut split a code point and lied about line counts; activity followers replayed every loop's history) fixed `3907cfdb`, `2a2f5d2f`: one 8 KB encoded budget per op; cuts at code points with `cuts[{lines, tailLine}]` (additive wire field, decided by make-impossible-states-unrepresentable); only running children are followed, from `Session.events({ from: "now" })`. Server status prints one field per line when wider than the terminal (`992d1f8c`, experience-first). Child op summaries use the tool's own summary after the host merge                                                              |

## Pass 6 (sweep of main at `9e9b3dd2`)

Reports: `~/.cache/gent-pass6/pass6-{core,extensions,tui-sdk,live}.md`, the `pass6-sub-*` reports and 28 captures. Verdict: not polish. Core 0 P1 + 8 P2; extensions 1 P1 + 11 P2; TUI/sdk 0 P1 + 3 P2; live 4 P2 (live A raised to P1: every approval inside a cell ends the cell with the crash text). Pass-5 regressions: a child's completion stops at a runtime line and drops its answer (N1); two asks in one step lose the first answer (core I1); a failing `sessionAgent` read stops the worker silently (S1); a reloaded grep or large edit op loses its body (O1); a query filter blanks tray activity (A1/N2); an unknown agent name breaks a session for good (S2/A2).

### Pass 6 triage

| Batch           | Rift             | Items                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Interactions    | `p6-interaction` | live A (P1) an approved or declined cell op ends the cell as lost worker state; live B a sibling op in one cell is interrupted when another op asks; core I1 two asks in one step drop the first answer; core I2–I4; CL2 cell receipts sorted as text                                                                                                                                                                                                                                                                         |
| Session agent   | `p6-agent`       | N1 (P1) completion slice stops at runtime lines, drop its event replay; S2/A2 `session.create` rejects an unknown agent; S1 `sessionAgent` read errors handled and logged; live C children resend their start result (`childTaskText` wording); E1 `/btw` runs the session's agent and model; E2 `prompt`/`handoff` are interactive; dead agent plumbing (`switchSession` agent, `requestedAgent`, empty `admission: {}`, `RunSpec.parentToolCallId`, `--run-spec`); raw JSON for `wake`/`session.send` in the completion row |
| Providers/files | `p6-providers`   | M1 a credential read races a write (atomic auth writes); M2 the auth lock wait is interruptible; M3 `/driver` writes keep unknown keys; O2 `writeFileAtomic` keeps the file mode; V17 effort ceilings; V18 Haiku effort; V19 empty env var; V20 redundant refresh guard; V21 `ProviderAuthInfo` tagged union; live D pinned `ccVersion` blocks Opus 5.5; config failure names its file and clears after a fix                                                                                                                 |
| Rendering       | `p6-render`      | O1 reloaded grep/edit ops keep their body, summary drawn; CL1 a cut inside one long line; stale `cuts` after a live result; A1/N2 tray follows unfiltered rows, stable row order; declined op text; failed count after reload; doc drift (`docs/extensions.md`, log dir)                                                                                                                                                                                                                                                      |
| Tools           | `p6-tools`       | T1 trailer inside quoted messages and heredocs; T2 abbreviated long options; tools P3s                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Tests           | `p6-tests`       | T1 (TUI) one Effect render-wait helper (about −350 lines); `makeClientTraceLogger` test copy; test-only names in `protocol` and `test-utils`; test files that mirror no source file; `narrowR` casts                                                                                                                                                                                                                                                                                                                          |

### Pass 6 results

| Batch           | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tests           | done `8ee7d9ff` … `afecd37b`, merged `a81a33049`: one Effect render wait (`waitForFrame`/`waitUntil`, deadline inside the poll) replaces seven copies at 174 sites; the product client trace logger is the tested one; 20 test-only names moved from `protocol` to `test-utils`; `tsconfig.locks.json` and its guard deleted (the main typecheck covers tests); files that mirror no source folded into their owners; `narrowR` deleted. Review (no P1/P2) fixed `124d0c5d`: guard fixture names a real module; `SessionRuntime.Live`'s type names the actor client it provides, so the last test cast is gone; banners name their subject. Net −687 lines. Open P3: about 40 more file-name banners in sdk, TUI and examples tests                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Session agent   | done `efff1884` … `4f462975`, merged `4d50793fd`: a child's completion reads past the loop's own lines and keeps its answer, the event replay deleted (N1, P1); the child's task says its start-turn reply is the result, so children no longer resend it (live C, checked live); `session.create` rejects an unknown agent (S2/A2); a failed agent read fails the turn, not the worker (S1); `/btw` forks on the session's agent, model and reasoning (E1, additive optional create fields); `prompt` and `handoff` are interactive (E2); `--run-spec`, `requestedAgent`, `RunSpec.parentToolCallId`, `DelegateEntry.runSpec` removed, no empty admission stored; `wake` and `session.send` summarize their results. Review P2 (a handoff could inherit an agent its new project lacks) and P3 (a retried create failed after its agent went) fixed `260a2b9a`: the effective agent is checked against the target cwd, in a `once` admit step that runs only on a fresh create, outside the transaction. Headless startup errors print on stderr (`641c64c7`, experience-first)                                                                                                  |
| Tools           | done `83c4c622` … `6bba4a19`, merged `291a4f3c6`: bash commands are read as shell words with source offsets; the trailer goes after the `commit` word as `--trailer=Session-Id:<id>`, never inside quotes or heredocs (T1); any prefix of a destructive long option counts (T2, subtract-before-you-add); `&>`, `>                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `, ` | &`, here-strings (T3); nested text is classified only where a shell runs it (T4); more destructive forms (T5); one line-count rule and `BashResult.status: blocked \| background`(T6); the fallback walk applies every`.gitignore` with negation (T7, T8); a normalized edit matches inside a line (T9). Review P1 (`xargs … sh -c '{}'`and a`!`git alias hid a reset) and P2/P3 (the trailer broke an escaped nested script; a match dropped searched spaces) fixed`b9cbb749`: wrappers hide nothing, a script the guard cannot read asks (fail safe, make-impossible-states-unrepresentable), git alias values are classified, the trailer goes only where the insertion stays in one word. Now asks by design: `eval "$(…)"`, a shell fed from a command, checked git subcommands under xargs fed from a command. T10 open (needs `extensions/api` and the TUI utils) |
| Providers/files | done `a5894116` … `3bb57fe1`, merged `7db69d420`: `writeFileAtomic` keeps the file mode (O2); auth writes are atomic 0600 files, one per provider at the same path and codec as the old KeyValueStore (no format change, counsel-verified), and a torn read is discarded only under the lock (M1); the auth lock wait is interruptible (M2); a driver config write keeps unknown keys (M3); OpenAI efforts have a floor and a ceiling per model (V17); Anthropic leaves effort out for haiku on both paths (V18); the refused-refresh adoption deleted, the H4 lock serializes read-refresh-write (V20); `ProviderAuthInfo` is a tagged union (V21); Claude Code 2.1.280 advertised, so Opus 5.5 answers (live D, one paid turn $0.055); config health names its file and clears once fixed. V19 rejected: effect rc.112 already treats "" as missing. Review P2s fixed `5ae86125`, `402b15e9`: override entries merge per entry and keep unknown keys; `writeFileAtomic` follows a symlink and keeps it (use-the-platform; the pass-5 tests that asserted link replacement changed)                                                                                              |
| Rendering       | done `b7b26105` … `793a8a8d`, merged `fd1eee26b`: `OutputCut` is a tagged union `Text` \| `Items` (wire only); a reloaded grep keeps head and tail matches and an edit keeps its diff input, with a summary line when a body cannot fit (O1; live and reload rows identical in gamut); a cut inside one line records the characters left out (CL1); a live result drops stale cuts; a live cell failure settles its running ops as a reload does, one failed count; the tray follows the working children of unfiltered rows, in start order (A1/N2, additive `createdAt`); hint width by display width; docs match `Session.send` and the log directory. Review P2s (an op could exceed 8 KB through its id and tool name; a grown input could erase a small body) and P3 (a follower missed a child's next turn) fixed `ef79804e`: fixed fields are counted and clipped, the output plan reserves its frame and cut records before input grows, one follower per live loop across turns. "declined" and "in background" draw from `BashResult.status` (make-impossible-states-unrepresentable); the grep header shows the exact file count; singular row noun                   |
| Interactions    | done `f9cc9a42` … `dfcff688`, merged `53ba17746`: an approval asked inside a cell waits for its answer in place, so an approved op continues the cell and a declined result reaches the cell code (live A, P1; checked live); a sibling op runs on while the dialog is open (live B); the turn stays Running while a cell waits (redesign-from-first-principles: the cell source cannot replay); `InteractionOwnership.take`; the live suspension path removed from the kernel; two asks in one step keep the first answer (I1); I2–I4; receipts sort by call number (CL2). Review P2 (a later reply could reverse a decision) fixed `b465e047`: the first answer wins in one conditional UPDATE, a differing reply gets `InteractionDecisionConflictError` (make-operations-idempotent). Cancel and restart proven end to end; a closing loop hung on any running cell host call (present on the base too), fixed `8d25d884`: `BranchToolWork.stop`, a close counts as a crash and reuses recovery. ARCHITECTURE interaction flow updated. Pass 7: the cell subtitle lists source verbs while the header counts ops that ran (apps/tui/src/tool-renderers.tsx:498, utils.ts:620) |

## Pass 7 (sweep of main at `3c31ab03`)

Reports: `~/.cache/gent-pass7/pass7-{core,extensions,tui-sdk,live}.md`, `pass7-sub-*` and 38 captures. Verdict: not polish. Core 0 P1 + 1 P2 (only polish otherwise, no pass-6 regression); extensions 1 P1 + 8 P2; TUI/sdk 0 P1 + 2 P2; live 0 P1 + 2 P2, all six live checks pass. Pass-6 regressions: a shell name as an argument asks (X2); the trailer lands in a stored git alias (X3); a finished child's follower never stops (A1); row headers count a trailing newline as a line (R1).

### Pass 7 triage

| Batch        | Rift             | Items                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Interactions | `p7-interaction` | I1 (P1) an approval in a non-interactive child is declined at once and the child is told to ask its parent (children wake, never block); K1 the owned ask is read inside the service, `OwnedAsk` deleted; K2 a reply after a dismissal; K3 a clear error when a sibling native call holds the slot; K6 one name for the approval service; cell `run` checks `stopping`; `CellToolCallSuspended` pass-through; lost-cell and cancelled-cell texts say what was recorded; a retried reply matches its contract; `childTaskText` covers a turn woken by the parent's answer                                                                |
| Guard        | `p7-guard`       | X1 ANSI-C escaped quote; X2 a shell word counts only as a command or after a wrapper; X3 no trailer in a stored alias or an `echo`; `git $(…)`, `bash <(…)`, `fish -c`; the raw-text regex tier folded into the parser; live noise: `git add .`/`-A` safe, `find -delete` asks like `rm`, `git checkout -` safe, `git stash` push/pop/apply ask (live P2-B: children share one worktree), sensitive-name rule reviewed                                                                                                                                                                                                                  |
| Providers/fs | `p7-providers`   | live P2-A Anthropic prompt caching is never on; V1 effort on claude-sonnet-4-5; V2 anchored `-pro` rows; F1 the fallback walk matches git's `.gitignore` rules; F2 ignore rules above the root, `.git/info/exclude`, global excludes; K4 the lock does not scope the caller's effect; K7 no temp dir left in a project after a crash; K8, K9; a config `agents` change reaches a new child without a restart; models.dev flags; test-only exports                                                                                                                                                                                       |
| Rendering    | `p7-render`      | R1 one line-count rule in row headers, no count on declined or background rows; R2 an expanded cut grep shows its gap and total; R3–R6; A1 a finished child's follower stops; C1 a handoff shows its inherited agent; M1 `--agent` without `-H`; M2 the stderr test in the gate; one error line for a CLI startup failure; G1, G2; K5 a dropped field keeps a cut record; the cell subtitle counts ops that ran; a waiting cell draws its running sibling op; child rows show distinct ids; agents-pane count and order match the tray; monitor and multi-line summaries; `gamut status` strips colour; about 40 file-name test banners |

### Pass 7 results

| Batch        | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard        | done `e4a2c842` … `aa8f9b34`, merged `69db9182`: ANSI-C quotes decoded with source offsets (X1); only a word in command position is a command, with a `WRAPPERS` table and `eval`/`ssh`/`watch`/`env -S` read as joined scripts (X2); a stored alias is a derived word, never given a trailer (X3); `git $(…)`, process substitution, `fish` and a computed command word are unreadable runs and ask (decided by make-impossible-states-unrepresentable; `$EDITOR` noise accepted); the regex tier folded into per-command `COMMAND_RISKS`; `git add .`/`-A` and `checkout -` safe; `git stash` moves ask (decided by serialize-shared-state-mutations); `find -delete` and `git rm -f` ask; sensitive names are key, env and credential files only. Counsel: `env -S` and busybox P1s, computed command word P2, fixed in `aa8f9b34`. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Interactions | done `e303fed5` … `9afd0ef2`, merged `4041a566`: a non-interactive session declines an approval at once, and the bash and monitor blocks tell the child to ask its parent with `session.send` (I1, decided by children-wake-never-block); the service reads its owner itself, `OwnedAsk` and `ApprovalServiceApi` deleted (K1, K6; decided by no-context-params); one reply check, scoped to its session branch, refuses a reply to a closed request or from another branch (K2); a retried reply succeeds and a changed one conflicts (decided by redesign-from-first-principles: the socket client retries); `InteractionSlotBusyError` (K3); a cell run checks `stopping` before it claims; `CellToolCallSuspended` deleted; lost and cancelled cell texts follow the recorded ops. Counsel: cross-branch retry P2, fixed in `9afd0ef2`. Probes red for each fix. The TUI "1 failed" cell header went to the rendering batch                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Providers/fs | done `cbc5b4f9` … `ac707876`, merged `27547b0f`: Anthropic prompt-cache breakpoints on both auth paths (live: turn 2 read 13,462 cached tokens) and cache prices in `calculateCost` (additive optional `ModelPricing` fields); one Anthropic effort table (V1); anchored GPT-5 Pro row (V2); inside a work tree the fallback walk asks git (`ls-files --exclude-standard`, `check-ignore --no-index`), with a git-glob matcher tested against real git elsewhere (F1, F2; decided by use-the-platform); `writeFileAtomic` stages a sibling file (K7); the auth lock holds a private scope (K4, refactor: the type already forbids the bug); one `resolveRegistryForCwd` (K8); config reads follow file edits, reusing a decoded file while its stat stamp holds (decided by derive-dont-sync); `runProcess` decodes UTF-8 as one stream; four test-only exports demoted. Counsel: ignored-target walk P2, split multibyte P2, config reread P3, fixed in `ac707876`. Probes red for each fix. Carried to pass 8: `disabledExtensions` edits wait for a restart; a new delegate later-turns flake (5 steps where 4 are expected); `unescapeStr`/`findMatch` exported for tests; eleven commits used `--no-verify`, the merged branch gate passed                                                                                                                                                          |
| Rendering    | done `0bb60130` … `fc2afecb`, merged `50820003`: one line-count rule, and a cut bash row counts the whole output as its body does (R1); cut grep draws gap rows and totals (R2); `headCut`/`tailCut` wire-only fields (R3); one truncation marker, one `countNoun`, SummaryLine fallback (R4–R6); one watcher per listed child lives across turns, so the loop's own turn start fills the activity line (A1, decided by derive-dont-sync); `planOutput` empties an uncuttable field with a record (K5); `clipSummary` keeps a summary's first line; CLI startup failures print one line on stderr from the gated `ops.ts` (M2, live P3 6); `--agent` without `-H` is refused (M1, decided by subtract-before-you-add); a created session takes its agent from its snapshot (C1); a cell failure reads apart from op failures; the status line shows every running op; session ids show their random tail; pane counts and order match the tray; monitor summary; `gamut status` strips colour; `dataPathsIn` demoted (G2); 95 banners renamed in sdk, TUI and examples. Counsel: follower restart P2 and cut bash count P2, fixed in `fc2afecb`. Probes red for each fix; live gamut check passed. Carried to pass 8: about 180 banners in core, extensions and tooling tests; the TUI mirrors the fs-tools line rule until T10; a `delegate.start` limit error prints a `$bunfs` stack in the cell body |

## Pass 8 (sweep of main at `2ef4afee`)

Verdict: not polish. Reports: `~/.cache/gent-pass8/pass8-core.md`, `pass8-extensions.md`, `pass8-tui-sdk.md`, `pass8-live.md`. Three P1s (G1, G2, FS1), about 25 P2s. Live check: five of six pass; the open agents pane never refreshes.

Decisions:

- N2/C7 (tagged owner call): whether a turn can ask a user comes from the turn's origin, not from a stored session flag. A turn a client prompt starts can ask; a turn started by `delegate.start`, a parent `session.send`, a wake or a monitor declines at once. `/handoff` and `/btw` no longer inherit the flag. Decided by derive-dont-sync.
- V1-V3: verify the thinking and effort facts against the Anthropic docs before any change. Decided by prove-it-works.

### Pass 8 triage

| Batch                 | Rift           | Items                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard                 | `p8-guard`     | G1 (P1) echo/printf text piped into a shell; G2 (P1) `bash -s` with arguments reads stdin; R1 one `readOption` with `ValueOptions`, which fixes G3 option clusters and G5 `--rcfile`; G4 `coproc`, `function`, `setsid`, `chronic`, `su -c`, `pnpm exec`; G6 git config/env shell text (core.pager, core.sshCommand, `GIT_SSH_COMMAND`, `rebase -x`, `submodule foreach`, `bisect run`); G7 a redirect to a sensitive file; G8 `case`/`${…}` inside `"$(…)"`; G9-G14 |
| fs                    | `p8-fs`        | FS1 (P1) inside a work tree git's listing is primary for the native index; FS2 cwd inside an ignored directory; FS3 untracked nested repos and submodules; FS4 binary files and uncapped lines in grep; FS5-FS11; `unescapeStr`/`findMatch` test-only exports                                                                                                                                                                                                        |
| Interactions and core | `p8-core`      | C1 (pass-7 regression) `planOutput` share formula; C2 a stored reply whose wake failed is re-driven on retry; N2/C7 turn-origin interactivity (decided above); N1 and live P3-1 a declined child in its first turn ends its turn with the question, one parent wake; C3 `pendingRequestId`; C6 redundant scan; cell texts C1-C4, W1, N3; a `delegate.start` limit error prints a `$bunfs` stack; the delegate later-turns flake                                      |
| Providers             | `p8-providers` | live P2-B the models catalog file carries a format stamp and refetches on change; V1-V3 thinking, `none` and `max` through one payload transform, after the docs check; C4 `writeFileAtomic` staging cleanup and long names; C5 `readFresh` ordering; V4-V8; `disabledExtensions` edits wait for a restart                                                                                                                                                           |
| Rendering             | `p8-render`    | live P2-A and U1 the open agents pane refreshes on the tray's triggers and counts by own state in title, heading and tray; U2 one stderr line for every startup failure; U3 `shortId` for branches; U4-U9; agents-view A1/A2; live P3-2 `ctx N%` counts system prompt and tools; live P3-3 blank lines between blocks; about 180 file-name banners in core, extensions and tooling tests                                                                             |

### Pass 8 results

| Batch                 | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Providers             | done `2181a1b8` … `4a30a6dc`, merged `8c78eee8`: the models.dev cache carries a schema-derived format stamp and an older file refetches (live: step 2 cost $0.0040 with cache prices, was about $0.028); one Anthropic request plan for both auth paths: adaptive thinking, `disabled` for `none` where accepted, `max` effort, no `temperature` where it is rejected, each checked against the platform.claude.com thinking and effort pages (V1-V3, V6-V8, decided by prove-it-works); one `effortAtOrAbove`; `Model.reasoning` from the catalog reaches the driver as `ProviderHints.supportsReasoning` and the OpenAI name regex is deleted (V5, decided by redesign-from-first-principles); `writeFileAtomic` removes only its own staging file and fits NAME_MAX (C4); a changed user config is decoded under the writers' lock (C5); session profiles keyed by active extension ids, leased by turns, loops, requests and queries, and a superseded profile retires with its last lease (decided by derive-dont-sync). Counsel: profile accumulation P2, fixed in `4a30a6dc`. Probes red for each fix. Carried to pass 9: the TUI half of `disabledExtensions`; branch resources keep their first profile; extended thinking budgets for 4.5 models                                 |
| Guard                 | done `105cc88a` … `8140884d`, merged `f4c6f95f`: one `readOption`/`parseArguments` with `ValueOptions` replaces five scanners (R1, G3, G5); a shell reads a stdin script only from an immediate echo, printf, heredoc or here-string, and every other producer asks (G1, G2 and the counsel P1s `\| cat \| sh`, `\| (sh)`, `printf -- -v`; decided by make-impossible-states-unrepresentable); explicit wrappers and a `SUBCOMMAND_RUNNERS` table, the inferred-command fallback deleted after it flagged ordinary arguments (decided by subtract-before-you-add); git shell text from config keys, env vars, `rebase -x`, `submodule foreach`, `bisect run` (G6); redirect writes and `sed -i` checked for sensitive files (G7); `case` and `${…}` parser state (G8); G9-G14; publish and push matched only as the subcommand; background jobs carry the server process generation, so a new profile no longer interrupts live jobs (extension-owned column added by the extension's own table setup, not a core migration, decided by boundary-discipline). exec-tools.ts +760 net over the pass. Counsel: three stdin P1s, fallback noise P2, action position P2, fixed in `8140884d`. Probes red for each fix. Carried to pass 9: jobs of a retired profile read running until restart |
| fs                    | done `7b3e2638` … `89b688a9`, merged `81b2b8d5`: inside a work tree git's `ls-files` decides every listing, outside one the `.gitignore` walk does (FS1 P1, FS2, FS3, decided by use-the-platform); the fff native finder is deleted from the extensions package, because it only ordered results after FS1 (decided by subtract-before-you-add; the TUI file popup keeps fff); grep skips binary files and cuts lines at 500 chars (FS4, decided by guard-the-context-window); byte-wise gitignore matcher agrees with git on 53 of 53 cases (FS5); no listing follows a symbolic link (FS6); git listings drop hook `GIT_*` env, bound 100k materialized files, time out at 10 s (FS7, FS8); a skip-worktree entry does not count to the bound; a non-UTF-8 name is counted in the optional `GrepResult.unreadable`; whole-number params (FS10); five test-only exports removed. Counsel: sparse index P2, fff cost P2, lost names P3, fixed in `89b688a9`. Probes red for each fix. Net −379 lines in the fixup. Open: T10 line rule waits for the render merge; grep's result type rendered as `object` in the cell (given to the core batch)                                                                                                                                          |
| Rendering             | done `9731bf90` … `b10c5caf`, merged `cbfedaf0`: the agents controller owns every refresh trigger and re-reads the open pane and its selected detail, with one read in flight (`coalescedRead`) (live P2-A, U1); a row's section is its own loop state, so title, headings, tray and glyphs share one rule and `propagateRunning` is deleted (decided by make-impossible-states-unrepresentable); every startup failure prints one stderr line (U2); one `shortId` for sessions and branches (U3); one bash result decoder (U4); `subtreeCounts` deleted (U5); typed detail status (U6); core `splitLines`/`lineCount` is the one line rule for the cut record, TUI, fs-tools and exec-tools (U7, U9, T10 closed); main scoped once (U8); rows indent only under a drawn parent and follow reads no second listing (A1, A2); the ctx gauge uses the provider count of its own step (live P3-2); one blank line between blocks (live P3-3); 196 test banners renamed or deleted, plus guards.ts. Counsel: slow-read pile-up P2, section counts P2, gauge after a model switch P2, fixed in `5e22302e`. Probes red for each fix; live gamut check with the pane open passed. Carried to pass 9: a phantom blank row at the top of the visible screen (split-footer)                          |
| Interactions and core | done `c445414b` … `564b94e7`, merged `07949328`: `planOutput` share covers its own reserve (C1, pass-7 regression); a retried reply re-drives the wake and event while its answer is untaken (C2, decided by make-operations-idempotent); `pendingRequestId` and the cross-branch scan deleted (C3, C6); turn origin decides approvals, revised after counsel: a turn can ask unless its session has a parent and no client opened it, the server stamps `MessageMetadata.fromClient` (additive optional) at `message.send`, the initial prompt and `steer.command`, removes client `extensionId`, and an extension send removes `fromClient`; `admission.interactive` removed, old rows decode, an old unstamped child turn declines (N2/C7, decided by derive-dont-sync and make-impossible-states-unrepresentable); a declined child ends its first turn with the question, one parent wake (N1, live P3-1); cell texts C1-C4, W1, N3; a failed host op reaches the model as its message, no `$bunfs` stack; the delegate later-turns flake was a wake re-arm race, now under the branch file lock; one `SIGNATURE_TYPE_LIMIT` renders every shipped tool's result whole. Counsel: old child row P1 and top-level wake turn P2, fixed in `564b94e7`. Probes red for each fix            |

## Pass 9 (sweep of main at `dbab0054`)

Verdict: not polish. Reports: `~/.cache/gent-pass9/pass9-core.md`, `pass9-extensions.md`, `pass9-tui-sdk.md`, `pass9-live.md`. P1s: O1 (a handoff session counts as a child, pass-8 regression) and `@file` expansion rewriting `$&`-style text. Live check aborted.

Incident: during the first pass-9 run, a sweep agent wrote guard test cases through a shell heredoc whose body held its own terminator; the shell ran `rm -rf ~` for about 80 s. `~/.config`, `~/Developer/personal/dotfiles` and `~/Music` were deleted; no backup was configured. Every brief now carries a SAFETY section: destructive probe text only as string literals in a Write-created .ts file, harmless targets, never executed. Live gamut checks are off until herdr is restored.

Decisions:

- Headless approvals decline by default, as a session with no user does; approving every ask needs an explicit flag. Decided by make-impossible-states-unrepresentable (the incident shows what an unattended approve-all can do).
- The TUI `@` popup reads the fs-tools git listing through a read request and fff is deleted from the repo. Decided by one-source-of-truth and subtract-before-you-add.
- Guard: one `CommandSpec` table first (EX7); a tree-sitter-bash parser (EX8) waits for pass 10 as a proof branch that must net at least −400 lines with the suite unchanged. Decided by subtract-before-you-add.
- AN-1 is verified with one short paid headless run (`gent -H`), not gamut.

### Pass 9 triage

| Batch      | Rift        | Items                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core       | `p9-core`   | O1 (P1) a child is a spawned session, one predicate with spawn depth; O2 and MX-1 a send made for a client-opened request keeps the client origin on its own session and branch; O3; PC1-PC4; jobs of a retired profile; K1-K5                                                                                                                                                                        |
| Guard      | `p9-guard`  | EX1 `-oc` cluster value; EX2 `/dev/stdin`, `/dev/fd/0`, `/proc/self/fd/0` are stdin; EX3 arithmetic `$(( ))` and `(( ))`; EX4-EX6; EX7 one `CommandSpec` table and `Run` union; EX9 the approval block written once; EX10 test-only exports                                                                                                                                                           |
| Extensions | `p9-ext`    | AN-1 the request plan applies to the HTTP body, `display: "summarized"`, `xhigh`; FS-1 the line cut keeps surrogate pairs; FS-2 `FileIndex` Tag folded; FS-3-FS-6; P2-4 the `@` popup reads a git listing request, fff deleted; CE-1 no worker stack on any cell error; CE-2 `$ref` resolved in signatures; CE-3, CE-4; DL-1; WK-1, WK-2; MX-2, MX-3, MX-5; AN-2, MX-4                                |
| Rendering  | `p9-render` | `@file` expansion (P1) splices at match positions; headless: only `TurnCompleted` settles, non-zero exit on an unanswered error turn, approvals decline unless the flag is set, one stderr line (P2-1, P2-2, P3-3); the open pane re-reads detail only when the listing row changed (P2-3); ops inside a cell are separated (P3-1); the core line rule in the last four TUI counts (P3-2); P3-4, P3-5 |

### Pass 9 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard      | done `fe07e1ce` … `522e8ee2`, merged `02c9f80a`: a shell option cluster with a valued `-o`/`-O` reads its value and keeps the cluster (EX1); a shell or `source` given the stdin device classifies the segment's input (EX2); `$((` is arithmetic only with a closing `))`, and a command substitution inside it is still read (EX3); `{}` is a placeholder only under xargs, parallel or `find -exec` (EX4); readable xargs input is classified (EX5); npm exec, npx, bunx, `fd -x`, gh deletes, `gh api` DELETE, SQL drop and truncate, and git config from the environment (EX6); one `CommandSpec` table and one `Run` tagged union replace 17 tables and helpers, net −127, the suite unchanged and a 1217-string differential with 0 regressions and 5 new asks that fail closed (EX7, decided by subtract-before-you-add); bash and monitor share `approveBashCommand` (EX9); the test-only exports are named as the test seam (EX10, decided by test-through-public-interfaces). `source -` is not stdin in bash 3.2 (decided by use-the-platform). Counsel: no findings. Probes red for each fix. EX8 (tree-sitter) waits for pass 10                                                                                                                                                                                                                                                                                                                                                                |
| Rendering  | done `0a5eacc6` … `088352de`, merged `6b50f5c39`: `@file` expansion splices each code block at its match span, so `$&`-style file text and repeated references stay literal (P1-1); headless follows only its own turn (the first live client message with its prompt after the send), settles on that turn's `TurnCompleted` or on the send's failure, declines every ask unless `--approve-all` (only with `-H`), prints one stderr line per failure and treats an `ErrorOccurred` notice as a warning (P2-1, P2-2, P3-3, decided by make-impossible-states-unrepresentable); the agents pane re-reads a running row's detail on every poll and other rows only when their stamp moved (P2-3, decided by derive-dont-sync); a blank line between a cell's ops and its text (P3-1); the last four TUI counts use the core line rule (P3-2); P3-4, P3-5; the TUI client ignores a notice. Counsel: another turn's completion P1, stale running cost P2, two stderr lines P3, fixed in `088352de`. Probes red for each fix. Open: headless matches its turn by prompt text because `message.send` returns no admitted message id (carried to pass 10)                                                                                                                                                                                                                                                                                                                                                          |
| Core       | done `a1bf9798` … `253fe464`, merged `83bddccb`: one `isSpawnedSession` predicate decides spawn depth, the ask rule and handoff creation, so a handoff session's turns can ask again (O1 P1, decided by one-source-of-truth); a client request's send to its own branch carries a branded `ClientRequestGrant` and the loop stamps `fromClient` only while the grant is live, checked under the admission permit (O2, MX-1, counsel P2, decided by make-impossible-states-unrepresentable); clients and extensions cannot set `joinedTurn` or runtime custom types (O3); a profile resolve reads the config and registers under one uninterruptible step, is keyed on the extension files on disk and imports them versioned, profiles share process resources by chain key with ref counts (PC1-PC4); a failed config read is not cached (K1); the models.dev cache and `writeFileAtomic` sync before the rename (K4, K5); a phase-failed turn ends with exactly one `TurnCompleted` (`streamFailed`) and runs `turnAfter` once, so a goal pauses (render decision 1, counsel P2); `ErrorOccurred.notice` (additive optional `Literal(true)`) marks the compaction fallback, and headless and the TUI read it. Counsel: late client origin P2, failed-phase `turnAfter` P2, notice consumers P2 (render batch), fixed in `36fb007b`. Probes red for each fix. Open: btw ignores `notice`; exec-tools jobs of a rebuilt resource; a hook-count test for a late failure; `message.send` returns no admitted id |
| Extensions | done `f65ed0e4` … `d3e00771`, merged `3201e573`: the Anthropic request plan reaches the HTTP body on both auth paths, adaptive thinking with `display: "summarized"`, `xhigh` on the families that have it (AN-1, live-checked); grep keeps surrogate pairs, decodes UTF-16, skips files over 10 MB (optional `oversized`), reads 16 files at a time in path order (FS-1, FS-4, FS-5); the listing is a plain function (FS-2); a tracked path under a link is not listed (FS-3); a git timeout fails the search (FS-6); `FilesRpc.List` serves the git listing and the `@` popup ranks it, sharing one pending read; `@file` follows the inline cap and a binary stays a reference; cell errors reach the model as name and message (CE-1); signatures resolve local `$ref`, keep union kinds past the limit, render records (CE-2, CE-3); a failed tool throws its error text (CE-4); delegate texts (DL-1); a wake fire is keyed by its due time and core admission skips a message id whose turn settled (WK-1, counsel P1, decided by make-operations-idempotent); one-step `schedule` (WK-2); btw ignores a notice; MX-2, MX-3, MX-5; AN-2 and MX-4 as named test seams. Counsel: replayed wake P1, per-keystroke listing P2, fixed in `86c90f50`. Probes red for each fix. User decision after the batch: fff stays in the TUI popup; the restore (fff ranks, git membership filter, fff's own frecency) waits on a dependency-edit approval                                                            |

## Pass 10 (sweep of main at `a6ea1aa7`)

Verdict: not polish. Reports: `~/.cache/gent-pass10/pass10-core.md`, `pass10-extensions.md` (sub-reports beside it), `pass10-tui.md`. P1: T1 (a large paste into the middle of the draft corrupts the sent text). Pass-9 regressions: CE-1 (unbounded cell signature), T2, T8, T9 (`@` popup and `@file`), C2 (failed-turn hooks under the interrupt permit).

Decisions:

- C1: child sessions stored before `bded8dce` read as handoffs and their wake turns can ask. Accepted and documented in ARCHITECTURE.md: an old child and an old handoff are identical on disk, so any rule would guess. Decided by make-impossible-states-unrepresentable (no stored value tells them apart) and the persisted-format rule.
- C2 is P2, not P3: a user extension's hook can deadlock the loop, and a user extension is as privileged as a shipped one. The fix is one finalize path (C3).
- FS-1: grep's regex runs in a Worker that can be ended, not through `rg` (no new external binary). Decided by use-the-platform.
- T13: user client extensions get the same public entry as shipped ones. Decided by the owner rule that a shipped extension is never more privileged.
- G4: `effect/noInlineProvide` is off in the tests override, and the 306 suppressions are deleted. Decided by subtract-before-you-add.
- fff returns to the TUI `@` popup (user decision); the restore waits on a dependency-edit approval.

### Pass 10 triage

| Batch      | Rift          | Items                                                                                                                                                                                         |
| ---------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core       | `p10-core`    | C2 and C3 one finalize path with hooks outside the interrupt permit; C4 settled-id check atomic with admission; T4 the debug model's 429 is retryable; PF1-PF7; SV1-SV6; H1; C1 note; X1 docs |
| Extensions | `p10-ext`     | PV-1 OpenAI API key through the Responses client; CE-1 signature length budget; FS-1 grep in a Worker; FS-2 one decoder for read, grep, edit; GD-1-GD-6; WK-3, WK-6; PV-2; CE-2; AV-1         |
| TUI        | `p10-tui`     | T1 (P1) paste at the caret; T2 the session cwd for `@file` and `!cmd`; T3 atomic submit; T5, T6 headless exit codes; T7 follow queries; T8-T11; T12; T13 public client entry; TUI P3s         |
| Tooling    | `p10-tooling` | G1 line-comment disables; G2 GENT_* readers; G3 comments are not consumers; G4 noInlineProvide off in tests; G5 lexer to an oxlint rule; tooling P3s                                          |

### Pass 10 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tooling    | done `fe642873` … `e2ca864f`, merged `a39b5278`: the block-disable guard catches `//` line comments (G1); the GENT_* guard sees quoted, direct-property and `Bun.env` reads and counts only real writers (spawn env records, assignments, package-script prefixes), and checks its own allowlist (G2, found the hidden `GENT_LINK` reader); comments, including those inside template interpolations, keep no export alive (G3); `effect/noInlineProvide` off in tests, 306 suppressions deleted (G4, decided by subtract-before-you-add); the workspace-import lexer is the `gent/declared-workspace-imports` oxlint rule and the plugin lives in `@gent/tooling` (G5, decided by redesign-from-first-principles); one `Finding` type replaces fourteen; lint rules that restate `effect/*` dropped; each suppression approval counts its sites (counsel P2, decided by make-impossible-states-unrepresentable); guard rows the manifests already enforce dropped; `.js` entry aliases removed; the TUI client entry is a guarded package surface. Net −507 lines. Counsel: four P2 guard gaps, fixed in `0c29f0c1`. Probes red for each fix                                                                                                                                                                                                                                                                                                                          |
| Core       | done `ef43dac1` … `0acd0fb4`, merged `8bf605f3`: a phase-failed turn ends through `finalizeTurn`'s receipt (`appendTurnReceipt`, exactly once by stored duration) and hooks (`emitTurnAfter`) outside the interrupt permit; the reported deadlock did not hold, the real defect was a Cancel landing on the next turn (C2, C3); admission checks settled and running ids under one queue permit (C4); the debug model's 429 is a retryable `RateLimitError` (T4); extension order by code unit (PF1); one directory scan keys and loads a profile (PF2); a self-interrupting resource fails its extension only (PF4); `RunOpener` tagged union (PF6); one shared-resource map (PF7); PF5 rejected (a directory stamp misses in-place edits); `driver.list` drops overrides and the one-variant tag (SV1); test-only `serverId` gone (SV2); the steer grant is off the public `SteerCommand` (SV3); deleting a session keeps and detaches the handoffs that continue it (SV5, decided by thread-is-a-view); nine unused exports demoted (H1); C1 note in ARCHITECTURE.md. Counsel: a client request's steer lost its origin (pre-existing, found by the batch), fixed in `0acd0fb4`: an own-branch steer is admitted re-entrantly while the grant is live, deduplicated by stored message id. Probes red for each fix                                                                                                                                                   |
| Extensions | done `001590a6` … `f21df5e5`, merged `bd6fb128`: OpenAI API-key requests go through the Responses client, `max_output_tokens`, reasoning summary, temperature only for non-reasoning models (PV-1); the cell signature has a length budget, each `$ref` expands once, a plain-kind fallback (CE-1, pass-9 regression); read, grep and edit share one decoder, edit keeps the file's encoding and BOM, and edit or overwrite refuses a file that does not decode exactly (optional `lossy` on read) (FS-2, counsel P2); grep matches in a Worker that a timeout ends, a line JSC gives up on is reported as `undecided` (threshold 50 ms against a measured 320 ms fastest give-up), 30 s limit, about 12 ms extra per whole-repo grep (FS-1, counsel P2, decided by use-the-platform); six guard fail-open cases closed (GD-1 to GD-6); a repeating alarm loops instead of recursing, wake is not readonly (WK-3, WK-6); context-1m only for the 4.6+ family, confirmed against the platform docs (PV-2); cell error detail kept (CE-2); agents-view reads core's `isSpawnedSession` from the public API (AV-1, SV6); `..cache` is not leaving the base (FS-3). Counsel: lossy edit P2, silent grep miss P2, Blob URL leak P3, fixed in `f21df5e5`. Probes red for each fix. Open: `btw.ask` check and send are not atomic; tests that assert inside `assertOptions` on a summary step may pass vacuously; CE-4, FS-6                                                  |
| TUI        | done `04bedc2f` … `7986d56b`, merged `ca84457f`: a large paste lands at the caret and replaces a selection (T1, P1); a submit captures its session and the cwd of that session, and `sendMessage`/`steer` take an explicit target, so a switch mid-expansion cannot misroute a draft (T2, T3, counsel P1, decided by make-impossible-states-unrepresentable); headless honors `interrupted` and `streamFailed`, and SIGINT/SIGTERM exit 130/143 (T5, T6); follow queries keep following across a switch (T7); quoted `@"path"` references, trailing punctuation, the core line rule (T8-T10, T17); fff ranks the `@` popup again (user decision), filtered to the `FilesRpc.List` listing, keyed by session, at most 5 fff pages then the shared matcher fills (about 9 ms worst key, was about 80 ms) (counsel P2 x2); prompt history merges and writes atomically (T11); one frecency read path (T12); `@gent/tui/extensions` is the public client entry and every shipped client extension imports only it; the entry's consumers are its importers wherever they live (T13, decided by the owner rule); the compaction notice is a muted row; the tray polls only while needed; grapheme-safe delegate subtitle. Live: `gent -H --debug` answers on prompts that drew the 429 before (core T4). Probes red for each fix. Open: the compiled binary cannot resolve `@gent/tui/extensions` or `@gent/core/extensions/api` for a user extension outside the repo (D5) |

## Pass 11 (sweep of main at `f3ce4450`)

Verdict: not polish. Reports: `~/.cache/gent-pass11/pass11-core.md`, `pass11-extensions.md`, `pass11-tui.md`. P1: WK-1 (a repeating `notify` alarm overwrites its own notice and grows the file). Pass-10 regressions: CE-A (self-caused error kills the cell worker), M1 (a `!cmd` spawn failure loses the draft), TX1 (a leaked fff finder), TL1 (stale allowlist names). Carried batches running: `p11-ex8` (tree-sitter proof branch), `p11-loader` (the compiled binary resolves the public entries for user extensions, D5).

Decisions:

- F11 (shared server mode, open since pass 3): deleted with idle shutdown and `runtime.status` if the deletion test agrees; its only writer is an e2e fixture in memory mode. Decided by subtract-before-you-add; not a trust gate (no persisted format, reversible).
- Guard gaps GD-7 to GD-14 wait for the EX8 result, so the two batches do not rewrite the same parser.
- PV-A (reasoning summary for unverified organizations) and PV-C (context-1m on the API-key path) change only after a docs check. Decided by prove-it-works.

### Pass 11 triage

| Batch      | Rift          | Items                                                                                                                                                                                |
| ---------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Core       | `p11-core`    | L1 lost submit; B2 fixed-port server takes the lock; H1 trust in the profile key; H2 spawn depth past 21 handoffs; R1 (F11) and R2 `EventPublisher` deletions; L2-L4; H4-H9; SDK P3s |
| Extensions | `p11-ext`     | WK-1 (P1); CE-A; FS-1 CRLF edits; PV-A-PV-D (docs check first); extension P3s outside exec-tools                                                                                     |
| TUI        | `p11-tui`     | M1; M2; TX1; TX2; B1 reconnecting; H3 loader sort; M3-M9; TX3-TX10                                                                                                                   |
| Tooling    | `p11-tooling` | TL1; TL2; TL3 client-extension import rule; TL4-TL12                                                                                                                                 |
| Guard      | after EX8     | GD-7 to GD-14                                                                                                                                                                        |

### Pass 11 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tooling    | done `83ab314f` … `d4c80ff2`, merged `397441d3`: the name-keyed export allowlist is deleted (TL1); the guard entry routes package scripts, and only their `scripts` values, to the GENT_* guard (TL2, counsel P2); the client-extension owner rule is an oxlint rule, only the roster imports sibling clients, and the TUI regex test is deleted as a second source (TL3, counsel P2, decided by one source of truth); dead overrides removed and each remaining one deletion-tested (TL4); the build script is linted (TL5); namespace receivers come from Effect imports, not capitalization (TL7, counsel P2); ternary and `childEnv` env shapes (TL8); fixtures only by directory (TL9); plugin rules read from the plugin (TL10); 36 `Deferred<void>` suppressions replaced by `Deferred.succeed(x, void 0)` (TL11); `typeof import()` and `ns.it` covered (TL12). Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Loader     | done `0a3fae52` … `1d989de5`, merged `6c19d524`: the compiled binary resolves the public entries for a user extension outside the repository (D5); `BuiltinExtensionModules` in `@gent/extensions` binds the `effect`, `effect/*` and `@effect/*` peers that shipped extensions import, and a test derives the set from those imports (counsel P2, decided by derive-dont-sync); client-only names (`@gent/core/protocol`, `@gent/tui/extensions`, `solid-js`, `@opentui/solid`) resolve only for `*.client.*` files, through a per-load prefix and a `Bun.build` rename, because a runtime `onResolve` sees no importer (counsel P2); a combined-process test and a compiled-binary e2e test (`test:e2e` depends on `build`). Probes red for each fix. Live: binary in a PTY rendered a user client widget while a server file importing protocol failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| EX8        | rejected, branch `p11-ex8` (`f30f1d53`, `0442cb28`) not merged: tree-sitter-bash read every shape the hand parser reads, and after counsel it failed closed by construction (a role table over all 59 named node kinds, checked against `node-types.json`; every redirect target walked; P1 `$(< <(…))` fixed red first); the 9,733-string differential had 0 lets. It failed the memory bar: RSS +58 MB at 1,000 classifications and +223 MB at 10,000 (bar: under 50 MB at 1,000, or bounded by a reset); a parser reset every 500 calls did not help, because the wasm module holds a 32 MB minimum heap and JS wrappers are collected late. Net −288 lines, below the −400 bar. Decided by subtract-before-you-add: the dependency does not pay for itself. Its regression corpus moves to the hand parser in the guard batch. Not measured: the native N-API binding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Extensions | done `daac3a4d` … `aa7c1f60`, merged `f024008a5`: a repeating notify alarm moves only its own row and keeps each notice (WK-1, P1); a self-caused error cause reads one level and the cell worker lives (CE-A); system error code, path and syscall are logged (CE-B); edit matches across CRLF lines and keeps each line's ending, an exact match first (FS-1, counsel P2); write keeps an existing file's encoding, and `writeFileAtomic` takes bytes and writes empty content (FS-2 to FS-4, a real empty-write bug); an SSE error frame fails the search (NT-1); CRLF skill bodies (SK-2); the OpenAI API key retries once without the reasoning summary an unverified organization is refused, remembered per key fingerprint (PV-A, counsel P2); gpt-6 sol/luna rows (PV-B); no `context-1m` beta on either Anthropic path, the docs say 1M needs none, and a model outside the family table lists at 200k (PV-C, decided by the docs); `repairToolPairs` deleted, about 190 lines (PV-D); the compatible driver takes `apiUrl` (PV-G); a notify fire stores its notice and moves its row in one write, so the carried "notify dedupe across a shutdown" item is closed (counsel P2). Probes red for each fix. Open: MODEL_CONFIG is behind opencode-claude-auth v2.2.1 (base betas, opus-4-5 effort beta); a live OAuth run past 200k is unconfirmed                                                                                                                                                                                    |
| Guard      | done `9ee3fe90` … `d08f1d1a`, merged `2e4d4a1c`: the xargs/parallel placeholder is an `Option<string>` read from `-I`, `--replace`, `-i` and `-J` (GD-7); unreadable input follows runners to the innermost command and asks when its word or git's subcommand is missing or the placeholder (GD-8); `sudo --shell/--login` (GD-9); `gh -R/--repo` after the group and `release delete-asset` (GD-10, GD-11); any SQL `DROP`, `TRUNCATE`, and `DELETE FROM` without a `WHERE` outside a comment (GD-12, counsel P2); runners `arch`, `pkexec`, `unshare`, `systemd-run`, `sg` (GD-13); xargs input is divided as xargs divides it (quotes, escapes, `-0`, `-d`, `-I` lines) and asks when it cannot be rebuilt, one command per item with `-n1`/`-L1`/`-I`, and `parallel :::` ends the command (GD-14, counsel P1: a quoted `'DROP TABLE t'` was split into fragments); `xargs --max-lines` no longer eats the next word (found by the batch); the monitor approval names a cwd outside the session (EX-1). The EX8 regression corpus (39 destructive, 1 sensitive, 4 safe shapes) passes on the hand parser with no source change. Four shapes now rated safe because xargs `-I` and parallel pass a line as one argument (decided by redesign-from-first-principles). Probes red for each fix. Open: `a[1]=2` over-asks                                                                                                                                                                                                                     |
| TUI        | done `b45b9a5c` … `ccfeb407`, merged `9f4d76b5`: a failed `!cmd` spawn and a refused send or steer return the text to the draft of the branch they came from, in send order, none lost, and the reason shows in that session, held until the reader returns (M1, M5, counsel P2 x2, decided by one-owner-per-state: refusals join the existing per-branch draft store); shell spill under `GENT_DATA_DIR` (M8); an autocomplete pick after a newline (M2); one fff finder per directory, destroyed with the client runtime (TX1); fff records a pick against the session's file (TX2); `isReconnecting` matches the PascalCase tags (B1); client extension ids by code unit and a duplicate id fails both (H3, TX6); headless startup signals, whitespace prompts, declined and background bash (M3, M4, T16); quoted directory drill-in (M6); the shared line rule (M7); one `sameIdentity` (M9); the `@` listing and `/btw` name the session they were typed in, a busy `/btw` is refused out loud (TX3, TX4, T22); the wake tray re-reads on a pulse (TX5); `driverList`, `ClientSetupError` and `ClientEffect` deleted (TX7, TX8); `AutocompleteContribution.onOpen`, one listing read per open (T18); the skill pick write is scoped, the palette read is one fiber (T21); server copy names one server per data directory. T20 rejected (decided by composition-over-flags). Probes red for each fix. Open: fff database ignores `GENT_DATA_DIR`; extension-side session comparisons duplicated; `parseFileRefs` has only test consumers |
| Core       | done `093c5931` … `ab3a6d53`, merged `fe3e2aaf`: the worker owns take-to-start in one uninterruptible region, and an interrupted admitted start releases its reservation and requeues the item, so no queued turn is lost or stranded (L1, counsel P2, decided by make-impossible-states-unrepresentable); a fixed-port SQLite server takes the lock and never attaches (B2); project trust is part of the profile scan and key (H1); spawn depth walks the whole parent chain, a cycle fails closed (H2); shared server mode, idle shutdown and `runtime.status` deleted, +52/−533 (R1, F11); `EventPublisher` deleted, runtime yields `EventStore` (R2); follow-up verbs and the QueueFollowUp grant removed, one `interject` (L2-L4); the package validator alone owns a duplicate key (H4); process resources version by path (H5); `driver.set` names a registered driver (H6); dead lifecycle members, SDK re-exports, `BuildFingerprint.Test` and dedup options removed; one header rule for both transports; the logger appends; `headTail` odd limit. Net −126 lines. Probes red for each fix. Open: H6 wide form (a stored `DriverRef` with no id, persisted read, owner decision); an idle admit starts ahead of queued follow-ups (pre-existing order rule); `ActiveInteraction`/`ApprovalResult` names                                                                                                                                                                                                                            |

## Pass 12 (sweep of main at `26fb9000`)

Verdict: not polish. Reports: `~/.cache/gent-pass12/pass12-core.md`, `pass12-extensions.md`, `pass12-tui.md`, `pass12-tooling.md`. Pass-11 regressions: C1 (`releaseStart` erases the turn-failure mark and strands its item; probe against the real inbox), FS-1 (the exact-first edit match can start between a CR and its LF), TU1 (a `!cmd` whose output send is refused comes back as a command and runs again on Enter). Gate: TL-1 (CI has failed on every recorded run: ubuntu runner, darwin-only build targets), TL-2 (turbo build inputs miss theme JSON, skill markdown and patches, so a stale binary can be restored).

Decisions:

- C1: the start is the loop's job, not the interrupted caller's; `releaseStart` goes if the structure removes the gap. Decided by make-impossible-states-unrepresentable.
- AN-1: the long-context backoff is deleted. Its only default candidate after pass 11 is `interleaved-thinking`, and a second request gets the same 400. Decided by subtract-before-you-add; independent of the open live OAuth check, which concerns the window, not the backoff.
- TL-1: CI runs on a macOS arm64 runner (the build targets darwin-arm64 by design); the repo is public, so the runner is free. Workflow edit only, no push.
- C7 (`serverLock` verbs in the SDK entry) stays as decided in pass 11.

### Pass 12 triage

| Batch      | Rift          | Items                                                    |
| ---------- | ------------- | -------------------------------------------------------- |
| Core       | `p12-core`    | C1; C2 `ExtensionStatePublisher` deletion; C3-C6; C8; C9 |
| Extensions | `p12-ext`     | FS-1 to FS-4; AN-1; WK-1 to WK-3; PV-1                   |
| Guard      | `p12-guard`   | GX-1 to GX-7                                             |
| TUI        | `p12-tui`     | TU1 to TU10                                              |
| Tooling    | `p12-tooling` | TL-1 to TL-12                                            |

### Pass 12 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Extensions | done `5b37f561` … `b48d72b5`, merged `b61cfdc4`: an edit match never splits a CRLF pair, a search with a CR matches the raw file and every other search the LF view, so an LF search finds every CRLF site (FS-1 pass-11 regression, FS-2); a match is its ranges (FS-3); write and edit refuse a lone surrogate (FS-4); the Anthropic long-context backoff deleted, about 190 source and 300 test lines (AN-1); an answered turn writes the wake file only when it clears something and clears the blocked notices it was shown (WK-1, WK-2); `wake.cancel` names each id once (WK-3); the OpenAI refusal set keyed by the key, fingerprint dropped (PV-1). Counsel: only polish, a coverage gap; the new bare-CR test found that a replacement in a bare-CR file wrote LF breaks, fixed red first. Net −180 lines. Probes red for each fix                                                                                                                                                                                                                                                                                                        |
| Tooling    | done `6ac6e38d` … `68c1b624`, merged `b5af9ef6`: CI runs on `macos-15` arm64 with Bun from `packageManager`, it had failed on every recorded run on ubuntu (TL-1, counsel P2 for the macos-14 retirement; not pushed, so not run on GitHub); build inputs are `src/**` and `patches/**` plus the root `package.json` are global, so a theme, skill or patch edit changes the build hash (TL-2, measured with `--dry=json`); the gate checks formatting (TL-9); smoke runs `--debug` in a scratch data dir (TL-10); gamut checks herdr first, cleans up a failed `up`, settles on the run record newer than the last `send` (TL-5, TL-6, counsel P2), TL-7; one `isTestSupport` for the export guard and the lint rule, one `seedAuthKeys`, `Auth`/`AuthApi` leave `host` (TL-3); `emptyQueueSnapshot` to test-utils (TL-4); `tsconfig` paths removed and any alias fails the guard (TL-8, decided by encode-lessons-in-structure); every workspace manifest has a surface row (TL-12); unread test inputs dropped (TL-11). Two counsel P2s rejected: `apps/tui/turbo.json` already lists `scripts/**/*.ts` and `bin/gent*`. Probes red for each fix |
| Guard      | done `02863d8d` … `3964fc98`, merged `e5e3978d`: xargs `-n/-L/-l` and parallel `-N` group the input by count (`InputBatch`, `logicalLines`, a trailing-blank `-L` line joins the next) (GX-1); `-a/--arg-file`, `::::` files and a `< file` redirect on xargs or parallel mark the input unreadable (GX-2, counsel); parallel `:::` source combinations with `:::+` linking and a 1024-job cap, replacement strings `{n} {.} {/} {//} {/.} {#} {%}`, perl strings and `--colsep` ask (GX-3); BSD `-J` appends when R is not a word (GX-4); unreadable input followed through `VAR=value` runners and into runner scripts (GX-5); each SQL argument read alone, comments, strings and quoted names blanked per dialect (GX-6); any statement that starts with DELETE needs its own WHERE, whatever its target list (GX-7, counsel P1: a quoted schema-qualified MySQL target was rated safe, decided by fail-closed); a dollar quote searched only at `$` (232 KB heredoc 167 ms → 27 ms). Probes red for each fix. Open: `bash < script.sh` rated safe like `bash script.sh`; xargs `-s` splits; parallel `-n N` with `{n}` read like `-N`          |
| Core       | done `89970a3b` … `23d4088c`, merged `18a6d0af`: a reserved start runs in the loop scope (`startInLoop`), admission and the fork of its start are one uninterruptible step, so an interrupted caller stops waiting while the start still runs; `releaseStart` and `claimStart` deleted, the failure mark can no longer be lost (C1 pass-11 regression, C3, decided by make-impossible-states-unrepresentable); recovered turns start through `startRecovered`; `ExtensionStatePublisher` deleted, the State facet publishes through `EventStore` (C2); `server-root.ts` folded into the SDK root and the harness (C6); project trust read once for the server and the TUI, fail closed on a broken user config (C8); a lock entry that cannot be written fails the start, headers derived once (C9); C4, C5. Counsel: only polish; the loop-close test it asked for found no bug. Probes red for each fix                                                                                                                                                                                                                                           |
| TUI        | done `35e5b7f0` … `d2c56f04`, merged `59055960`: a refused send of a `!cmd`'s output restores the output as a message and says the command ran, so Enter cannot run it twice (TU1 pass-11 regression); one send retry policy for send, steer and the startup prompt, under one request id, only on transport failures (TU2, TU10, counsel P3); a refusal keeps paste placeholders (TU3); an error set before a session's snapshot survives it (TU4); a failed `/btw` after a switch is reported with its question (TU5); the loader binds `@gent/extensions/client` for user client extensions, so shipped and user clients import one surface (TU6, decided by the owner rule); one fff finder, for the directory it ranks (TU7); `--isolate` and SDK copy (TU8); three dead exports, `SessionSettings` from the protocol, and the export guard counts a leaf app's names only inside the app (TU9). Counsel: no P1/P2; steer retry and mid-ranking finder tests added. Probes red for each fix                                                                                                                                                    |

## Pass 13 (sweep of main at `7548b607`)

Verdict: not polish. Reports: `~/.cache/gent-pass13/pass13-core.md` (only polish), `pass13-extensions.md`, `pass13-tui.md`, `pass13-tooling.md`. P1: EX-1 and EX-2 (pass-12 regressions: `psql --command='DELETE FROM t'`, `mysql --execute=…`, a DELETE inside a `$$` body, after `EXPLAIN ANALYZE`, or in a `PREPARE` rated safe); U1 (choosing the session already on screen resets its status, metrics and settings, and the next `/model` or `/think` erases the session's model). P2: EX-3 (a comment between DROP and its object), EX-4 (an option missing from a runner table hides the real command), EX-5 (four SQL dialect readers), U2 (pass-12 held error returns), TL13-1 (the parallel test suite overloads a 3-core CI runner), TL13-2 (gamut rebuilds the TUI but runs a stale cell worker).

Decisions:

- SQL guard: stop parsing SQL. A SQL client whose SQL text (every argument, attached and long-option values, and readable input) contains DELETE, DROP or TRUNCATE as a word asks, with or without WHERE, comments or quoting. The dialect readers, `sqlCode` and `deletesAll` are deleted. Over-asks on `select 'delete'` are accepted. Decided by fail-closed and subtract-before-you-add: three passes of SQL parsing reopened holes each time.
- Runner options: an option a runner's table does not know makes the command unreadable (ask), instead of guessing which word is the command. Decided by fail-closed.
- TL13-1: the worker count is sized to the machine in one place; CI confirmation needs a push, which waits for the owner.

### Pass 13 triage

| Batch   | Rift          | Items                               |
| ------- | ------------- | ----------------------------------- |
| Guard   | `p13-guard`   | EX-1 to EX-5                        |
| TUI     | `p13-tui`     | U1 to U10                           |
| Tooling | `p13-tooling` | TL13-1 to TL13-4, TL13-6 to TL13-12 |
| Polish  | `p13-polish`  | core C1-C4, TL13-5 docs, EX-6, EX-7 |

### Pass 13 results

| Batch            | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Polish           | done `61295e0d` … `98fb664e`, merged into main after `d0008bde`: steering docs drop `ExtensionStatePublisher` and host auth (TL13-5); `reserveAndStart` inlined, the unread `trustedProjects` merge dropped (core C1-C4); a failed wake re-arm still shows its notices, so the answered turn clears only what it read (EX-6); an edit whose `oldString` matched only after unescaping is refused without a write, and the whitespace/unicode tier runs on the literal search (EX-7, counsel P2: unescaping `newString` too could corrupt code that holds a real escape, decided by fail-closed). Net +120 lines, mostly tests. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Guard            | done `e6879097` … `3fc6e07b`, merged `670e4722`: a SQL client asks on a DELETE, DROP or TRUNCATE word anywhere in its SQL text; the dialect readers, `sqlCode` and `deletesAll` deleted (EX-1, EX-2, EX-3, EX-5); an option a runner's table does not know asks (EX-4); counsel P1: SQL the guard cannot read asks (`-f`/`--file`/`-init`, a `<` redirect, unreadable piped input, psql `\i`, MySQL `source`, SQLite `.read`), the guard never opens a file; counsel P2: PREPARE, EXECUTE, EXEC, `sp_executesql` and `DO` blocks ask, so hex-built SQL asks; per-client valued-option tables. `exec-tools.ts` −36 lines. Accepted over-asks: mysql `-f` (`--force`), `< /dev/null`, a `-p` password holding a trigger word. Differential over 1,388 commands: 45 changed, all toward asking. Open: SQLite `.restore FILE` replaces the database and does not ask. Decided by fail-closed. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| TUI              | done `a47c8434` … `f67e0535`, merged `209d5486`: choosing the session in view is a no-op, `UpdateBranch` deleted so `switchSession` is the one identity writer (U1, U2; a branch-switch red test found the second writer); the settings RPC input is a change of named fields, `optionalKey(OptionFromNullOr)`, merged by the server inside its transaction, so a `/model` or `/think` no longer erases the other field (U1 server side, decided by one-owner-per-state; the stored row and event keep their shape); a held error lasts until a later status replaces it, `snapshotIn` deleted (U3); a `/btw` failure in a left session is not the new pane's error (U4); a large refused text returns as a paste placeholder (U6); the startup prompt is an ordinary submission, a failed send returns to the draft, no automatic re-send (U7); `GentConnectionError` leaves the RPC error unions (U10); U5 documented. Counsel P2: a send whose reply was lost keeps its request id, so an unchanged resend dedupes on the server (decided by idempotent retries). U8 rejected (subtract-before-you-add). Live check: `/model` + `/think` survive a same-session pick. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                    |
| Tooling          | done `f8a4592b` … `7381fa68`, merged `ca6a54be`: test workers sized once per package (bare `--parallel`), turbo runs one test package at a time, a 30 s bun backstop behind the inner `Effect.timeout` bounds (TL13-1, TL13-12; on E-cores 11 failures → 2 known flakes; local test time about 19 s → 36 s, decided by correct-not-pragmatic; CI confirmation needs a push); gamut runs the root build so the cell worker is rebuilt, the TUI build stops without it (TL13-2, decided by one owner), a hidden `/goal status` message no longer opens a turn (TL13-8), `--prompt` needs a value and pgrep no longer matches `gent-cell` (TL13-11); testbeds linted, about 208 errors fixed (TL13-9); one test-file vocabulary for rules and guards (TL13-3); the paths guard reads every tsconfig as JSONC and checks each row's alias (TL13-4); turbo inputs trimmed (TL13-6); test-utils stops re-exporting names host owns (TL13-7); fixture oxlint under one bound (TL13-10); the retired-surface guard reads AGENTS.md, ARCHITECTURE.md and docs/, with rows for passes 11-13. Counsel P2 rejected: `apps/tui/turbo.json` already declares `bin/gent` and `bin/gent-cell` outputs. Open: `runProcess > surfaces nonzero exit code` still flakes under the hook with no root cause (8/8 alone), message-projection tests are CPU-bound. Probes red for each fix                                                                                                                                                                                                                                  |
| Interrupted work | done `56bf6715` … `36076b16`, merged `d9465dca` (owner idea: report work that did not finish before the previous process stopped, like Claude Code; design `~/.cache/gent-pass13/interrupted-work-design.md`): one `loopOpen` hook kind runs once per branch loop open, each hook on its own fiber without the turn permit, so a hanging user hook delays no turn and no other hook (counsel P1, decided by children-wake-never-block); wake alarms and monitors re-arm there, and a notice clears only when it fired before the turn started; a delegate child stopped mid-turn resumes when its parent opens, core primes the loop with `GetState` before a repeated durable send, and reconcile reads the receipt after a re-send so a child that finished in the crash window still wakes its parent (counsel P1); a background shell job the server stopped is marked interrupted on its row and reported once on open, the notice starts a turn; a tool call a restart cut short is reported `Interrupted`, not run again, while a call parked on a question resumes — the `parked` mark is written when the call parks and a failed mark fails the step (counsel P2; additive optional field on `pending_tool_calls_json`, decided by fail-closed); a profile-resolve deadlock fixed; SQLite `.restore` and `.import` ask. Rejected by principle: a startup sweep of unopened sessions (YAGNI), a "noticed" column (derive-dont-sync). Open: orphaned `bash -c` after a hard kill; the profile lease lives until the loop closes (`captureAgentLoopRuntimeContext`). Probes red for each fix |

## Pass 14 (sweep of main at `9dd48a9a`)

Verdict: not polish. Reports: `~/.cache/gent-pass14/pass14-core.md`, `pass14-extensions.md` (sub-reports `pass14-sub-*.md`), `pass14-tui.md`, `pass14-tooling.md`. Pass-13 changes held except GD-3. P1: L1 (an 11th queued message closes the loop and interrupts the running turn), X-1 (six client requests without `readonly: true` wait for the running turn: `/btw`, wake tray, goal label, `$` skills). P2: L2 (a withdrawn admission hands its interrupt to the next turn), H14-1 (the auth gate reads the model prefix, not the driver routing), H14-2 (launching from `$HOME` scans `~/.gent` as user and project scope), GD-1 (SQL from `$VAR` or `$(…)` rated safe), GD-2 (SQL-client shell escapes and output pipes rated safe), GD-3 (pass-13 regression: benign runner flags ask), CE-1 (a late throw kills the cell worker), CE-2 (cancelled cells spend the three lifetime worker replacements), PV-1 (the keychain 401 retry re-reads the same token), U14-1 (an error shown mid-turn hides the running turn: Esc, Ctrl+C and interject misbehave), U14-2 (raised from P3 by fail-closed: a refused long `!cmd` returns as a placeholder, Enter runs a command the reader cannot see), TL14-1 (`GENT_LINK=1` never reaches the build through strict-env turbo), TL14-2 (three stale docs, about 2,150 lines).

Decisions:

- GD-3: runner options without flag tables; every later word is a possible command and the worst rating wins. Decided by fail-closed and subtract-before-you-add; refines EX-4, still never guesses toward safe.
- GD-1: SQL whose text is not literal (a parameter expansion or a command substitution) asks. GD-2: SQL-client meta-commands use a read-only allowlist; anything else asks. Decided by fail-closed.
- L1: queue-full is its own typed refusal; the loop closes only on loop or persistence failure. Decided by make-impossible-states-unrepresentable.
- PV-1: a 401 forces a refresh through the refresh token before retrying. Decided by fix-root-causes.
- U14-1: "a turn runs" and "the error shown" become two values; `AgentStatus.Error` is deleted. Decided by one-owner-per-state.

### Pass 14 triage

| Batch      | Rift             | Items                                     |
| ---------- | ---------------- | ----------------------------------------- |
| Core       | `p14-core`       | L1, L2, H14-1, H14-2, core P3s            |
| Extensions | `p14-extensions` | X-1, CE-1, CE-2, PV-1, extension P3s      |
| Guard      | `p14-guard`      | GD-1, GD-2, GD-3, guard P3s               |
| TUI        | `p14-tui`        | U14-1 to U14-6                            |
| Tooling    | `p14-tooling`    | TL14-1, TL14-2, tooling P3s (unused deps) |
| Efficiency | `p14-efficiency` | E2, E3, E4, E5 (flag), E6, E8-E11         |

Efficiency sweep (`pass14-efficiency.md`, lens `.claude/skills/architecture-loop/efficiency.md`), baseline from a copy of the owner's database, 2026-09-09 to 09-23: 1,980 Anthropic steps with 0 cache reads and writes, about $104 of $129 at list prices (fixed on main by `cbc5b4f9d`, replay simulates −61%); reasoning items never passed back on either provider; child cost never reaches the parent (children 39% of spend); the cached prefix is byte-stable; static prefix about 12k tokens, the skills listing 45% of it; read line numbers 0.37% (polish). The owner approved three paid probes (Anthropic caching about $0.10, Codex subscription $0, reasoning before/after at most $5).

### Pass 14 results

| Batch        | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TUI          | done `22ad9f40` … `f90c9d5f`, merged `c200ee40`: "a turn runs" and "the error shown" are two values, `AgentStatus` deleted, only the stream, lifecycle and snapshot write `running`, a turn start clears the error, so Esc, Ctrl+C, interject and the activity reporter keep working under an error (U14-1, decided by one-owner-per-state; an error now stays after `TurnCompleted` until the next turn); a refused `!cmd` returns as the command Enter runs, and a kept block shows one way (U14-2, U14-6, decided by fail-closed); the same-branch check left to `switchSession` (U14-3); `SessionShellProvider` folded into `ComposerMemoryProvider` (U14-4). Counsel P2: a turn that started during a disconnect drops the held error, counted from `metrics.turns` with no server field. Open: a branch not in view shows its held reason on return even when a turn started there. Live check: `/model typo` mid-turn, then Esc interrupts. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                            |
| Core         | done `d97b13e9` … `43243540`, merged after `b7dde62c`: a full follow-up queue is its own typed refusal (`FollowUpQueueFull`), `orCleanup` passes it through and the loop closes only on loop or persistence failure (L1, decided by make-impossible-states-unrepresentable; the reply error union gained a member, old replies decode); a withdrawn admission runs take → `beginTurn` → `advanceOrIdle` under the interrupt permit (L2); admission asks `canStartTurnNow` alone (L3); the auth gate asks for the driver the turn routes through (H14-1); one `hasProjectScope` over realpaths, so a launch from `$HOME` reads `~/.gent` once as user scope (H14-2); `SessionRuntime.Live` deleted (L5); the product model resolver loses its test hook, the check lives in test-utils (H14-3); `DEFAULT_AGENT_NAME` leaves the authoring entry, six authoring types stay by the deletion test (H14-4); the settings wide event logs the stored result (U14-5); S2-S5. S6 rejected (subtract-before-you-add). Counsel: no findings. Probes red for each fix                                                                                                                                                                                                                                                                                                                            |
| Tooling      | done `342e27a4` … `831fe1a1`, merged `3e390ebd`: `GENT_LINK` deleted, an uncached turbo `link` task after the build re-points `~/.bun/bin/gent` even on a cache hit (TL14-1, proved with a real run, pointed back at main); `docs/actor-model.md` and `docs/malleability.md` deleted, the FX receipt filed under research, stale ARCHITECTURE receipts fixed (TL14-2, TL14-4); four unused dependencies dropped and guards fail on a dependency or catalog entry nothing uses (TL14-3); one `readJsonc(path, schema)` for every config (TL14-7); `build.ts` typechecked as an Effect program (TL14-8); one `isShippedSource`, `isSteeringFile`, `ruleSubject` (TL14-5, TL14-6, TL14-9); one set of runner settings, the Solid preload only in `bunfig.toml` (TL14-10); gamut `wait` after a slash command (TL14-11). Counsel P2: a slash command settles only on a stored event after the send, or after 5 quiet reads; P3: comments are stripped before the dependency scan. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                 |
| Extensions   | done `d6cb7c94` … `8573e8aa`, merged `a641f99f`: six client requests (`btw.fork/ask/progress`, `wake.Pending`, `goal.get`, `skills-list`) answer while a turn runs; the flag `readonly` renamed `answersDuringTurn`, opt-in, a request without it waits for the permit (X-1, decided by fail-closed; the `/btw` test no longer waits for idle); a late throw or unhandled rejection in cell code reaches output and the worker lives (CE-1); only a worker that dies before completing a cell counts toward the failed-launch limit (CE-2, counsel P2); a 401 on the keychain path refreshes (PV-1), invalidates only the credential the request sent via one `authorizedClient` (counsel P2), and a refresh write-back never overwrites a newer sign-in (compare-and-swap, counsel P2; the keychain has no lock, so the race is narrowed, not closed); about 20 P3s (wake refuses a past alarm, MCP `isError` is a failure, models.dev memo 5-minute TTL, `preview` key dropped with old rows still decoding). Counsel P2: an uncaught error is shown only where its origin is known — AsyncLocalStorage names the cell for timer throws; a rejection or microtask throw has no store in Bun and goes to the next cell labelled "origin unknown" (decided by make-impossible-states-unrepresentable). SS-4, PV-5, CE-8 rejected. Probes red for each fix                             |
| Guard        | done `42724ae6`, `7d3ecd96`, `6aab0a76`, `7d0aab2c`, merged `4c8a7931`: after an option a runner does not name, every later word may be the command (GD-3); SQL known only at run time, client meta-commands, psql variables, SQLite shell functions and output pipes ask (GD-1, GD-2); each runner reading is read once. Counsel P1s, both red first: a parent option can hide the subcommand, so every plausible subcommand position is read and the strongest rating wins (`npm --loglevel silent exec --`, `uv --cache-dir x run`; `git --no-pager status` stays safe); an unquoted expansion in a SQL connection position asks (`ShellWord.splits`; `psql -d "$DB"` stays safe). Accepted over-ask: `git --no-pager commit -m push`. Decided by fail-closed. Differential over the pass-13/14 corpus: only intended flips. Probes red for each fix. Agent safety slips (harmless heredoc and `python3 -c` probe text) led to the tightened brief                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Live check   | gamut `mixed` on main `2a9730ab` through herdr, prompt driving delegation, a background job, a wake alarm, the cell, `/btw` mid-turn and resume. Sound: `/btw` answers during a turn, the alarm tray and the wake fire, the resume notice names the unfinished `bun test --watch`. Anthropic children failed on a locked Keychain (environment). Found: LV-1 a `/btw` fork opened mid-turn takes over the parent's unanswered task (16 duplicate children in the same tree); LV-2 a child's failure reason never reaches the parent, which re-spawned four waves; LV-3 Ctrl-C cannot quit while the `/btw` pane is open, or while children keep starting turns. Batch `p14-live`. For pass 15: `ExtensionStateChanged` is 31% of stored events                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Cache notice | done `f654607b`, `c4c7f6db`, `5bc47f9e`, merged `76dc4cfb`: a client extension `@gent/cache` derives prompt-cache miss rows from stored usage (a notice-row bucket; nothing stored, the model never reads them) with the cause (idle, tool, approval, child, wake, a long response, model switch, changed prefix) and a waste total. Counsel P2s, one round, red first: priced by the model the runtime priced (`StreamEnded.pricedModel`, additive optional); missed tokens fill the cache-write bucket first; the gap is start to start and names "expired during a 7m response"; the feed no longer waits for extensions, a late subscriber gets the branch's history first; rows wait for the model catalog and are born with their final text (`modelCatalog()`, `NoticeRow` sources answer `Option`). Decided by one-owner-per-state and native-history-settles-before-commit. Open: the hub keeps every envelope of the branch for the feed's life (memory grows with session length); cache batches B (1-hour TTL) and C (warmer) deferred. Live check with real Anthropic usage waits for an unlocked Keychain. Probes red for each fix                                                                                                                                                                                                                                      |
| Efficiency   | done `11b447d8` … `49bbe7b1`, merged `13af06f9`: reasoning goes back with its provider state (E2); Codex requests carry `session-id` (E3, measured: later steps with a cache read 3/18 → 17/36, 0/18 with the header removed); the Anthropic prefix marker ends at the system prompt, the tail marker stays (E4, unit evidence only); a child's completion carries its whole bill and compaction is priced (E6, `TurnCompleted.costUsd` only when every step's usage is known; additive optional fields); compact skills listing behind `GENT_SKILLS_LISTING`, default full, −69% of the listing (E5, flag until measured); local Date line with zone; failed and interrupted `StreamEnded` name their model. Reasoning before/after on Codex: 8/9 correct both, +60-170 input tokens per step. Counsel, one round, red first: Anthropic adaptive thinking sends `drop_block` for a prefix-bound block and OpenAI retries once without reasoning it cannot decrypt (P1, decided by fail-closed, doc receipts in the report); both child-bill writers use `steps > 0 && usageKnown` (P2); a zero cache read on an implicit-caching provider is no changed prefix (P2, found by the batch's probe). Paid spend $0; the Anthropic probe is blocked on an expired gent Anthropic sign-in and a locked Keychain. E7 (bindings only on change) proposed, not built. Probes red for each fix |
| Live fixes   | done `6fe250b2` … `70a12cb9`, merged `b0bc8ef1`: each `/btw` question reaches the fork under a header that says an unanswered request above is the session's work, not the fork's (LV-1, framing only, decided by fix-root-causes; copying history only up to the last completed turn rejected, it drops what the question asks about); a child's completion carries the error its turn ended on, one line of at most 1,000 characters (LV-2, `details.error` additive optional); a second Ctrl+C within the quit window after one that cancelled a turn exits, and any other gesture between the presses disarms it (LV-3b). LV-3a rejected: not reproducible idle, the live failure was LV-3b with the pane open. Counsel, one round: draft and transcript-toggle disarm, header stripped only by message type, error cut with its ellipsis. Rejected: the full transcript shows the frame (full detail is the raw view). Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Live check 2 | gamut `sol-luna` on main `b0bc8ef1` through herdr (Anthropic still blocked). Sound: the `/btw` fork answered in one step with no tool calls (LV-1); the children fixed the app, 15 pass, 0 fail; double Ctrl+C quit with the `/btw` pane open and children running (LV-3); the wake alarm survived the restart. Found: LV-4 a parent whose interrupted turn stopped its children is never told, so its next turn said "Tasks 4–6 are still running" with none running (the stop settles the rows so no message wakes the parent, and no notice reaches its next turn); LV-5 the `/btw` pane showed the question but not the stored answer while the tray had four rows (the pane appears squeezed below its content). Batch `p14-live2`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Live fixes 2 | done `47f640f9` … `3b783ff1`, merged `5764abf2`: a parent whose interrupted turn stopped its children reads a non-waking notice naming them on its next turn (LV-4); an open docked pane keeps its body visible on a short terminal: trays hide while any pane is open (decided by experience-first: the opened pane wins), the pane gives way in whole rows and drops its key hint before its last body row (LV-5). Counsel P2s, red first: an unanswered turn keeps the notice; a turn clears only the notices its prompt showed (`ShownStopNotices`, in memory, fail-closed on a lost process); at most 8 children per notice plus "and N more"; a one-row body at 13-14 rows. Open: `@gent/wake` clears its notice on an unanswered turn and by elapsed time, the same two bugs; goes to `p15-ext`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Pass 15 (sweep of main at `6104bf67`)

Verdict: not polish. Reports: `~/.cache/gent-pass15/pass15-{core,extensions,guard,tui,tooling,efficiency}.md`. Pass-14 changes held. No P1. P2: three guard fail-opens (G1 flags from a variable, G2 shell alias and `hash -p`, G3 SQL beyond DELETE/DROP/TRUNCATE); C15-1 `loopOpen` hooks wait for a resumed turn after a restart; X15-1 = T15-1 = EF15-1 `/btw` stores a state pulse on the parent for every fork event (1,053 of 1,155 stored pulses in the live run; 0.6% of the owner's real events); X15-2 an Anthropic refresh can return the stale credential; TL15-1 the unused-dependency guard skips `peerDependencies` (dead `@effect/sql-pg`); TL15-2 the safety rule lives only in the per-pass brief. Reductions: R1 one command-start rule in the guard (−30 lines, one concept). Every large pass-14 addition passed the deletion test. Efficiency: nothing left to change directly; pass-14 changes measured on Codex (reasoning with provider state 0/29 → 47/47, later Codex steps with no cache read 45% → 28–33%, child bills 3/3); Anthropic caching still unmeasured (sign-in).

Decisions:

- Stored state pulses: fix the writer (`/btw` pulses only when its view changes), not a core pruning of stored rows. Decided by fix-root-causes; the stored share in real use is 0.6%.
- A resume notice for an interrupted background job no longer starts a turn: it is a notice the next turn reads, reusing LV-4's mechanism, with wording that does not invite a rerun (C15-3). Opening a session to read it must not spend a paid turn with no user present. Decided by the owner's original request (report unfinished work on resume, as Claude Code does) and fail-closed on spend; "children wake" still holds for a child's completion.
- `GENT_SKILLS_LISTING` default: decided by a $0 Codex before/after run (sol-luna), after the pass-15 batches merge.

### Pass 15 triage

| Batch      | Rift          | Items                                                                                                                                          |
| ---------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard      | `p15-guard`   | G1, G2, G3, R1, G4                                                                                                                             |
| Core       | `p15-core`    | C15-1, C15-4 to C15-10, X15-2                                                                                                                  |
| Tooling    | `p15-tooling` | TL15-1 to TL15-6 (skill matches practice, safety in prompts)                                                                                   |
| Extensions | `p15-ext`     | X15-1 (btw pulses), C15-3 (resume notice), X15 P3s, T15-2 to T15-5, C15-10, wake notice clearing (from `p14-live2` counsel); after `p14-live2` |

### Pass 15 results

| Batch          | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tooling        | done `d9ec46b4` … `dca1dbb7`, merged `273ef22c`: the unused-dependency guard reads `peerDependencies`; it failed on the dead `@effect/sql-pg` peer before the peer, the root devDependency and the catalog entry were dropped (pg tree −51 lines of `bun.lock`) (TL15-1, a guard blind spot closed); the e2e suite runs every file in `tests` (TL15-5); gamut presses Ctrl-C until the TUI exits, at most four times (TL15-6, live check pending); the architecture-loop skill matches practice (a rift, a counsel round and a merge per batch; triage step; baseline and coverage pathspecs fixed, they matched 0 files), the safety rules live in both prompts, `rejected.md` carries the passes 13-15 rejections (TL15-2 to TL15-4); the steering guard reads `.claude/skills` (decided by encode-lessons-in-structure). Counsel P2: the root gets credit for peers a workspace's used dependency asks for, red first; a late quit press reaching an idle shell rejected (the relaunch starts after the loop). Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Guard          | done `2680d04e` … `ca4aa4b4`, merged `fc9f214b`: one rule for the command word after an unnamed option, `subcommandPositions` deleted (R1, −21 lines); a risky command asks when a word before `--` is known only at run time — unquoted, `"$@"`, or quoted and not the value of an option the table names (G1, counsel P1 widened it to quoted words: quoting keeps a word whole but the command still reads `-rf`); shell `alias` values read as scripts, `hash -p` asks (G2); SQL asks unless each statement starts as a read or `INSERT`, with overwrite words (`UPDATE`, `OR REPLACE`, `OUTFILE`, `lo_*`, `pg_file_*`, `dblink*`) asking anywhere (G3; a plain `INSERT` loses no data); `truncate`, `shred`, `rimraf`, `dropdb`, `rsync --delete`, `crontab -r`, docker prunes, git plumbing (G4); unreadable `xargs` input before `--` of a risky command asks (counsel P1). Decided by fail-closed. Differential over 2,223 commands: 111 toward asking, 0 away. Accepted over-asks, named in the tests: `rm "$f"`, `kill $PID`, `cp $a $b`, `psql "$DATABASE_URL"` (the `--dbname=` form stays safe), `find … \| xargs rm` (`xargs rm --` stays safe), `CREATE TABLE`, `lo_get`. Main gate: the known `runProcess` flake once, green on re-run. Probes red for each fix                                                                                                                                                                                                                                                                                         |
| Core           | done `9f70255b` … `21986d35`, merged `9b85fc2f`: `loopOpen` hooks run beside a turn resumed at open, not after it (C15-1); an unsettled call after a restart reads that no result was recorded; one turn-record write path with a failure policy (`writeTurnRecord`); a turn's cost is whole or absent and a summary is priced by its own model; one session route for the turn, the snapshot and the auth gate (`resolveSessionRoute`); the Date line is read per turn (`dateSection(now)`); an Anthropic refresh never returns a stale credential (X15-2). Counsel P2: a refresh could overwrite a newer sign-in; the write-back now keeps only when the store holds the credential whose token was refreshed, or still equals the pre-refresh read, or was empty before and after (decided by fail-closed), red first on the B-signs-in-during-A's-refresh ordering. Probes red for each fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Live check     | gamut `sol-luna` on main `f5a0df70` through herdr (Anthropic still blocked). Sound: the fixture reached 18 pass, 0 fail; the parent asked "what is still running?" after a Ctrl+C that stopped its children named the stopped workers (LV-4); the `/btw` pane showed the answer with the trays hidden, Esc brought them back (LV-5); the fork made 0 tool calls; `gamut restart` quit and resumed; the guard asked on `rm -f` and `kill $pid` (declined). Found: after the interrupt, the stop notice's "Start a new child for a task that still needs doing" led the model to restart four workers unasked; the resume notice started a turn that tried to rerun `bun test --watch` (C15-3, as predicted). Both go to `p15-ext`, decided by the C15-3 rule: a notice never invites a rerun the user did not ask for. Baseline for X15-1: 41 stored `@gent/btw` pulses for a one-sentence fork answer, 24 `@gent/delegate`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Extensions     | done `c65c4a87` … `0453508c`, merged `83851bcb`: `/btw` pulses only when the fork's view changes and throttles streamed text to 250 ms with a final flush (X15-1: a 200-chunk answer stores 3 pulses); only the loop-open reconcile re-sends a child's start (X15-3); `forkQuestionBody(text)` (X15-4); an alarm at the current second is now (X15-5); a worker fault before its first cell ends the worker (X15-6, narrowed: after a cell a stray fault may be cell code); an interrupted background job on resume is a notice the next turn reads, not a turn (C15-3; additive nullable column `background_bash_jobs.notice_read_at`); `ShownNotices` in the public extension API with three consumers (delegate, wake, exec-tools), wake now clears only notices an answered turn showed (W); the stop notice and the resume notice tell the model to report, and restart only when the user asks (live check); `hasProjectScope` exported, `hasProjectExtensionScope` deleted (C15-10); every key and paste disarms the quit arm through an input watch (T15-2); a notice-row source releases history after 5 s and keeps its late rows (T15-3); test-only moves (T15-4, T15-5); the goal partial-usage test waits for Idle (gate flake). Counsel P2s, red first: the column migration survives two processes racing it; no backfill, so an old interrupted job is shown once more, never dropped (decided by fail-closed); a slow source is not failed at the bound, only stops holding history (decided by bound-every-wait on the hold). Probes red for each fix |
| Live check 2   | gamut `sol-luna` on main `05bdf94c` through herdr. Sound: a detailed `/btw` answer of 1,884 chunks stored 133 `@gent/btw` pulses (one per ~14 chunks; before: about one per fork event) and the pane showed the full answer, 0 fork tool calls (X15-1); a wake `at` the current second fired (X15-5); after a Ctrl+C that stopped Tasks 5-6, "what is still running?" named them and restarted nothing; `gamut restart` with a background `sleep 900` started no turn, "what happened to the background job?" reported it stopped and said it would restart only if asked, the next reply no longer raised it (C15-3); `delegate.list` three times. The fixture ended 15 pass, 1 fail: the fail is Task 5, stopped by the interrupt. No defect found                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Kernel         | owner request 2026-09-24 ("the RLM kernel … persist between handoffs in a thread … a scratchpad for long sessions"). done `5b2523c7`, `1f00d826`, merged `69f4318d`: a `/handoff` session's opening branch starts with its predecessor's cell namespace, copied once into its own `cell_namespaces` row at first kernel start (not live keying by thread: going back to the old session never sees the new one's writes; decided by derive-dont-sync and fail-closed on shared state); the restore report names `previousSession`; delegate children, `/btw` forks and other branches start empty; the cell reads the parent ids through `ExtensionContext.Session` (no new core concept, no stored-format change). Reset saves an empty namespace, `CellNamespaceStorage.clear` deleted. Counsel P2s, red first: the first start always writes the branch's own row (inherited or empty), so a predecessor's later save is never inherited; only the session's oldest branch inherits (`listBranches`); a worker is kept only after its restore succeeded, so a failed session lookup retries on the next cell. Open: the compaction bindings note reads only the branch's own row, so before the handoff's first cell it lists no bindings                                                                                                                                                                                                                                                                                                                            |
| Live check 3   | gamut `sol-luna` on main `1b1bcfce` through herdr: a cell stored `scratch`; `/handoff` (accepted in the TUI) opened the new session; its cell printed `scratch` with `restored: { restored: ['scratch'], omitted: [], previousSession: <old id> }`; a delegate child's `typeof scratch` reported `undefined`. No defect found                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Skills listing | measured ($0 Codex `gpt-5.6-sol`, headless, two prompts, each mode once): first request 8,905 → 5,162 input tokens (−42%), turn totals 52,956 → 32,647; both listings found the right skill, compact read a wrong skill first once and corrected itself in the turn. done `6e8c91aa`, merged `f090d79d`: the compact listing is the only listing; `GENT_SKILLS_LISTING`, the full format and the env-guard row deleted (−82 lines, one concept). Decided by guard-the-context-window, progressive-disclosure, subtract-before-you-add                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## Pass 16 (sweep of main at `a2a39443`)

Verdict: not polish. Reports: `~/.cache/gent-pass16/pass16-{core,extensions,guard,tui,tooling,efficiency}.md`. Pass-15 changes held everywhere (guard differential over 2,239 strings: 151 toward asking, 0 away; core suite 1,066 pass). No P1. P2: EF16-1 per-turn notices (stopped children, wake fires, interrupted jobs) sit in the system prompt, so each one appearing or clearing loses the whole prefix cache, conversation included (15-26% of list-price spend in the gamut runs, which interrupt on purpose); X16-1 branch state files (wakes, delegates, goals) ignore `GENT_DATA_DIR` (192 of 198 wake files and all 38 delegate files in the owner's `~/.gent` belong to isolated runs; a run on a copy of the owner's database would fire the owner's alarms); C16-1 a compaction summary that fails after its model call spent tokens drops that spend while the turn reports a whole cost; G16-1 an alias used after its definition (`alias w=rm; w -rf X`) and G16-2 brace-expanded flags (`rm {-rf,X}`) are rated safe. Tooling: five guard blind spots (TL16-1 lint overrides that suppress nothing, TL16-2 test helpers and scripts outside the dead-export guard, TL16-3 one admission covering the whole session service, TL16-4 platform layers by name only, TL16-5 one-line-import rows). Efficiency baseline: median per-step input 12.9-14.0k, cache read 70-79% of input; prompt sections (first request, no project file) about 410 core, 1,040 tool guidelines, 1,630 skills, 1,475 cell and host tools.

Decisions:

- EF16-1: notices go after the cache boundary, as an unstored block after the last message, and the Anthropic tail marker stays on the last real block; the system prompt stays byte-identical while notices come and go. Decided by guard-the-context-window and fix-root-causes (the placement is the cause, not the notice). Whether `ShownNotices` moves into core follows from the new placement by the deletion test; the extensions and core sweeps both call it a small deep module today.
- `/btw` live-only pulse: rejected again (133 pulses stored 21 KB beside 266 KB of the fork's own chunks; 0.15% of the owner's event bytes).
- Empty `cell_namespaces` rows: kept (about 60 B, deleted by cascade, they carry the fixed start state).
- X16-1: the state-file root follows the data directory (one `Config` read; unset keeps `~/.gent`, no migration). The stale files already in `~/.gent/wakes` and `~/.gent/delegates` stay; deleting the owner's files is an owner call.

### Pass 16 triage

| Batch      | Rift          | Items                                                                                                |
| ---------- | ------------- | ---------------------------------------------------------------------------------------------------- |
| Core       | `p16-core`    | EF16-1 (notices after the cache boundary, with delegate, wake, exec-tools producers), C16-1 to C16-4 |
| Extensions | `p16-ext`     | X16-1 to X16-3, TL16-4 (extension-provided `BunServices.layer`)                                      |
| Guard      | `p16-guard`   | G16-1 to G16-7                                                                                       |
| TUI        | `p16-tui`     | T16-1, T16-2, the TUI "not worth a pass" lines                                                       |
| Tooling    | `p16-tooling` | TL16-1 to TL16-3, TL16-5 to TL16-8                                                                   |

### Pass 16 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TUI        | Merged `f1aa0968`. T16-1 `PickerFrame` squeezes from a measured height (drops hint, then title; `usePickerBodyRows()` via `@gent/tui/extensions`); T16-2 notice-row warning only for pending sources, `noticeRowsHoldKey` removed; cleanups (`parseFileRefs` deleted, tray relies on `DockProvider`, `stickyStart` only when sticky). Counsel 1-3 OK; 4 (short-terminal overlap) fixed in `ee5abded`: agents pane drops detail, headings, filter to keep the cursor row. New finding queued: agents pane cursor re-anchors on each 2 s poll.                                                                                                                                                                                                                                                                                               |
| Core       | Merged `f47f3483`. C16-1 driver precedence folded into `resolveSessionRoute` (`resolveAgentDriver`, `DriverSource`, `ResolvedTurn` deleted); C16-2 failed admitted summary leaves `costUsd` absent; EF16-1 per-turn notices ride after the cache boundary as one unstored trailing message (`TurnProjection.notices`, `readNotices`, `ShownNotices` deleted); counsel fixups: heading "Host status for this turn, not a message from the user." and blank notices dropped (`90953e78`).                                                                                                                                                                                                                                                                                                                                                    |
| Guard      | Merged `2dd12794`. G16 classifier: alias values rated where the name is used (`aliasUseView`, `value "$@"`); only unquoted braces count as run-time words (`ShellWord.braces`); `RISKY_PARENTS` run-time child rule for every rated parent; find primaries incl. `-fprint*`/`-fls` read as redirects; PS0/PS1/PS4/BASH_ENV/ENV substitutions and PROMPT_COMMAND read; kubectl, terraform/tofu, docker compose deletion rows. Differential over 2,482 strings: 61 toward asking, 5 away (the named G16-6 find cases). Gate 0.                                                                                                                                                                                                                                                                                                               |
| Tooling    | Merged `2d8494ef`. TL16-1 override-off probe (`probeConfig` keeps every key, reads `code`/`rule_id`, unlabeled diagnostics fail, inner timeout); TL16-2 dead-export scan of test helpers, scripts and gamut; child-session writer check moved from a text guard to the oxlint rule `gent/child-session-writer-admits` (innermost function of any form, 5 invalid fixture cases); retired cell path row catches any core file or directory naming the cell; skill pathspecs; gamut `status` prints stored pulses per extension. Gate 1 on the `runProcess` flake only; core re-run 1067 pass. Flake probe: 0 hangs in 30,000 runs at load 15-18, 2 hits in 3,200 at load ~52 while parallel rift gates ran — load, not a code defect; assertions kept. Agent used two non-destructive python3 heredocs (rule breach, reported).             |
| Extensions | Merged `3c8cc36d`. X16-1 `resolveDataDir(home)` moved into core (gent-platform.ts, exported from extensions/api and host); SDK `dataPaths` and the branch state store follow `GENT_DATA_DIR`; an empty value removes the file; anthropic yields the host FileSystem/Path/ChildProcessSpawner (`AnthropicDriverServices`). Counsel fixups: a read treats `NotFound` as empty behind the `exists` fast path (a catch-only read slowed every turn and timed out the cell recovery test 3 of 7); comments say why auth and the fff cache stay outside the data dir. Gate 0. Open: `PLATFORM_LAYER` widening (TL16-4), `Crypto` in `ExtensionLoaderServices`, TUI Bun layers.                                                                                                                                                                   |
| Live check | herdr gamut sol-luna on main `b26c850a`. Six children spawned (fifth start refused by the 4-child cap), all reported; wake alarm fired and its file lived in `<data>/wakes/` and was removed after the fire, none in `~/.gent/wakes`; guard probes: alias-hidden `rm -rf`, `PS0` substitution and `kubectl delete` asked (declined, blocked), `alias r=rm` and quoted-brace `rm` ran with no ask; follow-up turn read 23,296 of 23,656 input tokens from cache (notices after the boundary); Ctrl-C stopped the running child and the next turn reported it without restarting it; agents pane fits at 43 columns. Finding D17-1: the cascade missed Task 6, whose turn the parent had started with `session.send` after its delegate run settled — batch p17-deleg. Polish: the alias ask reason shows `$@` rather than naming the alias. |

## Pass 17 (sweep of main at `4508c92f`)

Verdict: not polish; guard blind spots found (TL17-1 to TL17-6, platform layers outside the root), so this pass is not the last. The sweeps ran on the owner's Mac; their reports stayed there, and every item below is re-verified in the source before its fix. The work moved to the Linux devbox at `4508c92f`: the build hard-coded `bun-darwin-arm64` and failed on Linux (the darwin `@opentui` native package is not installed there), so `5c22179c` compiles for the host, decided by use-the-platform. The efficiency sweep was stopped before it finished; it runs again this pass.

Findings (P2 unless marked):

- TUI: the agents pane cursor re-anchors on every 2 s poll (`SelectList` `on(values)` with new row identities); TUI17-1 the short-terminal rule lives only in the agents pane (`/thread` at 11-13 rows, filtered `/model` and `/think` at 11-12 rows overlap); Bun layers provided outside the platform root (workspace.tsx, extensions/host.tsx, builtins.tsx); TUI17-2 nine comments that tell history (P3).
- Delegate: D17-1 (seen live) a parent interrupt leaves running a child turn that the parent opened with `session.send` after the child's delegate run settled.
- Extensions: PX17-1 turn notices reach the OpenAI-compatible drivers as a trailing `system` message (Mistral rejects it, so every later turn fails); X17-1 a job completion or wake fire is lost silently when the follow-up queue is full; CX17-1 a cell binding with a throwing getter or a trapping Proxy kills the worker at the snapshot; TX17-1 a session started in `$HOME` loads `~/.gent/AGENTS.md` twice every step; P3 stale docs, three duplicate driver-precedence tests, two unloaded examples, a bare block.
- Guard: G17-1 a variable after an unnamed flag reads as a subcommand (over-ask); G17-2 brace words such as `cp f{,.bak}` count as flags (over-ask); G17-3 `docker exec`, `docker compose exec` and `kubectl exec` inner commands are rated safe (fail-open); unpinned xargs-git, kubectl and compose option tables.
- Tooling: `PLATFORM_LAYER` catches layers by name only; `Crypto` in `ExtensionLoaderServices` unchecked per composition root; TL17-1 `.oxlintignore` `.tmp-*` row matches nothing; TL17-2 two dead root lint offs the probe skips; TL17-3 the SAFETY rules are missing from the skill; TL17-4 the dead-export scan skips `examples/`; TL17-5 no guard ties the Effect version across the catalog, overrides and patches; TL17-6 stale comments.

Decisions:

- D17-1: the sender owns the record. `session-tools` records each send (child session, message id, sender branch) at send time and reacts to its own interrupted turn: it dequeues a still-queued send, otherwise stops the child turn by message id, and writes a notice only for a stop that took effect. The record lives in memory unless the apply agent shows a child turn outlives the process. Decided by locality (the send and its undo in one module) and subtract-before-you-add. `SessionMessageDetails.from` gains an optional `branchId` (additive).
- PX17-1: one owner for the `<host-context-update>` wrap, shared by the Anthropic and OpenAI-compatible drivers. Decided by fix-root-causes.
- X17-1: a lost completion becomes a turn notice. Decided by acknowledge-before-processing.
- Item order: the `PLATFORM_LAYER` widening lands after the TUI batch moves its Bun layers, as a second tooling round from main. Decided by migrate-callers-then-delete-legacy-apis.

### Pass 17 triage

| Batch        | Rift           | Items                                                                                                                 |
| ------------ | -------------- | --------------------------------------------------------------------------------------------------------------------- |
| TUI          | `p17-tui`      | agents pane cursor, TUI17-1, TUI Bun layers, TUI17-2                                                                  |
| Delegate     | `p17-deleg`    | D17-1, delegate.ts state-file doc                                                                                     |
| Extensions   | `p17-ext`      | PX17-1, X17-1, CX17-1, TX17-1, P3 docs, tests, examples                                                               |
| Guard        | `p17-guard`    | G17-1 to G17-3, xargs-git and option-table pins, differential                                                         |
| Tooling      | `p17-tooling`  | `Crypto` roots, TL17-1 to TL17-6; then `PLATFORM_LAYER` widening after `p17-tui`                                      |
| Efficiency   | none           | re-run of the stopped sweep (tokens per turn, cache prefix stability, per-turn runtime work, TUI polls, gate time)    |
| TUI features | `p17-tuix`     | owner request: the last user message as a sticky top row; ← opens the agents pane with Claude Code style rows; EF17-3 |
| Efficiency   | `p17-eff`      | EF17-1, EF17-2 (fresh child), EF17-4, EF17-5, EF17-6 (bound layer), composer tests writing into `~/.gent`             |
| Tooling 2    | `p17-tooling2` | `PLATFORM_LAYER` widening                                                                                             |

Efficiency baseline (report `~/.cache/gent-pass17/pass17-efficiency.md`, captured request bodies with scripted replies, $0): first Anthropic request about 8.6k tokens (project instructions ~3.4k, skills 1,652, tool guidelines 1,101, host tool list 1,263); prefix byte-identical across steps, turns, processes and sessions; EF16-1 holds. No owner database on the devbox, so shares of real spend are estimates. Decisions: EF17-1, EF17-2 fresh-child reorder and marker, EF17-4, EF17-5 and the EF17-6 bound layer change directly (decided by guard-the-context-window and prove-it-works); EF17-3 joins `p17-tuix`, which owns the pane poll. Proposals for the owner, not acted on: a fork keeps the parent's system prompt verbatim and names its narrower tool set in the task (~$0.03 per fork); an OpenAI child sends the root session's `prompt_cache_key`; the cell `inspect` output format (EF17-6, needs the owner database).

### Pass 17 results

| Batch        | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Guard        | Merged `a7e87210`. G17-3 container and pod commands are read: `docker exec`/`run`, `docker container exec`/`run`, compose `exec`/`run`, `kubectl exec`, and (counsel) `--entrypoint` as the command word and `kubectl debug … --`; `run` rows added because a bind mount is the same hole. G17-1 `readingsAt` deleted: option tables gain `flags` (options taking no value), a run-time word after a listed flag is an operand, and after an option the table does not name the next run-time word still asks (counsel: `npm --omit dev "$CMD"`). G17-2 a brace word is a run-time option only when it starts with `-` or `{` or makes over 256 words; `braceWords` expands comma lists, nesting and sequences so the risks read the words a brace makes (the literal rule alone opened `git checkout src/{a,b}.ts`, `cp x/.env{,.bak}`, `gh api -X DEL{ETE,}`, `git worktree re{move,} -f`). The alias ask names the alias. Pins: xargs git, kubectl and compose option tables (both live). Differential over 1,576 strings against the base: 28 toward asking, 34 away (G17-1, G17-2 and `git --no-pager commit -m push`, removed from the accepted over-asks), 2 level-only (`.env{,.bak}` now sensitive). Residual: an unnamed valued option before a run-time word asks; Podman not covered. Flakes: wake alarm, btw pulses (load 36-41). Gate 0.                                                                                                                                                                                                                                                                   |
| Tooling      | Merged `4722773d`. `Crypto` in `ExtensionLoaderServices` (the runtime already supplied it; the type left it out; tests through the loader, E2E and `Gent.server` roots). Test temp directories left the repo (13 moved to scoped system temp dirs) with the guard `findRepoTempDirectories` (counsel: any tmp spelling, any temp call rooted in the repo). The lint-off probe covers the root rules block; `typescript/no-base-to-string` and `typescript/await-thenable` offs removed as dead. One SAFETY block in the loop prompts, held equal by `findSafetyBlockDrift` (counsel: bounded by the prompt fence, blank lines, trailing space); SKILL.md names a git worktree as the fallback when rift has no reflinks. The dead-export scan reads `examples/` (`AddNoteTool`, `SessionNotesSummary` unexported). `findEffectVersionDrift` ties catalog, overrides, patches and manifests to one exact `catalog.effect`; the SDK takes `@effect/opentelemetry` from the catalog; `patches/README.md` documents all three patches. Stale gamut and lint comments fixed. Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| TUI          | Merged `a9de797d`. The `SelectList` cursor follows its entry by `rowKey` once the reader moves it, and clamps only when the key is gone (the 2 s poll no longer re-anchors it); a key two rows share resolves to the nearest row, and prompt search keys repeated prompts by occurrence (counsel). One short-terminal owner: `PickerFrame` has a `detail` slot it drops first, then `SelectList` drops headings, then the filter or query row; the palette and autocomplete budget their query line through `queryRow` and `PickerHost` (counsel); the agents pane's `paneLines`, `rows(headings)` and inner `Body` and the `usePickerBodyRows` export are deleted. The client runtime and git watch read the root's platform services (`BunSocket.makeNet` stays: a connection constructor, not a layer). A slash command sent before the client extensions load waits for the load, then runs (counsel, decided by acknowledge-before-processing). History comments rewritten. Gate 0 after one wake flake retry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Extensions   | Merged `be953e28`. PX17-1 the OpenAI-compatible drivers wrap the trailing turn notices as `<host-context-update>` user messages (`withHostContextUpdates`, providers.ts owns the format and the Anthropic test pins the SDK's wrap to it); a system message inside the conversation keeps its role (counsel). X17-1 a refused background completion or wake fire becomes a turn notice and a log line; new nullable column `undelivered_at` on the background job table (additive), one writer `recordDelivery`, and a replay that delivers clears it (counsel). CX17-1 a cell binding with a throwing getter or trapping Proxy is omitted as unsupported and the worker lives (property descriptors read, accessors never called). TX17-1 a session in `$HOME` reads `~/.gent/AGENTS.md` once (`hasProjectScope` exported). P3: state-file and driver docs, three duplicate precedence tests deleted, `hello-tool.ts` deleted (session-notes covers it), `prompt-rules` gets a load test, bare block removed. Open: the `boundedNotice` comment says `context.read` pages background output; unverified. Gate 0. The wake alarm test asserted the first turn went idle before a 0.3 s alarm; it now asserts the queued wake follows the first answer (`a06051aa`).                                                                                                                                                                                                                                                                                                                                                      |
| Delegate     | Merged `b2850aae`. D17-1: `session-tools` owns the cascade for turns its `session.send` opened, and delegate keeps the cascade for delegate runs (the deletion test showed neither covers the other; decided by locality). `session.send` delivers by steer, which `dequeueFollowUp` cannot take back, so core gained `Session.stopMessage` (new persisted actor operation `StopMessage`, additive; `messageId` left `Session.stop`): it takes back a waiting steer, cancels an unstarted turn or stops a running one, and answers whether it reached anything. A step's join and a stop's take-back decide under one permit (counsel race). The in-memory send record keys by child and message, keeps a record whose send failed, leaves with deleted sessions, and writes the "Stopped child turns" notice only from the stop's own answer (counsel). `SessionMessageDetails.from.branchId` optional (additive). `interjectionMessageId` public. Accepted limit: after a restart a resumed correction turn is not in the record; the turn that sent it died with the process and the user can stop the child from the agents pane (decided by subtract-before-you-add). Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Tooling 2    | Merged `36bf0bc6`. `PLATFORM_LAYER` widened: every `@effect/platform-bun` layer provision outside the platform roots is a finding, read from names bound to the package (static, dynamic and destructured imports, aliases), across split lines, and at re-exports (counsel: no false finding on a local `Bun*` name). Roots are a file-to-reason map (build.ts added); one allowance: openai.ts `BunHttpServer.layerServer` for the OAuth redirect listener (a user extension may do the same). Callers: storage, anthropic and openai read the host's `Crypto`; `SqliteStorage.TestWithSql` left shipped code for `testSqliteStorage()` in test-utils (about 100 test call sites). Known misses of a text scan: `.then(m => m.layer)`, computed access, default imports (none in the tree). Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| TUI features | Merged `125e25a7`. Owner request. ← on an idle composer (empty draft, editing mode, no overlay, transcript not expanded, no interaction) opens the docked agents pane; ← or Esc closes it, → or Enter opens a row. A bare keybind fires only when the composer is idle, gated at the command owner, so any extension can bind it. Pane rows read like Claude Code's: glyph, `name · activity`, time on the right, running first; rows pad by display width (`fitWidth`, counsel; thread view had the same bug); a running row shows the current turn's time (`SessionRuntimeState` Running/WaitingForInteraction carry `startedAtMs`, `AgentRowEntry.runningSince` optional, counsel). The sticky row `↑ <first line>` pins the reader's last prompt above the live tail while it is off screen: `readerPrompt` decides from metadata (client origin, or a message renderer's generic `prompt` option, which /btw uses), so a parent's send, a wake or a delegate start never pins (counsel); rows stored before `fromClient` do not pin; it is a memo of the items and a measurement reads no history (80,000 reads per 40 growth steps → 0). EF17-3: the closed tray reads only the current session's subtree (`getSessionTree` recursive CTE, optional `root`; 2,001 sessions: 63.6 ms and 591 KB per poll → 3.6 ms and 306 B); `Session.listSessions` became a function taking `{ root? }`. Gate 0.                                                                                                                                                                                                                  |
| Efficiency   | Merged `42c4ebf0`. EF17-1 operation cost from a table (0 mismatches over all code points): a 32-op `getSnapshot` 3,361 → 52 ms, `message.test.ts` 10.8 → 0.7 s. EF17-2 the system prompt is two blocks: a shared block and an agent block (`AGENT_PROMPT_PRIORITY`; the children section, the tool list, the tool guidelines and the cell guide go in the agent block, counsel), and the Anthropic API-key driver marks the shared end (3 of 4 markers, minimum 4,096 chars). Parent and fresh child share 22,162 chars (~5.5k tok, was 792 chars). Limit: a child with a different tool set still misses Anthropic's cache, because tool definitions come first. EF17-4 the lint-offs check runs as `lint:offs` beside oxlint and in a parallel read-only hook group, and fails closed on a crashed or partial probe (counsel); tooling tests 20.7 → 1.3 s. EF17-6 a bounded tool result keeps its shape, with long strings cut head and tail, encoded once (1.105 → 1.059 wire chars per source char), and the whole wrapper fits 8,000 chars (counsel: it was 8,129-10,958). Composer tests spill into a scoped data dir; the test preload gives each test process a temp home (a preload `GENT_DATA_DIR` broke 45 tests that read `<home>/.gent`). EF17-5 (concurrent cell tests) was reverted after flaking on two attempts; it stays open. Proposals for the owner: two relocated blocks with a marker on the Anthropic OAuth path (needs a sign-in probe); fork keeps the parent prompt; OpenAI child `prompt_cache_key`. The real `~/.gent/shell-output` holds 222 stray test files, left for the owner. Gate 0. |
| Live fix     | Merged `2bf4e83a`. Seen live on the headless devbox: the OpenAI device-code sign-in ended when the browser open failed. A failed open is now `AuthEvent.BrowserUnavailable`: the flow keeps running and the pane shows the URL (`browserUnavailable`); the device instructions put the code on its own line. Found in the same run, fixed in `p17-live2`: the idle event stream ends about every 90 s, and each reconnect reloads auth and resets the sign-in pane (the code was lost). Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Pass 18 (sweep of main at `4c1e19a0`)

Verdict: not polish; guard blind spots found (G18-1 to G18-5, G18-11 fail-opens; TL18-1 the SAFETY text an agent receives is not the text the guard checks), so this pass is not the last. Core proper is polish only. Reports: `~/.cache/gent-pass18/pass18-*.md`.

Findings (P2 unless marked):

- Guard: G18-1 `docker --tls` swallows the subcommand (a flag that prefixes a valued name); G18-2 an empty `--entrypoint`; G18-3 compose splits `--entrypoint` into words; G18-4 an unknown valued option before `--entrypoint`; G18-5 `docker create`, podman, nerdctl, `oc exec`/`oc rsh`, `docker service create`; G18-11 runner rows missing (tmux, screen, sshpass, at, hyperfine, nsenter, gosu…); G18-6 over-ask: the pass-17 container tables name no flags; G18-7 reduction: the xargs/parallel literal-input simulator (~400 lines); P3 G18-8 sudo run-time over-ask, G18-9 alias use read as `value "$@"`, G18-10 dead `KUBECTL_DEBUG_OPTIONS`, a derived terraform row.
- TUI: TUI18-1 (pass-17 regression) typing in the `/` popup after arrowing leaves the cursor on the wrong row; TUI18-2 (pass-17 regression) the pinned prompt row makes the live tail one row too tall; TUI18-3 a docked pane replaces the boot branch picker, so `-p` sends into an unchosen branch; TUI18-4 prompt search and the fork picker are the last modal overlays and show no rows at 9 rows; P3 TUI18-5 an unresolved slash command drops the text, TUI18-6 a printable bare keybind, TUI18-7 banners naming files that do not exist, TUI18-8 `showInput`, TUI18-9 `PickerHost` repeats `DockProvider`; the auth pane draws the closed provider list (seen live).
- Extensions and core: X18-1 (C18-1, EF18-1; the open `boundedNotice` item verified false) a background job's notice points `context.read` at the start stub, and the full output is readable nowhere; CX18-1 a thrown value with a throwing getter or a Proxy kills the cell worker; D18-2 the delegate cascade can settle a finished child as stopped; GL18-1 a goal created mid-turn is charged the whole turn; P3 D18-1 two stop notices for one child, D18-3 a child-path failure skips the parent cascade, SS18-1 joined-steer records never drop, C18-2 the bound runs twice per step, C18-3 docs show `stop({ messageId })`, C18-4 two stub tables in the harness, C18-5 the sessions section below the agent boundary and a bare `112`, C18-6 compat drivers get two leading system messages, EF18-3 every cell result lists every binding; R18-1 `Session.stop` loses its last product caller after D18-2.
- Tooling: TL18-1 three owners of the SAFETY text, counsel has none, and the filled brief already drifted; P3 TL18-2 `patches/README.md` names the wrong file, TL18-3 relative and `process.cwd()` temp dirs, TL18-4 destructured Bun layers, TL18-5 guide code blocks fail repo diagnostics, TL18-6 e2e `spawnServer` leaks on timeout, TL18-7 unmatched `.oxlintignore` rows and tsconfig overrides, TL18-8 AGENTS.md describes `lint` as oxlint only, EF18-6 a fixed OAuth port in `openai.test.ts`.
- Efficiency: no regressions (first request 8,637 tokens, prefix byte-identical, fresh child reads 5,700 tokens from cache, bound exactly 8,000 chars, gate 115 s). EF18-2 69-char cell receipt ids, EF18-4 the open agents pane re-reads every session each 2 s, EF18-5 a fourth marker (~$0.003 per child): proposals, not acted on.

Decisions:

- G18-7: delete the simulator; literal input fed to a risky inner command asks, and a shell whose `-c` script comes from the input counts as named by the input. 31 strings move toward asking, 3 away. Decided by subtract-before-you-add (a fail-closed guard needs no simulator for the cases it would allow).
- G18-9: kept as it is: an alias of a risky command asking is the G16 choice, and the over-ask is rare. Decided by correctness-over-pragmatism.
- X18-1: the full output goes where a read reaches it; the job module owns that reader, not a new `context.read` case per tool, and the notice states one omitted count. Decided by fix-root-causes.
- D18-2 and R18-1: the cascade settles by `stopMessage`'s answer, called outside the lock. `Session.stop` stays: stopping a branch without a message id is a separate ability a user extension may need. Decided by small-interface-deep-implementation.
- TL18-1: one `safety.md` that every loop prompt tells the agent to read; `findSafetyBlockDrift` is deleted. Decided by derive-dont-sync.
- TUI18-4: prompt search and the fork picker dock as `PickerFrame` panes (owner rule: docked panes). The auth pane's frame waits for `p17-live2`.

### Pass 18 triage

| Batch      | Worktree      | Items                                                                                          |
| ---------- | ------------- | ---------------------------------------------------------------------------------------------- |
| Guard      | `p18-guard`   | G18-1 to G18-8, G18-10, G18-11, terraform row, differential                                    |
| TUI        | `p18-tui`     | TUI18-1 to TUI18-9 (auth pane excluded)                                                        |
| Cell       | `p18-cell`    | X18-1, CX18-1, C18-2, C18-5, C18-6, EF18-3                                                     |
| Delegate   | `p18-deleg`   | D18-1 to D18-3, SS18-1, GL18-1, C18-3, C18-4, core polish lines                                |
| Tooling    | `p18-tooling` | TL18-1 to TL18-8, EF18-6                                                                       |
| Live fix 2 | `p17-live2`   | the idle event stream ends every 90 s; a reconnect resets the sign-in pane; then the auth pane |

### Pass 18 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard      | Merged `6ce595af`. G18-1 an exact long flag takes no value and an exact valued name wins before prefixes. G18-2 an empty literal `--entrypoint` is dropped. G18-3 compose `run` and `docker service create` read the entry plus the words after the service as one script, words single-quoted (`Command.entryScript`). G18-4 the entry is read over the whole option span for each start. G18-5 the docker rows are keyed under podman and nerdctl (`under`), kubectl rows under `oc`, with `oc rsh`, `create`, `container create`, `service create`, and one `COMPOSE_ROWS` for `docker compose`, `docker-compose` and `podman-compose`. G18-6 flag lists on the container, pod and runner tables (uv, op, timeout, env, npx, bunx, strace, sudo, doas); the optional value skip was left out (fail-closed). G18-7 the xargs/parallel simulator is deleted (−630 lines); `InputUse` is `{marker, within, argFiles}`, a shell whose `-c` holds the input or that takes appended input counts as named by it, and `--rpl`/`--plus` make every word a holder (the deletion opened them). G18-8 `runTimeOptions` reads only the words before the first command start. G18-10 `KUBECTL_DEBUG_OPTIONS` and the terraform state row deleted. G18-11 rows for gosu, su-exec, nsenter, fakeroot, sshpass, xvfb-run, watchexec, screen, at, batch, hyperfine (`Operands`: each operand its own script) and tmux (with `\;` chaining); `--health-cmd` read as a script. The orchestrator probe (42 strings; counsel could not write probes) found `kubectl run … --`, `oc run … --` and `batch` open; fixed. Differential over 1,811 strings: 66 toward asking, 20 away (all G18-6/G18-8 over-asks), 1 level-only. exec-tools.ts 5,246 → about 5,100 lines. Flake: wake notify alarm (load). Gate 0. |
| Live fix 2 | Merged `ac9f79ba`. LV17-2: the cluster reaper passivated an idle watched loop after `entityMaxIdleTime` (1 min), which closed `watchRuntime`; with the 30 s capped backoff that was the 90 s period. `AgentLoopResidency` counts holds; the first turns `Entity.keepAlive` on, the last off, and a failed switch-on takes its count back (counsel). A running turn and each runtime watcher hold it. Counsel: a wake alarm on an idle unwatched branch died with the passivated scope; `ExtensionContext.Session.holdResident` (scoped, uniform, a no-op outside a loop) is held by each pending alarm and monitor. LV17-3: `agent` in the client is a memo, so a reconnect snapshot with the same agent no longer resets the auth pane or re-runs the auth gate. The reconnect backoff starts fresh after a stream that served (counsel). Background jobs (process scope) and delegate children (own entities) are not exposed. Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Tooling    | Merged `84869cdb`. TL18-1 one `.claude/skills/architecture-loop/safety.md`; apply, sweep, counsel and the live-check task point to it; `findSafetyBlockDrift` deleted; the steering path roots gain `docs/`, `patches/` and `.claude/`, so a missing `safety.md` fails the guard (the sweep's claim that it already did was false). TL18-2 `patches/README.md` corrected and read by the path guard. TL18-3 `process.cwd()` and relative temp roots. TL18-4 destructured and `?.` Bun layer access. TL18-5 the guide's code blocks compile with the repo diagnostics through `check-guide-code.ts` in the examples typecheck (it caught p17-live2's new block at merge). TL18-6 `spawnServer` fails at its ready bound (`Effect.race` returned the first success, so the bound never fired) and stops the server with its scope, and a server that outlives SIGTERM is force-stopped (counsel). TL18-7 unmatched `.oxlintignore` rows and tsconfig overrides are findings; two rows repeating `.gitignore` deleted. TL18-8 AGENTS.md. EF18-6 `OAuthRedirectPort` reference, 1455 in product, an OS port in tests. Flakes: two delegate tests under load. Gate 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Cell       | Merged `3f432473`. X18-1 a cut background notice names a file that holds the whole output (`<data dir>/background-bash/<session>/<branch>/<toolCallId>.txt`, absolute, written once with `writeFileAtomic`) for the read tool, and states one omitted count; the whole notice, file line and frame included, fits 8,000 chars (counsel); no `context.read` case and no seam (decided by subtract-before-you-add). CX18-1 one total error renderer in the cell worker; a property getter runs only when it is a host accessor taken from `Error.prototype` and Bun's `BuildMessage`/`ResolveMessage` prototypes at worker start, by identity (counsel: a `.bind()` getter prints as native code and looped forever). C18-2 the projection returns the stored messages without re-decoding, and a `WeakMap` memo bounds each part once per step (40 large results: the prompt share 6.8-7.8 s → 3-4 ms). C18-5 the sessions section at `AGENT_PROMPT_PRIORITY + 10`, children at `+ 12`. C18-6 compat drivers get one leading system message. EF18-3 a cell result names the bindings that cell bound, with `bindingCount` (additive, optional); 50 cells 16,410 → 4,272 wire chars; the TUI shows `bound: … · N bindings in all` and old rows unchanged (counsel). Open, next pass: the namespace snapshot encoder (`encodeSnapshot`, cell-protocol.ts) still runs cell getters, `toString` and Proxy traps, so a bound value can hang the next cell until the 30 s deadline replaces the worker. Output files are kept like the job rows (owner: cleanup or not). Gate 0.                                                                                                                                                                                                                   |
| TUI        | Merged `1f131f51`. TUI18-1 the popup resets its list when the composer's query changes, and `SelectList` resets only on a real change of `open` (a resource notification reset it by accident, which hid the bug). TUI18-2 the live tail budgets commits against `liveRows() - stickyRows()`. TUI18-3 while the boot branch picker or an enforced sign-in holds the slot, an opener leaves the state unchanged (`SLOT_OPENERS`). TUI18-4 prompt search and the fork picker are `PickerFrame` panes in the footer dock; their centred geometry is deleted; `ChromePanel.Root` remains for `auth.tsx` only. An overlay that takes the slot runs the replaced overlay's cancel (`cancelOverlay`), so a previewing prompt search gives the draft back, and a late event from a closed search is ignored (counsel). TUI18-5 an unknown slash command returns to its draft with the error; a held command waits for `commandsSettled` (client extensions loaded and the session's server list answered) and returns to its draft if the controller cleans up first. TUI18-6 a keybind whose key types one grapheme (or `space`) is refused and listed with the failed extensions, builtins included (counsel: `🙂` is two UTF-16 units). TUI18-7 banners and history comments. TUI18-8 `showInput` deleted. TUI18-9 `countedDock()` shared by `DockProvider` and `PickerHost`; `PickerFrame` takes `lines` or `height` and owns its note row; `pickerLines` deleted from the public entry. TUI tests 989 → 1,019. Flake: "an overlay that takes the screen mid-commit" once. Gate 0.                                                                                                                                                                                                              |
| Delegate   | Merged `13a3b88d`. D18-2 `stopChild` claims the row under the lock, calls `stopMessage` outside it and gives the row back on failure; a child whose turn completed first delivers over the claim from its own hook, or from `reconcile` after a restart (turnAfter runs while the phase is still Running, so the answer alone cannot tell). D18-1 one notice per child: a turn records the branch whose stop first latched it (`interruptFor`, `StopMessage.requester` optional, additive); a take-back is silent only when that same branch's earlier stop already stops the turn, and otherwise names the correction (counsel: a take-back during the user's stop was lost). D18-3 each delegate turnAfter step fails alone and logs. SS18-1 `TurnAfterInput.joinedMessageIds` drops the record of a joined steer; `settleSentTurns` reads only the ended sender's children. GL18-1 a turn started before the goal is charged no tokens and time from `createdAt` (`TurnAfterInput.startedAtMs`). C18-3 docs; C18-4 one stub table; core polish (`removeFromQueue`, `phaseHolds`, the dead steer id, a history comment). `stopNotices` reads under the lock. The wake notify alarm test waits for Idle. `Session.stop` kept with no product caller (R18-1). Residual: a requester keyed by branch, not turn, leaves a take-back silent when a child turn stays stopping across a whole parent turn. Flakes: queue drain order, delegate tests under load. Gate 0.                                                                                                                                                                                                                                                                                                                         |

Pushed `a62ebac9`; CI run 36135838749 green (annotation: `actions/checkout@v4` targets Node 20). Live check: not run; every gamut preset needs an OpenAI or Anthropic sign-in, and the device code expired unused (the reconnect fix held it on screen for its whole 15 min life with 0 reconnects).

## Pass 19 (sweep of main at `a62ebac9`)

Verdict: not polish; guard blind spots found (G19-1 to G19-8 fail-opens, TUI19-7 tests writing into a shared `/tmp` home, TL19-1 doc code blocks no check compiles, XR19-3 bundled skill Markdown no guard reads), so this pass is not the last. Reports: `~/.cache/gent-pass19/pass19-*.md`. Efficiency: no regression (first request 8,664 tokens, prefix byte-identical, background notice 8,000 chars with a readable file, gate 109 s).

Findings (P2 unless marked):

- Core: C19-1 (P1 past 272k input on GPT-5) the budget reads only `limit.context`, the estimate runs 16-18% low and never anchors on stored usage, and a provider overflow rejection ends the turn with no compaction, so every later turn fails the same way; P3 C19-2 the `startedAtMs` doc, C19-3 two stale actor docs.
- Extensions: CX19-1 (pass-18 regression) a DOMException renders as "Error"; CX19-2 a cell declaration named like a Bun global is not a binding and `const tools` breaks the namespace; PV19-1 a failed OpenAI browser wait drops the pending login, so a pasted code fails; XR19-1 an unwritable skills cache fails every turn; XR19-2 a multi-step `/btw` reply stays on step 1; BG19-1 a background job's whole output stays in server memory until exit; P3 CX19-3, CX19-4, PV19-2 Mistral `developer` role, XR19-3 skill Markdown no guard reads, XR19-4 history comments.
- Guard: G19-1 an unnamed valued option after `--entrypoint` hides the command (G18-4 incomplete); G19-2 tmux `;`-ended words and command prefixes; G19-3 tmux arguments that are tmux commands (`if-shell`, `run-shell -C`, `confirm-before`, hooks, `bind-key`); G19-4 `send-keys` joins keys with no separator; G19-5 screen `-X` takes no value; G19-6 hyperfine `-L`/`-P` substitution; G19-7 podman JSON entrypoints and nerdctl repeated entrypoints; G19-8 45 runners and remote-exec tools still read as data; P3 G19-9, G19-10 over-asks; missing risk rows (`podman system reset`, `helm uninstall`, `kubectl apply --prune`).
- TUI: TUI19-1 at 7-10 rows docked panes and the palette overprint; TUI19-2 (pass-18 regression) the backoff resets before anything arrives, so a feed that fails at once retries every second; TUI19-3 Ctrl+C over a host pane quits and leaves the turn running; TUI19-7 render tests use `home="/tmp"`; P3 TUI19-4 composer keys behind the fork and branch pickers, TUI19-5 a paste during prompt search, TUI19-6 `pickerHeight` counts one row too many, TUI19-8 a failed slash list is never retried.
- Tooling: P3 TL19-1 doc code blocks in AGENTS.md and apps/tui/AGENTS.md are wrong and compiled by nothing; TL19-2 the `Session` verb list; TL19-3 `safety.md` and the efficiency capture method disagree; the CI checkout action on Node 20.

Decisions:

- G19-8: one rule, not 45 rows: in a command the table does not name, a later word that names a command the guard reads also starts a command (mutant m2: 54 toward asking, 0 away, no expectation broken); rows only for tools that take a quoted script (`vagrant ssh -c`, `gcloud … --command`, `nodemon --exec`, `entr -s`, `ansible -a`, `expect -c`, `autossh`, `podman machine ssh`). Decided by encode-lessons-in-structure: each pass found more runner rows; the rule closes the class.
- G19-4: `send-keys` keys are read joined with no separator as well as with spaces; keys sent across two calls stay an accepted residual. Decided by correctness-over-pragmatism.
- Risk rows for `podman system reset`, `helm uninstall`, `kubectl apply --prune` join the guard batch (a guard that misses destructive commands is in scope).
- C19-1: an additive optional `inputLimit` on the model, a budget of `min(inputLimit, context) − reserves`, an estimate anchored on the last stored usage, and a classified overflow before any output hands the window off once and retries the step. Decided by fix-root-causes and derive-dont-sync.
- BG19-1: job output streams into the spill file as it arrives; memory keeps a bounded head and tail. Decided by guard-the-context-window and use-the-platform.
- TUI19-3: Ctrl+C dismisses a held pane; the boot branch picker and an enforced sign-in keep exit. Decided by experience-first.

### Pass 19 triage

| Batch      | Worktree      | Items                                                                   |
| ---------- | ------------- | ----------------------------------------------------------------------- |
| Cell       | `p19-snap`    | the snapshot encoder runs no cell code (Pass 18 open item), CX19-1 to 4 |
| Guard      | `p19-guard`   | G19-1 to G19-10, the m2 rule, script rows, risk rows                    |
| Context    | `p19-ctx`     | C19-1 to C19-3                                                          |
| Extensions | `p19-ext`     | PV19-1, PV19-2, XR19-1, XR19-2, XR19-4, BG19-1                          |
| TUI        | `p19-tui`     | TUI19-1 to TUI19-8                                                      |
| Tooling    | `p19-tooling` | TL19-1 to TL19-3, XR19-3, the TUI19-7 guard, the CI action              |

### Pass 19 results

| Batch      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Guard      | Merged `b08ff8ff0`. G19-8 the m2 rule: a command the table does not name, and that is not in `DATA_COMMANDS`, runs a later word that names one (`unnamedRunnerCommands`), so poetry, pipenv, bundle exec, lxc, aws-vault, doppler, mise and direnv forms ask without a row each. tmux reads a command table, splits at `;`, reads `OperandCommands`, and treats send-keys as `Typed` input (`-H` hex, `-l` literal, and editing keys such as `C-m` and `Enter`). screen `-X` is read as a flag. hyperfine substitutes `-L`/`-P` values. `entryForm` reads the last, the JSON and every `--entrypoint`. New risk rows: database resets (rake, rails, mix, django, prisma, sequelize, typeorm, flyway, liquibase), `helm uninstall`, `apply --prune` and `podman system reset`. The orchestrator probe (40 strings plus a 2,000-word timing case; counsel could not write probes) found `bundle exec rake db:drop` open; fixed. The final probe had 0 bad in 12 ms. Differential: 101 toward asking, 9 away (all intended), 0 level-only. Left for later: xargs input takes quadratic time (about 85 s for 2,000 words); tmux display-menu and command-prompt; the quoted `gcloud ssh --` form; `pip show rimraf` over-asks. Gate 0.                                                                                                                                                                                                                                                              |
| Extensions | Merged `6ff8bd3ac`. OpenAI login: a failed browser wait keeps the pending login, a scope finalizer ends it, and a browser callback racing a pasted code claims the login atomically (`claimLogin`), exchanges once and stores once; the loser waits on the winner's result. Compat providers send every system message with the `system` role. Skills: an unwritable bundle cache and a failed git-root walk degrade with a warning instead of failing. btw shows each step of a multi-step reply (`assistantMessageIdForTurn(messageId, step)`). Background jobs stream their output into the job's file as it arrives; the row message is bounded to 2,000 characters and names the file. On replay, an old row with no file writes the file from its stored text (additive; the stored format is unchanged). The interrupted-job notice says the file holds only the output written before the stop. History comments were rewritten to state today's rules. Counsel found the login race, the old-row replay gap and the notice text; all fixed with red tests first. Flake: btw pulses under load (passed on retry). Gate 0.                                                                                                                                                                                                                                                                                                                                                               |
| Context    | Merged `2f5c74fe3`. C19-1: the input ceiling is `min(context − reservedOutput, inputLimit)`, so OpenAI's input cap binds. The estimate is anchored on the last request's reported input: `StepMeasure` is `{replyId, inputTokens, overheadTokens}`, and `StreamEnded` gains an optional `requestOverheadTokens` (additive; a row without it falls back to chars/4). Only the units that request held are scaled, output does not count, and the overhead is that request's own (decided by derive-dont-sync). A provider refusal for length (`isContextOverflow`, one pattern list in domain/driver.ts) hands off once (`StepResult.HandOff`) and retries. A window with nothing summarizable drops its summary and keeps a truncation marker. Older markers after the anchor leave the window. A second refusal fails the turn with `CONTEXT_OVERFLOW_AGAIN`. `request_too_large` (413) is not an overflow: Anthropic sends it for the 32 MB body cap and image size (decided by prove-it-works). `ModelContextProjection` is a plain interface. `stopLatch()` replaces two reads. Counsel found the output double count, the 413 pattern and the handoff that could not shrink; all fixed with red tests first. Six mutation probes were red. ARCHITECTURE.md principle 17 was updated. Note: turbo printed "No space left on device" during the build with 191G and 81% of inodes free; it looks transient and did not fail the gate. Gate 0.                                                |
| Tooling    | Merged `9a5b08412`. The guide check compiles the code blocks of every steering doc. It fails closed on a missing tsc, zero blocks, or a context with no `node_modules` (`requireContextModules`). Its turbo inputs list the steering set exactly (root files, app and package AGENTS/CLAUDE, `docs/**` without research, testbeds, patches, skills), and `findUnhashedSteeringFiles` fails in both directions when the inputs and `isSteeringFile` differ. `findUnshippedSkillFiles` blanks comments before reading rows. The link pattern reads a title after the target. `findSharedTestHomes` reads the value expression (next line, template, join) rather than the line, and passes `mkdtemp`/`makeTempDirectory`. Every test home gets its own directory. `committedFilesCommand`: in a hook it reads the index git hands the hook (`GIT_INDEX_FILE`, which is the commit being made); outside a hook it reads the HEAD tree CI checks out. CI runs `actions/checkout@v7` on Node 24. safety.md allows the efficiency capture run (fake keys, scratch data dir, no paid model). Counsel found the nested-doc inputs, commented rows, the one-line home regex and staged files; all fixed with red tests first, and 12 mutation probes were red. Safety slip: the agent ran an empty `python3 -` (no heredoc; nothing ran). Gate 0.                                                                                                                                                        |
| Cell       | Merged `d0a3224b7`. The value reader moved to its own lint-scoped file, `packages/extensions/src/cell-value.ts`, because the codec, the error renderer and the display share it (decided by fix-root-causes). It calls only intrinsics saved at load through the saved `Reflect.apply`: bigint through the abstract `String`, Map and Set through the saved `forEach`, lists grown through the saved `defineProperty`, and no for-of, spread or `push`. A total `displayValue` replaces `inspect` for logged and returned values, uncaught reports and causes. It matches `inspect` on 63 ordinary values; a Proxy shows as `[Proxy]`, a getter as `[Getter]`, and it has property and value caps. `tools` and `context` are permanent accessors that are not configurable (decided by correctness). `sameDescriptor` compares the flags. A reset that cannot restore a global reports it in the optional `unrestored` field on the reset reply (wire only), and the kernel replaces the worker. Counsel found the bigint and iterator calls, the silent reset skip, a non-Error cause through `inspect`, and the display itself; all fixed with red tests first, and 10 mutation probes were red. Out of reach, documented: Effect's field assignment and the renderer's string work use shared intrinsics, so a cell that poisons them stalls the worker until the host replaces it on timeout. Safety slip: the agent ran an empty `python3 -` heredoc (nothing created or changed). Gate 0. |
| TUI        | Merged `8c482fa0d`. TUI19-1: a docked pane on a short terminal reads its real Yoga height before each draw, drops its rules under 3 rows, and at 0 rows draws nothing and takes no keys (`KeyboardGate`, `useScopedKeyboard`); Esc still closes a hidden held pane without cancelling the turn. TUI19-2: the feed backoff resets only after both the `StreamSynchronized` marker and the first watch item arrive, and each attempt's scope closes both subscriptions. TUI19-3: Ctrl+C closes a held pane, then cancels, then quits; the boot branch picker and enforced sign-in quit. TUI19-4/5: a session pane holds every composer key and paste. TUI19-6: `pickerHeight` counts 4 rows of chrome plus a `queryRow` flag (decided by redesign-from-first-principles). TUI19-7: each render gets its own scoped temp home. TUI19-8: server slash commands list again after a reconnect and settle per connection generation, so a command sent during the new listing waits. A bare Esc keybind is refused (`holdsEscape`). Counsel found the settled state kept across reconnects; fixed with a red test first. 19 mutation probes were red. Open product question: at 6-8 rows with a turn running, a pane gets no cursor row unless the composer's blank row gives way. Flakes: btw pulses, and the cell background-shell notice (both passed on retry). Gate 0.                                                                                                                            |

Round end: all six Pass 19 batches merged. Build warning cause: `TURBO_CACHE_DIR=/workspaces/.cache/turbo` is on the 60G `/workspaces` volume, which is 100% full (the turbo cache there is 23G); turbo skips its cache writes and the gate still passes. It is the Bite work volume, so it is left to the owner.
