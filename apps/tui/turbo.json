{
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "inputs": ["$TURBO_EXTENDS$", "scripts/**"]
    },
    "build": {
      "inputs": ["$TURBO_EXTENDS$", "scripts/**/*.ts"],
      "outputs": ["bin/gent", "bin/gent-cell"]
    }
  }
}
